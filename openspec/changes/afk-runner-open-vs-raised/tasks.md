# Tasks — afk-runner-open-vs-raised

Every task is red-first: the failing test lands before the implementation,
green before moving on. Landing order matters — the predicate (§1) is what
every later task reads. Prerequisite: the in-flight uncommitted prep (the
spec-home archive and the `assumption-blast.ts` extraction) lands as its own
commits before §1 starts.

## 1. The two count sets and the three-valued verdict

- [x] 1.1 Openness predicate over a single resolution. Red-first in `tests/afk-runner/work/review-model.test.ts`: a new exported predicate reports `dismissed` open; `evidence-answered` closed; `assumed` open when no assumption in the round carries its finding id and closed when one does; `assumed` closed under the legacy fallback when no assumption in the round carries any `findingId`; `edited` open when the round's artifact hash snapshot equals the prior round's and closed when they differ; a missing prior snapshot (round 1) reads `edited` as closed. Then implement it in `review-model.ts` taking the resolution, the round's assumptions, and the two hash snapshots as plain arguments — pure, no I/O. Verify: `bun test tests/afk-runner/work/review-model.test.ts`
- [x] 1.2 `evaluateConvergence` returns both sets and three verdicts. Red-first in the same file: the returned `raised` set is byte-identical to today's counts for every input (pin with the existing fixtures); `open` counts only what 1.1 calls open; verdict is `converged` with an empty open set above nitpick and ≤3 open nitpicks, `open` when anything above a nitpick is open, and `needs-review` when the open set passes the converged test but the round recorded an edit above a nitpick. Then widen `evaluateConvergence` in `review-model.ts`, keeping its existing signature additive (no-context overload) so `materialize.ts:59` compiles unchanged. Verify: `bun test tests/afk-runner/work/review-model.test.ts tests/afk-runner/work/materialize.test.ts`

## 2. Making `edited` and `assumed` checkable

- [x] 2.1 Optional `findingId` on the assumption record. Red-first in `tests/afk-runner/work/agent-layer.test.ts`: `AssumptionRecordSchema` validates records with and without `findingId`; the `A<n>` id regex is unchanged; pre-change sidecar fixtures still parse. Then add the optional field in `agent-layer.ts` and teach the resolver prompt in `review-model.ts` to carry the finding id it assumed against, warning that a no-op edit counts as unresolved. Verify: `bun test tests/afk-runner/work/agent-layer.test.ts`
- [x] 2.2 Per-round artifact hash snapshot. Red-first in `tests/afk-runner/work/materialize.test.ts` plus a gate-files test: closing a round writes `sidecars/round-hashes-<n>.json` over the same file set `recordArtifactHashes` already covers; a round whose change folder did not move produces a snapshot equal to the previous round's; a missing prior snapshot is tolerated; the regenerated-every-round files (`review.md`, `assumptions.md`) are excluded and a test pins that exclusion. Then export `listAgentArtifacts` from `gate-files.ts` and call the existing `recordArtifactHashes` from the round-close path in `review-loop.ts` — no new hashing code. Verify: `bun test tests/afk-runner/work/materialize.test.ts tests/afk-runner/work/review-loop.test.ts`

## 3. Event model and replay

- [x] 3.1 Additive `open` counts and the widened verdict on the convergence event. Red-first in `tests/afk-runner/event-schemas.test.ts`, the kernel fold tests, and `tests/afk-runner/legacy-fold.test.ts`: a `convergence` event stamps and round-trips with and without `open` and with each of the three verdicts; a pre-change line parses and folds with `open` reading as `raised`; `DigestRecord` carries both sets via the shared `open ?? counts` reader; the `finding` event's action enum is unchanged (pin it, so a later change cannot widen it by accident). Then extend `ConvergenceEvent` in `event-schemas.ts`, the kernel event union and `flushConvergence` in `kernel/machine.ts`, the translation in `kernel/fold.ts`, and `legacy-fold.ts`. Verify: `bun test tests/afk-runner/event-schemas.test.ts tests/afk-runner/legacy-fold.test.ts tests/afk-runner/kernel/ tests/afk-runner/parity/`

## 4. Review loop wiring

- [ ] 4.1 `ReviewLoopResult` carries verdict, raised, and the genuinely-open lists. Red-first in `tests/afk-runner/work/review-loop.test.ts`: the result exposes `verdict` and `raised` alongside per-class open findings computed by §1's predicate (reading the round's assumptions and hash snapshots); the convergence event emitted per round carries both sets; a round whose findings were all edited-with-changes reports an empty open set and a `needs-review` verdict; round close writes the §2.2 snapshot before the `round_close` event. Then wire `review-loop.ts`. Verify: `bun test tests/afk-runner/work/review-loop.test.ts`
- [x] 4.2 Sidecar re-read agrees with the live loop. Red-first in the gate-settle tests: `readReviewResultFromSidecars` applies the identical predicate, so a resumed run's gate sees the same open set the live run would have. Then update `gate-settle.ts`. Verify: `bun test tests/afk-runner/work/gate-settle.test.ts tests/afk-runner/work/gate-settle-final.test.ts`

## 5. Cap-hit routing and the verification round

- [ ] 5.1 Routing by three verdicts, bound in the fold. Red-first in `tests/afk-runner/work/review-work.test.ts` and `tests/afk-runner/work/review-outcome.test.ts`: `converged` at cap flows to the tail with no gate; `open` at cap presents the early gate; `needs-review` at cap re-enters the loop for exactly one further round (`round_open(n+1, cap+1)` appended once, owedness rule respected) and then settles by that round's open set — an open MATERIAL from the verification round still presents the early gate; a second `needs-review` from that round buys nothing (the fold carries a later round's verdict); `reviewOutcomeOf` treats `needs-review` as unsettled until a later round's verdict is recorded, and a pre-change log (no open set) resumes exactly as today. Then implement the routing in `review.ts` (`runReviewWork`) above the loop. Verify: `bun test tests/afk-runner/work/review-work.test.ts tests/afk-runner/work/review-outcome.test.ts tests/afk-runner/resume-equivalence.test.ts`
- [ ] 5.2 The budget guard declines the verification round. Red-first in the same files: with projected spend at or over a non-null ceiling, or metered with unknown cost, a `needs-review` cap-hit flows to the tail without spending the round and without appending an `auto_decision`; a null ceiling (unmetered) never refuses; an unmetered run with unknown cost still gets the round. Then export `projectedSpend` from `auto-policy.ts` and apply the metered guard in the routing. Verify: `bun test tests/afk-runner/work/review-work.test.ts tests/afk-runner/work/auto-policy.test.ts`

## 6. The ladder

- [ ] 6.1 R1 and the never-cut blocker pre-check read the open set. Red-first in `tests/afk-runner/work/auto-policy.test.ts`: a round that raised and edited-with-changes every finding auto-approves under R1; a dismissed finding of any severity blocks it; a resolved BLOCKER alone no longer forces a gate while a dismissed one still does. The lists already became genuinely open in §4, so this lands as tests pinning the split against a plausible refactor. Verify: `bun test tests/afk-runner/work/auto-policy.test.ts`
- [ ] 6.2 R2 splits its two questions. Red-first in the same file: eligibility reads the open set (a fixed blocker no longer blocks it; an empty open MATERIAL set makes R2 inapplicable) while `strictlyDecreasingLastK` keeps reading raised totals; the budget guard is unchanged. Verify: `bun test tests/afk-runner/work/auto-policy.test.ts`
- [ ] 6.3 The integrity cross-check compares both sets. Red-first in a new `tests/afk-runner/work/gate-integrity.test.ts` plus `tests/afk-runner/work/gate-prelude.test.ts`: recomputing both count sets from the resolver sidecar and round hash snapshots and comparing against the round's convergence record returns clear on agreement; a mismatch in either set, or an unparseable sidecar, substitutes an open `POLICY-INTEGRITY` BLOCKER so no rule can auto-decide and the gate waits for a human; a pre-split convergence line (no open set) folds its open set equal to its raised set for the comparison. Then implement `work/gate-integrity.ts` (async reads) and wrap it in `signalsOf` in `gate-prelude.ts`. Verify: `bun test tests/afk-runner/work/gate-integrity.test.ts tests/afk-runner/work/gate-prelude.test.ts`

## 7. Gate evidence

- [ ] 7.1 Export the findings/resolutions joiner. Red-first in `tests/afk-runner/work/materialize.test.ts`: the joiner is exported, reads both `findings-<n>.json` and `findings-skeptic-<n>.json`, merges them before pairing with resolutions, and returns each finding's verbatim gap keyed by id; a missing sidecar yields an empty join rather than throwing; `review.md`'s bytes are unchanged. Then export it from `materialize.ts`. Verify: `bun test tests/afk-runner/work/materialize.test.ts`
- [ ] 7.2 Gate rows carry the sanitized gap. Red-first in the `gate-signals`, `gate-settle`, and `gate-render` tests: `findingsOf` and `expectedContentFor` carry the joined gap instead of `gap: entry.id`, sanitized — collapsed to one line, leading redirect marker stripped, truncated at 200; a gap with newlines renders as one truncated line; a gap beginning with a redirect marker does not corrupt the parse; a finding absent from the sidecars degrades to its identifier. Then update `gate-signals.ts` and `gate-settle.ts` with a shared sanitizer helper. Verify: `bun test tests/afk-runner/work/gate-signals.test.ts tests/afk-runner/work/gate-settle.test.ts tests/afk-runner/work/gate-render.test.ts`
- [ ] 7.3 The write-then-parse self-check covers the new row text. Red-first in `tests/afk-runner/work/gate-answers.test.ts`: a gate rendered from answers whose finding rows carry multi-line and arrow-leading gaps parses back to the same response; `renderGateAnswers` and `responseFromAnswers` flatten every free-text field they write. Then update `gate-answers.ts`. Verify: `bun test tests/afk-runner/work/gate-answers.test.ts`

## 8. Full verification and docs

- [ ] 8.1 Full gates and documentation. Run one full `bun run test` (never two concurrently; serial if load-demoted), `bun run typecheck`, `bun run lint`, `bun run format:check`, and `openspec validate --strict "afk-runner-open-vs-raised"` — all green, with the parity harness and memo oracle green inside the sweep. Update `docs/architecture/afk-runner.md` (raised-vs-open, the verification round and its budget refusal, the integrity cross-check, sanitized gap rows) and `docs/architecture/sdd-pipeline.md` (the review-loop convergence predicate, cap-hit routing, the convergence event shape, gate digest rows). Verify: all five commands exit green.
