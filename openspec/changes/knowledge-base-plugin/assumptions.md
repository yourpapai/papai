<!-- GENERATED by sdd-runner from JSON sidecars - regenerated wholesale per round. -->

## Assumptions

### A-F14-guest-rate-limit: Guest ask_knowledge_base spam amplified by defaultEnabled:true is bounded by existing per-user web-fetch quota, tool_prefs allow/ask/deny gate, BYOK per-group LLM budget, and sequential job batching; no dedicated per-guest embed/generateText rate limit added in Phase 1.
- **Basis**: convention
- **Confidence**: medium
- **Blast radius**: Unrecognized guest can invoke embed+retrieve+generateText every turn at group LLM cost; worst-case N guests * QPS scales with group size until per-group quotas/BYOK budgets trigger
- **Status**: open

### A-FEEDBACK-RETENTION: No retention/TTL or cardinality bound on knowledge_base_feedback (config_context_id, answer_id, voter) in phase 1; orphaned sha256 answer_ids accumulate per group until future cleanup job; voter GDPR deletion follows existing user-deletion path; guest votes stored but never expire
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: Unbounded storage growth per group proportional to ask_knowledge_base calls; no auto-cleanup on message delete in phase 1
- **Status**: open

### A3: Plugin-owned activeSweep per-owner flag is the intended skip-if-running enforcement; core scheduler's setInterval not being fixed is by design and the plugin guard is sufficient to prevent concurrent bun:sqlite sweeps.
- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: src/utils/scheduler.internal.ts + specs/knowledge-base/indexing/spec.md Scheduled jobs + design Decision 6
- **Status**: open

### A4: Compound index on (group_context_id, timestamp, message_id) is the correct backing for the watermark resume query; single-column group_ctx index is insufficient for the timestamp/messageId range + ORDER BY per 15min tick.
- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: src/db/schema.ts + src/db/migrations/<seq>_knowledge_base.ts + specs/knowledge-base/indexing/spec.md
- **Status**: open

### A-ANSWER-HASH: answerHash is sha256 of final delivered answer string after truncation/tail (or fallback literal), empty => sha256(""); answerId = sha256(configContextId|Date.now() decimal|answerHash) 64-char lowercase hex
- **Basis**: default
- **Confidence**: high
- **Blast radius**: specs/knowledge-base/spec.md feedback + plugins/knowledge-base ask_knowledge_base mint + knowledge_base_feedback joins
- **Status**: open

### A-DISTILL-INTERVAL-OPTIONAL: distill_interval_ms optional context key defaults 15*60*1000; no cron; plugin reads interval at tick start
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: specs/knowledge-base/indexing/spec.md scheduled jobs + plugins/knowledge-base distill scheduler
- **Status**: open

### A2: Lexicographic TEXT ordering of messageId is sufficient for watermark resume because timestamp is primary and millisecond granularity makes same-timestamp ties rare; lexical tie-break gives a total order consistent with ORDER BY even when numeric IDs would sort differently.
- **Basis**: code-evidence
- **Confidence**: medium
- **Blast radius**: specs/knowledge-base/indexing/spec.md resume query + src/db/schema.ts message_metadata + knowledge_base_cursors watermark
- **Status**: open

### A-PERMALINK-SYNTHETIC: channel source_ref permalink for message_metadata ingestion is papai://thread/<config_context_id>/<thread_id> (thread_id = root messageId, orphan own id); stable internal URL for LIKE purge and citation dedup
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: specs/knowledge-base/indexing/spec.md judge+distill + knowledge_base_chunks source_ref + answer tail
- **Status**: open

### A-CURATED-URL-ALLOWLIST: curated_kb_url httpFetch enforces providerAllowedHosts + public-URL tiering identical to GitLab README
- **Basis**: convention
- **Confidence**: medium
- **Blast radius**: specs/knowledge-base/indexing/spec.md curated KB + providerRuntime.httpFetch SSRF guard
- **Status**: open

### A-THREAD-ID-ROOT: thread_archive.thread_id equals thread root messageId; orphan chains use own messageId so PK (config_context_id, thread_id) stable
- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: specs/knowledge-base/indexing/spec.md crawl + knowledge_base_thread_archive PK + distill purge
- **Status**: open

### A1: Deferring system_ type and no-bot/no_bot reaction exclusions until message_metadata gains those columns is acceptable; retroactive reaction purge and system_ filtering are no-ops in this change with reactions_checked_at reserved for the future.
- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: specs/knowledge-base/indexing/spec.md Channel crawl requirement + specs/knowledge-base/spec.md Opt-out requirement + plugins/knowledge-base ingest
- **Status**: open

### A-FIFO-TIMEOUT: Local curated_kb_path streaming read enforces 5_242_880 byte-counted cap plus 5s AbortSignal timeout; FIFO/char-device/hung read aborts with warning, skips that source for the tick, and does not hold activeSweep flag beyond tick duration.
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: Single reindex tick for one configContextId skips curated KB source on hang; next 30-min tick retries; no cross-group sweep starvation
- **Status**: open

### A-F13-chunk-quota: Phase 1 corpus per config_context_id stays <10k chunks (curated KB + bounded distilled threads); no per-group quota/eviction/TTL enforced — brute-force cosine loads full partition and is acceptable; add maxScore/count observability and pagination guard only if threshold exceeded.
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: Retrieval latency/event-loop blocking grows linearly with chunks per group; degrades live ask_knowledge_base turns if group exceeds 10k — mitigated by per-group quota in Phase 2
- **Status**: open

### A-CHUNK-QUOTA: Phase 1 per-config_context_id corpus stays <10k chunks (curated KB + bounded distilled threads); no per-group quota/TTL/LRU or scan pagination — brute-force cosine loads full partition per ask_knowledge_base call; operational guard emits maxScore/count and warns if >10k, hard quota deferred to future change
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: Retrieval latency/event-loop blocking and SQLite BLOB memory per live turn grows linearly with chunks per group; exceeds 10k degrades ask_knowledge_base
- **Status**: open

### F5: The 60s overlapping resume window is accepted as the phase-1 safeguard; messages arriving more than 60s after the watermark has advanced are permanently skipped until a future full re-scan/backfill (e.g., the revalidate job extended to re-walk thread roots) is added. No per-message retry or larger window is introduced now.
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: Rare corpus gaps for late-delivered/backfilled channel messages; affects retrieval completeness only, never correctness of served chunks or secrets.
- **Status**: open

### A-CONFIG-KEYS-SNAKE: knowledge-base configRequirements keys are snake_case per configKeySchema (curated_kb_path etc.); design camelCase was editorial error
- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: openspec/changes/knowledge-base-plugin/design.md Decision 6 + src/plugins/manifest-validation.ts + src/plugins/types.ts
- **Status**: open

### A-PER-CONTEXT-WALLCLOCK: DISTILL_BATCH_LIMIT defaults to 50 and per-context wall-clock budget is 60s with per-LLM-call timeout; JUDGE_VERSION bump draining large pending queue slices per tick and isolates per-context failures via try/catch per contextId.
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: One slow group delays its own pending queue by one 15-min tick, does not starve other groups or block reindex (separate owner plugin:knowledge-base:reindex) beyond busy_timeout=5000 contention
- **Status**: open

### A-SCHEDULER-OVERLAP: Skip-if-running per job owner (plugin:knowledge-base:reindex/distill) when previous sweep exceeds intervalMs; at most one sweep per job runs on bun:sqlite at a time with per-LLM-call timeout; sequential reduce chain plus busy_timeout=5000 mitigates SQLITE_BUSY
- **Basis**: convention
- **Confidence**: medium
- **Blast radius**: Missed reindex/distill tick when sweep overruns interval; one slow group's LLM timeout delays later groups in same tick
- **Status**: open

### A-ANSWER-ID: answer_id minted as UUIDv4 or sha256(configContextId|timestamp|answerHash) per ask_knowledge_base call, returned as answerId, stable for feedback lifetime of that bot message; regeneration mints new id
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: feedback joins and vote deduplication; collision would orphan or merge votes
- **Status**: open

### A-WATERMARK-ORDER: Cursors watermark is lexicographic tuple (timestamp INTEGER ASC, messageId TEXT ASC) with resume WHERE (timestamp, messageId) > (wt_ts, wt_id) and 60s overlapping window for clock skew; partitioned by groupContextId mapped from configContextId
- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: distill ingestion resumability; wrong ordering would skip or duplicate threads on out-of-order inserts
- **Status**: open

### A5: KNOWLEDGE_BASE_DATA_DIR mkdir failure should surface via plugin_runtime_events warning (not just pino.warn) so admin settings validation can observe the corpus-empty state; ask_knowledge_base correctly stays eligible (required:false) rather than gating ineligible.
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: design Decision 5 + specs/knowledge-base/indexing/spec.md Curated KB requirement + plugin runtime events
- **Status**: open

### A-KB-DATA-DIR: KNOWLEDGE_BASE_DATA_DIR defaults to ./data/knowledge-base, jail via path.resolve + startsWith + realpath symlink check; bytesLimit 5_242_880 (5 MiB) for curated KB file and httpFetch, rateLimit 10 req/s per context for httpFetch only
- **Basis**: convention
- **Confidence**: medium
- **Blast radius**: curated KB ingestion per group — wrong dir would block reindex, wrong limits would truncate or reject valid KB
- **Status**: open
