# Tasks: knowledge-base plugin

Test-first order per `design.md` and Write/Edit TDD hook: every implementation file under `src/`, `plugins/`, or `client/` is gated by the hook, so the failing companion test lands before the implementation it covers.

## 1. DB migration and scope wiring

- [ ] 1.1 Write failing migration test `tests/db/migrations/knowledge_base.test.ts` asserting the four tables `knowledge_base_chunks`, `knowledge_base_thread_archive`, `knowledge_base_cursors`, `knowledge_base_feedback` exist with correct columns/PKs, compound indexes `(config_context_id, source_type, source_ref)` and `(config_context_id, judged_at, root_created_at)`, plus `idx_message_metadata_group_ctx_ts_msg ON message_metadata(group_context_id, timestamp, message_id)` — `bun test tests/db/migrations/knowledge_base.test.ts`
- [ ] 1.2 Create hand-written migration `src/db/migrations/<seq>_knowledge_base.ts` (`<seq>` = next sequential number after current max in `src/db/migrations/` — `079` when max is `078` — per `src/db/migrate.ts:validatePairwiseOrder`) exporting `Migration { id, up(db: Database) }` via `src/db/migrate.ts` (not `drizzle-kit generate`), creating `knowledge_base_chunks` (`id INTEGER PRIMARY KEY, config_context_id TEXT NOT NULL, source_type, source_ref, content, embedding BLOB NOT NULL, content_hash, indexed_at, kind, source_date` + UNIQUE `(config_context_id, source_type, source_ref)`), `knowledge_base_thread_archive` (composite PK `(config_context_id, thread_id)` where `thread_id` = root `messageId`, cols `root_created_at, last_post_at, raw_json, content_hash, score, judge_json, judged_hash, judge_version, judge_attempts, judged_at, reactions_checked_at`, index `(config_context_id, judged_at, root_created_at)`), `knowledge_base_cursors` (`config_context_id TEXT PRIMARY KEY, watermark_ts INTEGER NOT NULL, watermark_id TEXT NOT NULL`), `knowledge_base_feedback` (`config_context_id TEXT NOT NULL` + PK `(config_context_id, answer_id, voter)`), and `idx_message_metadata_group_ctx_ts_msg` (`CREATE INDEX IF NOT EXISTS`) — `bun test tests/db/migrations/knowledge_base.test.ts`
- [ ] 1.3 Write failing schema-validation test `tests/db/knowledge-base-schema.test.ts` asserting `sqliteTable` entries for the four tables exist with `BLOB NOT NULL` for `embedding` and matching indexes — `bun test tests/db/knowledge-base-schema.test.ts`
- [ ] 1.4 Declare `sqliteTable` entries for the four tables in `src/db/schema.ts` (or `src/db/knowledge-base-schema.ts` re-exported from `schema.ts`) with `BLOB NOT NULL` for `embedding` and matching indexes — `bun test tests/db/knowledge-base-schema.test.ts && bun run typecheck`
- [ ] 1.5 Write failing mapping test for `knowledge_base_*` in `src/chat/context-scope.ts:38` asserting `getScopeKey('group', …)` resolves to `group` scope — `bun test tests/chat/context-scope.test.ts`
- [ ] 1.6 Register the four tables in `src/chat/context-scope.ts:38` `ENTITY_SCOPES` as `group`-scoped and satisfy `CONTEXT_OWNED_COLUMNS` consistency guard — `bun test tests/chat/context-scope.test.ts`
- [ ] 1.7 Set `PRAGMA busy_timeout=5000` at DB connection open in `src/db/index.ts` and `src/db/drizzle.ts` (not inside the migration transaction per `src/db/migrate.ts:114` — PRAGMAs inside transactions are silently ignored) and add per-transaction retry on `SQLITE_BUSY` for `DELETE+INSERT` paths; verify live `ask_knowledge_base` reads do not surface `SQLITE_BUSY` under concurrent scheduled writes — `bun test tests/db/busy-timeout.test.ts`

## 2. Plugin scaffold and manifest

- [ ] 2.1 Write failing manifest validation test for `plugins/knowledge-base/plugin.json` asserting `apiVersion: 1`, `id: "knowledge-base"`, `storageScope: "group"`, `permissions: ["storage","scheduler","http"]`, `contributes: { tools: ["ask_knowledge_base"], jobs: ["reindex","distill"], configKeys: [...] }`, `providerAllowedHosts` + `providerAllowedHostsFromConfig` (for optional GitLab README and `curated_kb_url`), `defaultEnabled: true` — `bun test tests/plugins/knowledge-base-manifest.test.ts && bun run typecheck`
- [ ] 2.2 Create `plugins/knowledge-base/plugin.json` with `apiVersion: 1`, `id: "knowledge-base"`, `storageScope: "group"`, `permissions: ["storage","scheduler","http"]`, `contributes: { tools: ["ask_knowledge_base"], jobs: ["reindex","distill"], configKeys: [...] }`, `providerAllowedHosts` + `providerAllowedHostsFromConfig` (for optional GitLab README and `curated_kb_url`), `defaultEnabled: true` — `bun test tests/plugins/knowledge-base-manifest.test.ts && bun run typecheck`
- [ ] 2.3 Add `configRequirements` snake_case keys (`curated_kb_path`, `curated_kb_url`, `gitlab_host`, `gitlab_project`, `gitlab_ref`, `kb_min_value_score`, `retrieval_min_score`, `distill_batch_limit`, reserved `distill_interval_ms`) with `scope: context`, `required: false` (curated KB/GitLab optional so `config_missing` does not gate eligibility), `sensitive: true` for `gitlab_token`/BYOK, and `configKeySchema` validation — `bun test tests/plugins/knowledge-base-config.test.ts`
- [ ] 2.4 Add explicit `TOOL_METADATA['plugin_knowledge_base__ask_knowledge_base'] = read('plugin')` in `src/tools/tool-metadata.ts` overriding the `plugin_` open-world fallback, and declare capability `knowledge-base` / `knowledge-base/indexing` — verify `riskDefaults[read]` / `domainDefaults[plugin]` resolution and that `TOOL_DOMAINS` does not gain a `knowledge-base` entry — `bun test tests/tools/tool-metadata.test.ts`
- [ ] 2.5 Wire plugin discovery/approval (`plugin_admin_state.approvedManifestHash`) and per-context enable (`plugin_context_state`); verify `null` `task_instance_id` eligibility for live tool vs `configuredDefaults` filtering for scheduled jobs — `bun test tests/plugins/knowledge-base-eligibility.test.ts`

## 3. Pure indexing pipeline (normalize / scrub / chunk / vec)

- [ ] 3.1 Write failing unit tests for `plugins/knowledge-base/src/indexer/normalize.ts` (strip fences to raw code, strip HTML, remove image markup, inline links `text (url)`, strip backticks, collapse whitespace) — `bun test tests/knowledge-base/normalize.test.ts`
- [ ] 3.2 Implement `plugins/knowledge-base/src/indexer/normalize.ts` — `bun test tests/knowledge-base/normalize.test.ts`
- [ ] 3.3 Write failing tests for `plugins/knowledge-base/src/indexer/scrub.ts` (`Bearer`, `key=value`, `glpat`/`ghp`/`xox`, `AKIA`, 32-hex, JWT → `[REDACTED]`; narrow patterns keep `ISSUE-123`/tracker URLs) — `bun test tests/knowledge-base/scrub.test.ts`
- [ ] 3.4 Implement `plugins/knowledge-base/src/indexer/scrub.ts` (run **before** normalize) — `bun test tests/knowledge-base/scrub.test.ts`
- [ ] 3.5 Write failing tests for `plugins/knowledge-base/src/indexer/chunk.ts` (heading split, target 500 / overlap 50 `cl100k_base`, hard-split at codepoint+line boundaries, multi-byte safe `Array.from`, markdown tables not mid-word) — `bun test tests/knowledge-base/chunk.test.ts`
- [ ] 3.6 Implement `plugins/knowledge-base/src/indexer/chunk.ts` using papai's `ai-tokenizer`; add golden-fixture test pinning `cl100k_base` counts — if `ai-tokenizer` diverges >10% on curated KB fixture (≈ >2 tokens/500), vendor `@dqbd/tiktoken` as plugin-local dep — `bun test tests/knowledge-base/chunk.test.ts tests/knowledge-base/tokenizer-fixture.test.ts`
- [ ] 3.7 Write failing tests for `plugins/knowledge-base/src/kb/vec.ts` (`toBlob`/`fromBlob` float32 round-trip, buffer-copy note, `cosine` including non-finite/all-zero handling) — `bun test tests/knowledge-base/vec.test.ts`
- [ ] 3.8 Implement `plugins/knowledge-base/src/kb/vec.ts` — `bun test tests/knowledge-base/vec.test.ts`
- [ ] 3.9 Write failing tests for `plugins/knowledge-base/src/indexer/pipeline.ts` (scrub→normalize→chunk→embed batch `EMBED_BATCH=32` + `p-limit(3)`, `toBlob` serialization, transactional `DELETE WHERE config_context_id=? AND source_type=? AND source_ref=?` then `INSERT`s, `content_hash` sha256, empty-normalize still deletes prior chunks, embed count-mismatch fails closed with no commit) — `bun test tests/knowledge-base/pipeline.test.ts`
- [ ] 3.10 Implement `plugins/knowledge-base/src/indexer/pipeline.ts` with papai `bun:sqlite`/`drizzle` handles, keeping synchronous `transaction(() => { DELETE; INSERTs })` and `busy_timeout` retry — `bun test tests/knowledge-base/pipeline.test.ts`

## 4. Curated KB and GitLab README reindex path

- [ ] 4.1 Write failing tests for `curated_kb_path` validation: allowlisted, no traversal, sandboxed to `KNOWLEDGE_BASE_DATA_DIR` (env optional, default `./data/knowledge-base` resolved once at startup via cached `path.resolve(process.cwd(), KNOWLEDGE_BASE_DATA_DIR)`; absolute env used directly), jail via `path.resolve` + `startsWith` with directory-boundary `(resolved === dataDir || resolved.startsWith(dataDir + path.sep))` then `realpath` + `startsWith` re-check, atomic `open` with `O_NOFOLLOW` (fail closed on `ELOOP`/`EPERM`, fd `realpath` re-verify), `fstat` size check, `bytesLimit: 5_242_880` as `fstat` pre-check + byte-counted streaming cap + 5s `AbortSignal` timeout (FIFO/hung → warning, skip source, no `activeSweep` hold), `mkdir 0755` on demand (respect umask; `EACCES`/`EROFS` → `plugin_runtime_events` warning, not throw) — `bun test tests/knowledge-base/curated-kb-path.test.ts`
- [ ] 4.2 Implement curated KB local read (`fs` streaming, cap enforced even if file grows after `fstat` or is sparse/pipe; unbounded `fs.readFile` does not satisfy spec; `fstat > limit` skips with warning, prior chunks intact) — `bun test tests/knowledge-base/curated-kb-path.test.ts`
- [ ] 4.3 Write failing tests for `curated_kb_url` `providerRuntime.httpFetch` (tiered `providerAllowedHosts` + `providerAllowedHostsFromConfig` — admin bypass vs context HTTPS+public-IP via `ipaddr.js`, `bytesLimit: 5_242_880`, `rateLimit` only for `httpFetch`, allowlist-blocked → skip with structured warning without leaking token) — `bun test tests/knowledge-base/curated-kb-fetch.test.ts`
- [ ] 4.4 Write failing tests for GitLab README `providerRuntime.httpFetch` (tiered `providerAllowedHosts` + `providerAllowedHostsFromConfig`, `bytesLimit: 5_242_880`, `rateLimit` only for `httpFetch`, ETag/SHA cursor for reindex skip when unchanged, allowlist-blocked → skip with structured warning without leaking token) — `bun test tests/knowledge-base/curated-kb-fetch-gitlab.test.ts`
- [ ] 4.5 Implement `curated_kb_url` / GitLab README fetch (`httpFetch` with `bytesLimit`/`rateLimit`, ETag/SHA cursor for reindex skip when unchanged) — `bun test tests/knowledge-base/curated-kb-fetch.test.ts && bun test tests/knowledge-base/curated-kb-fetch-gitlab.test.ts`
- [ ] 4.6 Wire `reindex` source orchestration: hash/SHA change detection, idempotent delete-then-insert per `source_type`/`source_ref`, curated-KB failure does not purge GitLab chunks and vice versa, unchanged skips embed — `bun test tests/knowledge-base/reindex.test.ts`

## 5. Channel thread archive ingestion (message_metadata crawl)

- [ ] 5.1 Write failing tests for batched ingestion from `message_metadata` by `groupContextId` (`WHERE groupContextId = ?` via `idx_message_metadata_group_ctx`, `idx_message_metadata_group_ctx_ts_msg` backing `WHERE (timestamp, messageId) > (wt_ts, wt_id)` `ORDER BY timestamp ASC, messageId ASC` `LIMIT 200` per tick, `groupContextId === configContextId` via `getScopeKey('group', {storageContextId, chatUserId, contextType})` per `src/bot-message-caching.ts:22-29`, no `known_group_contexts` join) — `bun test tests/knowledge-base/ingest.test.ts`
- [ ] 5.2 Implement batched loader with in-memory `replyToMessageId` chain walk (visited-set cycle guard, max depth 100, cross-batch missing parent → orphan thread root, orphans grouped as own thread) and cursor watermark `knowledge_base_cursors` (`MAX(timestamp, messageId)` lexicographic, `WHERE timestamp >= watermark_ts - 60 AND (timestamp, messageId) > (watermark_ts, watermark_id)` overlapping 60s window; watermark advances only after successful batch commit in same `db.transaction()` so mid-batch kill rolls back watermark) — `bun test tests/knowledge-base/ingest.test.ts`
- [ ] 5.3 Write failing tests for NULL `groupContextId` backfill piggybacked on `plugin:knowledge-base:distill` first step (periodic bounded `LIMIT 200` scan of `WHERE groupContextId IS NULL`, per-row `getScopeKey` backfill or skip, per-run `pino` + `plugin_runtime_events` counters `{scanned, backfilled, skipped}`, failure isolated) — `bun test tests/knowledge-base/null-backfill.test.ts`
- [ ] 5.4 Implement NULL backfill — `bun test tests/knowledge-base/null-backfill.test.ts`
- [ ] 5.5 Write failing tests for archive mapping to `knowledge_base_thread_archive` ((config_context_id, thread_id) PK where `thread_id` = root `messageId` (orphan → own `messageId`), no `channel_id` column, `raw_json` = JSON array of thread's `message_metadata` rows, `content_hash` = sha256 over scrubbed+normalized texts only, `root_created_at`/`last_post_at` = min/max `timestamp`) and exclusion rules (normative: root authored by bot via `authorId`, `:no-bot:` in any message text → never write; `type` starting `system_` and `no-bot`/`no_bot` reactions require absent columns and are no-op deferred, `reactions_checked_at` reserved) plus bot-post stripping and author de-identification (`[1]/[2]` numbering) before archive/judge — `bun test tests/knowledge-base/archive.test.ts`
- [ ] 5.6 Implement archive upsert and retroactive `:no-bot:` purge (`DELETE` archive row + `DELETE FROM knowledge_base_chunks WHERE config_context_id=? AND source_type='channel' AND source_ref LIKE '<escaped papai://thread/...>#%' ESCAPE '\'` in one transaction when a previously archived thread later gains `:no-bot:`) — `bun test tests/knowledge-base/archive.test.ts`

## 6. LLM judge and distillation to channel chunks

- [ ] 6.1 Write failing tests for `JUDGE_VERSION` constant co-located with prompt, judge prompt (JSON `{score: 0..1, items: [{kind: 'qa'|'issue', text}]}` tolerant of fences/prose) and per-config LLM creds (central + `ByokLlmCredentials` override resolved via `contextConfig`/`adminConfig` at job time, Vercel AI SDK) — `bun test tests/knowledge-base/judge.test.ts`
- [ ] 6.2 Implement prompts (adapted to papai LLM facade — `providerRuntime` only for `httpFetch`, not for LLM) — `bun test tests/knowledge-base/judge.test.ts`
- [ ] 6.3 Implement `indexer/judge.ts` adapted to papai LLM facade — `bun test tests/knowledge-base/judge.test.ts`
- [ ] 6.4 Implement `indexer/distiller.ts` adapted to papai LLM facade — `bun test tests/knowledge-base/judge.test.ts`
- [ ] 6.5 Implement `indexer/archive.ts` adapted to papai LLM facade — `bun test tests/knowledge-base/judge.test.ts`
- [ ] 6.6 Write failing tests for pending selection (`SELECT ... WHERE judged_at IS NULL OR content_hash <> judged_hash OR judge_version <> :current ORDER BY root_created_at LIMIT DISTILL_BATCH_LIMIT`, sequential one-LLM-call-at-a-time, bump of `JUDGE_VERSION` re-judges entire history with zero crawl, content_hash change re-selects without bump) — `bun test tests/knowledge-base/judge-pending.test.ts`
- [ ] 6.7 Implement `selectPending` with `DISTILL_BATCH_LIMIT` SQL LIMIT and `JUDGE_VERSION` lockstep — `bun test tests/knowledge-base/judge-pending.test.ts`
- [ ] 6.8 Write failing tests for threshold and distillation (`KB_MIN_VALUE_SCORE` default 0.4, `score >= threshold` + `items.length>0` → one chunk per item via shared pipeline with `source_type='channel'`, `source_ref='papai://thread/<config_context_id>/<thread_id>#<n>'` preserving `#n` fragment, `kind`/`source_date` from `root_created_at`, synthetic `papai://` id for purge/citation dedup before `#`; unconditional purge `DELETE ... LIKE '<escaped permalink>#%' ESCAPE '\'` before insert so re-distillation leaves no orphans; below-threshold → stamped `score`/`judge_json`/`judged_hash`/`judge_version`/`judged_at` with zero chunks) — `bun test tests/knowledge-base/distill.test.ts`
- [ ] 6.9 Implement judge threshold, per-item chunk creation via pipeline, and unconditional purge — `bun test tests/knowledge-base/distill.test.ts`
- [ ] 6.10 Write failing tests for distillation error handling (`MAX_JUDGE_ATTEMPTS=5`, per-thread try/catch with warning containing only `threadId` + response length never text, left unstamped for retry; after 5 consecutive failures for same `judge_version`/`content_hash` → stamped `score=0` with `judge_json={error:…}` and excluded from `selectPending` until version/hash changes; embed failure mid-item leaves unstamped so next tick re-deletes+redoes all items; cap-reached purge removes partial writes before terminal stamp; kill mid-`DELETE+INSERT` rolls back with no partial chunks) — `bun test tests/knowledge-base/distill-errors.test.ts`
- [ ] 6.11 Implement attempt cap, per-thread isolation, partial-embed cleanup, and transaction rollback parity (`bun:sqlite` vs `better-sqlite3`) — `bun test tests/knowledge-base/distill-errors.test.ts`

## 7. Tiered retrieval and grounded answer synthesis (ask_knowledge_base tool)

- [ ] 7.1 Write failing tests for `plugins/knowledge-base/src/kb/retrieve.ts` (tiered retrieval with `config_context_id` partition (`SELECT ... WHERE config_context_id=? AND <tierSql>`), tiers `HIGH_TIER_SQL source_type IN ('readme','curated_kb')` / `LOW_TIER_SQL` (unvetted including `channel`), top-k=6, `RETRIEVAL_MIN_SCORE` gate on high-tier best (empty/below → serve low tier only; pass → high first + fill `k-highCount` from low ranked after), `maxScore` as best among served — `bun test tests/knowledge-base/retrieve.test.ts`
- [ ] 7.2 Implement `plugins/knowledge-base/src/kb/retrieve.ts` (keep `HIGH_TIER_SQL`/`LOW_TIER_SQL` identical, add `AND config_context_id=?`, `log.warn` dimension path includes `configContextId`) — `bun test tests/knowledge-base/retrieve.test.ts`
- [ ] 7.3 Write failing tests for dimension guard and degenerate vectors (dropped `byteLength !== queryDims*4` with warn `{id, sourceRef, dims, queryDims}`, non-finite cosine dropped with warn, all-zero/non-finite query vector → structured error fail-closed with no fabricated citations, single bad row does not throw when others scorable, expired dims naturally age out on next reindex) — `bun test tests/knowledge-base/retrieve-guards.test.ts`
- [ ] 7.4 Implement dimension guard, non-finite drop, all-zero fail-closed — `bun test tests/knowledge-base/retrieve-guards.test.ts`
- [ ] 7.5 Write failing tests for prompt assembly (`buildAnswerMessages`/`renderContext` with tier labels `readme/documentation`, `curated KB`, `support-channel thread` ordered by priority, context/tool outputs marked untrusted) — `bun test tests/knowledge-base/prompt.test.ts`
- [ ] 7.6 Implement prompt assembly with tier labels and citation instruction (`[n]`/`[n,m]`, `CITATION_MARKER` `(?!\()`) — `bun test tests/knowledge-base/prompt.test.ts`
- [ ] 7.7 Write failing tests for citation linking/linking-dedup and stripping (`linkCitations` → markdown links, `stripTrailingSourcesList` only when canonical tail exists, `CITATION_MARKER` prevents model-wrapped link laundering, uncited excerpts absent from tail, tail `Источники:` header, framing/injection via retrieved chunks treated as untrusted data) — `bun test tests/knowledge-base/citations.test.ts`
- [ ] 7.8 Implement `linkCitations`/`stripTrailingSourcesList`/`CITATION_MARKER` with tail dedup before `#` — `bun test tests/knowledge-base/citations.test.ts`
- [ ] 7.9 Write failing tests for truncation and platform caps (truncate **body** to `min(4000, platformMax-96)` at paragraph boundary with `…(truncated)` **before** appending canonical `Источники:` tail intact; `body+tail > platformMax` → truncate tail only at complete `[n](url)` boundaries never mid-URL/mid-bracket so retained links stay resolvable; if no complete entry fits, omit tail except header; Discord delivery via `chunkForDiscord` `src/chat/discord/format-chunking.ts:18` but truncation still respects link validity; platform caps Telegram `4096` `src/chat/telegram/metadata.ts:22`, Kontur Talk `4096`, Discord `2000` `src/chat/discord/metadata.ts:20`, Mattermost `16383`; no platform branch beyond delivery cap/chunking; empty/no-context fallback literal is the no-answer text) — `bun test tests/knowledge-base/truncation.test.ts`
- [ ] 7.10 Implement truncation (4k paragraph-boundary + platform-adjusted budget) and `Источники:` tail preservation with link-valid tail truncation — `bun test tests/knowledge-base/truncation.test.ts`
- [ ] 7.11 Write failing tests for `ask_knowledge_base` `execute` end-to-end: `embed(question)` via per-config LLM provider (BYOK override via `contextConfig`/`adminConfig` at call time, Vercel AI SDK) → tiered `retrieve` → `buildAnswerMessages` → `generateText` via same provider → `linkCitations` → `stripTrailingSourcesList` → body truncate → append tail → return `{ answer, answerId, sources, maxScore }` with stable `answerId = sha256(configContextId + '|' + Date.now() + '|' + answerHash)` 64-char lowercase hex where `answerHash` = lowercase hex `sha256` of final delivered answer string after truncation/tail (or fallback literal; `sha256("")` if empty) and `Date.now()` is stringified decimal; regeneration mints new id; `answerId` persisted as `knowledge_base_feedback.answer_id` FK — `bun test tests/knowledge-base/ask-tool.test.ts`
- [ ] 7.12 Implement `plugins/knowledge-base/src/tools/ask-knowledge-base.ts` tool body and register as `plugin_knowledge_base__ask_knowledge_base` (`contributes.tools`) — `bun test tests/knowledge-base/ask-tool.test.ts && bun run typecheck`
- [ ] 7.13 Write failing tests for grounded fallback (no excerpt supports answer → honest fallback "no reliable answer" with no invented workaround/escalation; empty corpus → empty list + `maxScore=0` without error; `maxScore` is best among served chunks) — `bun test tests/knowledge-base/fallback.test.ts`
- [ ] 7.14 Implement fallback literal and empty-corpus handling — `bun test tests/knowledge-base/fallback.test.ts`

## 8. Capability gating, tool_prefs, and guest mode

- [ ] 8.1 Write failing tests for `ask_knowledge_base` gating via `src/tools/tool-preferences.ts:resolveToolPermission` + `wrapTool` pipeline (resolution `toolOverrides` → `domainDefaults[plugin]` → `riskDefaults[read]` → implicit `allow`; `allow` exposes normally, `deny` removes tool+prompt hint, `ask` wraps schema with `_permission_reason` and gates execution until explicit per-call user confirmation with no chunks returned) — `bun test tests/knowledge-base/tool-prefs.test.ts`
- [ ] 8.2 Wire gating (no plugin-side branching beyond metadata; `ask` confirmation path is handled by core) — `bun test tests/knowledge-base/tool-prefs.test.ts`
- [ ] 8.3 Write failing tests for guest mode (`guest_mode` on `authorized_groups`, `actorRole: 'guest'` never provisioned into `users`/`group_members`, `applyGuestReadOnlyFilter` includes `ask_knowledge_base` as `read` with same `allow`/`ask`/`deny` gating and without memory capture) and thread-isolation (sibling threads share `configContextId` corpus but live turns `storageContextId` histories do not leak) — `bun test tests/knowledge-base/guest-and-isolation.test.ts`
- [ ] 8.4 Implement guest read-only inclusion and verify `storageContextId -> configContextId` mapping for corpus lookup vs thread-isolated retrieval/prompt/feedback write — `bun test tests/knowledge-base/guest-and-isolation.test.ts`

## 9. Scheduled reindex and distill jobs

- [ ] 9.1 Write failing tests for job registration (`contributes.jobs: ["reindex","distill"]` → owners `plugin:knowledge-base:reindex` / `plugin:knowledge-base:distill` per `docs/architecture/plugins.md:16`, `PluginScheduledJob { name, intervalMs }` `src/plugins/runtime-types.ts:126`, `scheduler.register(owner, { interval: job.intervalMs })` `src/plugins/contributions.ts:92` with `reindex 30*60*1000` and `distill 15*60*1000` global per owner; per-context `distill_interval_ms` override is deferred to intra-sweep debounce `lastDistillAt` check, not re-register; no `DISTILL_CRON` cron field) — `bun test tests/knowledge-base/scheduler-registration.test.ts`
- [ ] 9.2 Implement `plugins/knowledge-base/src/jobs/reindex.ts` and `plugins/knowledge-base/src/jobs/distill.ts` registrations via `contributionRegistry.register` — `bun test tests/knowledge-base/scheduler-registration.test.ts`
- [ ] 9.3 Write failing tests for per-context sweep (`getScheduledJobContextIds("knowledge-base", manifest)` two-arg `src/plugins/scheduled-contexts.ts:10` filtered to `eligible`+`enabled`; `defaultEnabled` + `taskInstanceId !== null` is `configured` and scheduled by default; `null` contexts require explicit `plugin_context_state` enable; sequential `runPluginScheduledJob` reduce chain `src/plugins/contributions.ts:172` with per-context try/catch isolation so one group's embed error does not abort others; embed batching `EMBED_BATCH=32` + `p-limit(3)` within a context) — `bun test tests/knowledge-base/scheduler-sweep.test.ts`
- [ ] 9.4 Implement sweep (enumerate eligible contexts, resolve LLM creds via BYOK facade at execution time, run pipeline, emit `pino` counters `{threads, judged/indexed/failed, chunks_indexed per source_type, maxScore}` and `plugin_runtime_events` `src/db/plugin-schema.ts:58` scoped `pluginId: 'knowledge-base'`) — `bun test tests/knowledge-base/scheduler-sweep.test.ts`
- [ ] 9.5 Write failing tests for skip-if-running per owner (plugin-owned `activeSweep` flag per `plugin:knowledge-base:reindex`/`distill`; core `src/utils/scheduler.internal.ts:284` `setInterval` has no cross-tick guard so handler checks flag at entry — if prior sweep not completed when next `intervalMs` fires, new tick is skipped with `pino` info, not queued; at most one sweep per job on single `bun:sqlite` WAL file) and `activate()` lightweight check (only `cursors` existence, no heavy embed/index, respects `activationTimeoutMs=5000` `src/plugins/types.ts:198`) — `bun test tests/knowledge-base/scheduler-guard.test.ts`
- [ ] 9.6 Implement `activeSweep` guard and `activate()` — `bun test tests/knowledge-base/scheduler-guard.test.ts`
- [ ] 9.7 Write failing tests for idempotency (`content_hash`/`judged_hash`/`cursors` watermark; repeating without new content produces no new chunks and leaves `cursors` unchanged) and for `DISTILL_BATCH_LIMIT` slicing with 60s per-context wall-clock budget + per-LLM-call timeout (judge version bump drains large pending queue slice per 15-min tick; one slow group does not starve others or block `reindex` owner beyond `busy_timeout=5000`) — `bun test tests/knowledge-base/idempotency.test.ts`
- [ ] 9.8 Implement idempotent hash/version checks and batch/timeout slicing — `bun test tests/knowledge-base/idempotency.test.ts`

## 10. Settings UI via configRequirements (generic plugin config)

- [ ] 10.1 Write failing tests for settings persistence via generic routes `GET/POST /settings/api/plugins/config` + `/settings/api/admin/plugin-config` (`src/plugins/store.ts`, `src/debug/settings/plugins-routes.ts` — not custom `/settings/api/knowledge-base/*`; `synthetic-web-search` is core, not a plugin precedent) — `zod` validation for `curated_kb_path`/`url`, GitLab host/route, `kb_min_value_score`, `distill_batch_limit`, `retrieval_min_score`, encrypted-at-rest `gitlab_token`/BYOK masked on read with `unset`/`Clear` support, `required: false` so unset does not gate `eligible`, per-config-context group-shared storage and `config_missing` re-evaluation, admin defaults seeding without overriding customized prefs, changes take effect on next run without restart (creds resolved at job/tool time, not at `activate()`) — `bun test tests/knowledge-base/settings-routes.test.ts`
- [ ] 10.2 Implement settings via `configRequirements` and wire `contextConfig.get('curated_kb_path')`/`get('curated_kb_url')` snake_case reads in reindex path — `bun test tests/knowledge-base/settings-routes.test.ts && bun run typecheck`
- [ ] 10.3 Wire generic Plugins section rendering (`PluginsSection.svelte`/`AdminPluginsConfigSection.svelte` via `adminConfig`/`contextConfig` facade `docs/architecture/plugins.md:19`) — `bun test tests/knowledge-base/settings-ui.test.ts && bun run typecheck`
- [ ] 10.4 Verify `ChatRouter` fan-out resolves same `configContextId` corpus across Telegram/Mattermost/Discord/Kontur Talk without platform branch in retrieval/prompt — `bun test tests/knowledge-base/cross-platform.test.ts`

## 11. Feedback table, observability, privacy, and rollback

- [ ] 11.1 Write failing tests for `knowledge_base_feedback` per-group partitioned feedback (`(config_context_id, answer_id, voter)` PK, `voter` = per-user `chatUserId`, thread-isolated one row per voter; groups do not collide/leak; stored feedback does not alter retrieval ranking/threshold in this change; reserved for future phases; guests can leave feedback once future trigger exists but do not trigger memory capture; no retention/TTL in phase 1 — orphaned `answer_id`s accumulate) and `answerHash`/`answerId` determinism (`answerHash` = lowercase hex `sha256` of final delivered answer after truncation/tail or fallback literal, empty → `sha256("")`; `timestamp` = `Date.now()` decimal) — `bun test tests/knowledge-base/feedback.test.ts`
- [ ] 11.2 Implement `knowledge_base_feedback` writes as storage-ready (no chat interaction/reaction/tool or `/settings` route writes it yet — write path deferred to future `perm:`-like callback/tool that upserts `(config_context_id, answer_id, voter)` without migration; `answerId` minted/returned alongside `answer/sources/maxScore` for joins; regeneration mints new id, prior rows remain) — `bun test tests/knowledge-base/feedback.test.ts`
- [ ] 11.3 Write failing tests for observability without secret leakage (per-run `pino` structured counters and `plugin_runtime_events` per `configContextId` — `threads` crawled, `judged/indexed/failed` distilled, `chunks_indexed` per `source_type`, `maxScore`; dimension mismatches/contract violations warn with only `id`/`sourceRef`/`dims`/`queryDims`/lengths never chunk text/embeddings/tokens/secrets; logs never contain `Bearer`/`glpat`/`AKIA`/JWT values; `KNOWLEDGE_BASE_DATA_DIR` `mkdir` failure surfaces via `plugin_runtime_events` warning not just `pino.warn`; brute-force scan warns if per-`config_context_id` chunks >10k with `maxScore`/`count` guard) — `bun test tests/knowledge-base/observability.test.ts`
- [ ] 11.4 Implement `pino` + `plugin_runtime_events` counters with secret-safe logging and 10k chunk guard — `bun test tests/knowledge-base/observability.test.ts`
- [ ] 11.5 Verify rollback semantics: disabling per-group via settings or rejecting `knowledge-base` in admin `plugin_admin_state` stops scheduling and hides `ask_knowledge_base`; tables stay dormant; hard rollback `DROP TABLE IF EXISTS knowledge_base_*` + `DELETE FROM migrations WHERE id='<seq>_knowledge_base'` without touching `idx_message_metadata_group_ctx_ts_msg` (or recreate it if batched drop occurred) — document in `docs/architecture/plugins.md`/`docs/architecture/overview.md` if warranted — `bun test tests/knowledge-base/rollback.test.ts`

## 12. Final verification

- [ ] 12.1 Run full gate: `bun run test`, `bun run typecheck`, `bun run lint`, `bun check:full` (includes `format:check`, `knip`, `duplicates`); fix `max-lines`/`max-lines-per-function` by splitting files, never by gaming formatting or adding lint-disables — `bun check:full`
