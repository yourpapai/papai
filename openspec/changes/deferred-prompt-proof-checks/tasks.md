# Tasks — Deferred-prompt proof checks

Test-first order per design.md D12 (red before each implementation). Verification commands are listed per task; the Write/Edit TDD hook tracks `tests/` files written this session, so always write the failing test before its implementation file.

## 1. Proof store (capped JSONL)

- [x] 1.1 Write failing `tests/deferred-prompts/proof-store.test.ts` (imports `src/deferred-prompts/proof-store.js`): append/load round-trip, record shape `{run_id, check, variant, started_at, finished_at, verdict, observations[]}`, cap to last ~50 runs dropping oldest, atomic trim (temp+rename, no partial file), DI of `{path, now}` with a temp dir. Verify red: `bun test tests/deferred-prompts/proof-store.test.ts`
- [x] 1.2 Implement `src/deferred-prompts/proof-store.ts` per design D5: append-only JSONL at `dirname(DB_PATH ?? 'papai.db')/proof-checks.jsonl`, single-writer promise chain, lazy file creation, injected path/clock; no logging of recorded prompt text. Verify green: `bun test tests/deferred-prompts/proof-store.test.ts` && `bun run lint`

## 2. Async runner + poller delivery record

- [x] 2.1 Write failing runner tests in `tests/deferred-prompts/proof-checks.test.ts` (imports `src/deferred-prompts/proof-checks.js`) using fully injected `ProofCheckDeps` (fake clock/timers/event bus/handlers/store — poll for conditions, no wall-clock assertions): `PROOF_CHECKS` registry covers exactly the five ids; sync bug4/bug5 run inline with their verdict predicates (D7) and always cancel the proof prompt; async checks return `{status:'started', run_id}` and observe `deferred:fired` + `deferred:alerted` filtered by prompt id with row-read fallback at window close; window defaults/cap and fire_at derivation (≈ now+90s, bug3 ≈ +10 min, alert variant 2× ALERT_POLL_MS); teardown guarantees — cancel + unsubscribe + final record on completion/timeout/error, failures recorded as `inconclusive`/`pending`, never uncaught; second concurrent async run returns structured `busy`; start-of-run sweep + explicit `cleanup` cancel marker-matching prompts owned by the admin's storage context only. Verify red: `bun test tests/deferred-prompts/proof-checks.test.ts`
- [x] 2.2 Implement `src/deferred-prompts/proof-checks.ts` (D6/D7/D8): registry, `runProofCheck`, module-level single-flight lock, marker helpers (`[[proof-check:<runId>]]`, marker sentence in delivery_brief), event subscription with guaranteed `unsubscribe`, bug-2 anchor read from `getCachedHistory` + trace correlation, bug-1 trace/delivery comparison against the locale-resolved `completion.doneFallback`. Verify green: `bun test tests/deferred-prompts/proof-checks.test.ts` && `bun run lint`
- [x] 2.3 Write failing delivery-record tests (same file): the poller seam appends `{runId, responseText, delivered: true, at}` only after `sendProactiveMessage` resolves `true` and only when every prompt in the group carries the marker; mixed groups skip; error-path delivery is not recorded; nothing about the outbound message changes. Also include the existing `tests/deferred-prompts/poller.test.ts` expectations. Verify red: `bun test tests/deferred-prompts/proof-checks.test.ts tests/deferred-prompts/poller.test.ts`
- [x] 2.4 Add the marker-gated pure-record line to `src/deferred-prompts/poller.ts` after the successful `sendProactiveMessage` (design D9), with the disposable-instrumentation comment. Verify green: `bun test tests/deferred-prompts/proof-checks.test.ts tests/deferred-prompts/poller.test.ts` && `bun run lint`

## 3. Tool surface (gate, schemas, registrations)

- [x] 3.1 Write failing `tests/tools/proof-checks.test.ts` (imports both new tool modules and `src/tools/diagnostics.js`): gate matrix — tools absent for non-admin, group context, proactive mode, and guest paths, present only for `isBotAdmin === true && contextType === 'dm' && mode === 'normal'`; `run_proof_check` input schema (check enum described, `variant?`, `wait_seconds?` cap 15 min, `cleanup?` mutually exclusive with `check`); sync check returns the finished record inline, async returns `{status:'started', run_id}`; busy on concurrent run; `read_proof_results` lists most-recent runs with verdicts and filters by `run_id`; bound-id-missing degrades to a structured error. Verify red: `bun test tests/tools/proof-checks.test.ts`
- [x] 3.2 Implement `src/tools/proof-check-run.ts` and `src/tools/proof-checks-read.ts` (one tool per file, `make[Action]Tool` factories, AI SDK `tool()`, Zod schemas with `.describe()` on every field, binding `options.storageContextId`/`options.chatUserId` at assembly time) and assemble both inside `maybeAddDiagnosticsTools` in `src/tools/diagnostics.ts`. Verify green: `bun test tests/tools/proof-checks.test.ts tests/tools/diagnostics.test.ts` && `bun run lint`
- [x] 3.3 Register in `src/tools/tool-metadata.ts`: `run_proof_check` → `write('diagnostics', 'create')`, `read_proof_results` → `read('diagnostics')`; confirm no new domain and prefs/preset resolution still classifies both (deny removes, ask wraps). Verify: `bun test tests/tools/tool-metadata.test.ts tests/tools/tool-preferences.test.ts tests/tools/tool-preferences.testing.test.ts` && `bun run lint`

## 4. Docs and full verification

- [x] 4.1 Add one bullet each, marked disposable (to be removed with the module), to `src/tools/CLAUDE.md` (diagnostics readers section) and `docs/architecture/tools.md` describing the two tools, the shared gate, the write/read risk tiers, and the poller record line. Verify: `bun run format:check` && `bun run lint`
- [x] 4.2 Final gate: run full `bun test`, `bun run typecheck`, and `bun run lint`; then `bun run test:mutate:changed` to confirm the mutation ratchet over the branch diff (new `src/` files covered by the three new test suites; `poller.ts` holds its floor) and `bun check:full` as the pre-finish check. All green before handoff.
