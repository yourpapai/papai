# Tasks — sdd-runner-decomposition (part 3 of 3)

Implements the proposal's tasks 6.1–6.2, 7.1, 8.1–8.3, 9.1 only: operator surfaces, the decompose-stage `needs_split` re-entry, and docs. Every task is red-first — the failing tests land before the implementation, green before moving on (design.md, Hook / TDD interaction). Group → proposal mapping: §1 ≈ 6.1, §2 ≈ 6.2, §3 ≈ 7.1, §4 ≈ 8.1, §5 ≈ 8.2, §6 ≈ 8.3, §7 ≈ 9.1. Do not revisit landed session-1/2 scope.

## 1. Tree-aware resume routing (design D1–D2; proposal 6.1)

- [x] 1.1 `descendantGateOf` discovery. Red-first in `tests/sdd-runner/run-index.test.ts`: from a parent's persisted `children` records plus its event-log spawn lines (`lastSpawnedHandleOf`), resolve each `running` child's concrete runId, load the child's `state.json`, recurse into grandchildren, and return the deepest gate-pending descendant runId; an unloadable child state counts as no pending gate (parent falls back to plain resume); a visited-set guards malformed cyclic state; a non-parent / no-descendant state returns null. Then implement in `run-index.ts` (design D1). Verify: `bun run test tests/sdd-runner/run-index.test.ts`
- [x] 1.2 `continue`/resume descent into a pending child. Red-first in `tests/sdd-runner/continue.test.ts` + `tests/sdd-runner/resume-flow.test.ts`: `continue` on a plan parent whose next action lives inside a descendant routes to that child's gate decision flow (`runGateResume`) with the child's concrete `sdd <childRunId>` line; with no in-flight descendant the parent resumes at the next pending child via the existing `runChildren` skip-forward (unchanged); `runResume`'s plan-parent `gate-pending` result threads `childRunId` (the one-field `PlanResumeResult` addition); non-parent routing decisions and `resolveRunId` exact/prefix/ambiguous semantics pinned byte-identical. Then wire `continue.ts` + the resolver in `resume-flow.ts` (design D2). Verify: `bun run test tests/sdd-runner/continue.test.ts tests/sdd-runner/resume-flow.test.ts`

## 2. Discovery surfaces (design D3; proposal 6.2)

- [x] 2.1 Mode-bearing listings and parent→child sole-candidate routing. Red-first in `tests/sdd-runner/cli-routing.test.ts` + `tests/sdd-runner/run-index.test.ts` + `tests/sdd-runner/continue.test.ts`: the multi-pending gate listing prints each run's mode beside its version (`gate <mode> v<n>`, matching `routeBySoleCandidate`'s hint); `routeByState` and the sole-candidate paths consult `descendantGateOf` for a gate-null parent and route to the active child's gate (`{ kind: 'gate', runId: <childRunId> }`) instead of a blind parent resume; tree-member prefix ambiguity keeps failing loudly listing every candidate; non-parent routing decisions pinned unchanged. Then implement in `cli-routing.ts` + `continue.ts` (design D3). Verify: `bun run test tests/sdd-runner/run-index.test.ts tests/sdd-runner/cli-routing.test.ts tests/sdd-runner/continue.test.ts`

## 3. Decompose split re-entry (design D4–D6; proposal 7.1)

- [x] 3.1 `needs_split` report field and decomposer prompt. Red-first in `tests/sdd-runner/decompose.test.ts`: `DecomposeReportSchema` gains optional `needs_split` (undefined reads false; sidecars without the field parse unchanged); the decomposer prompt teaches the field and the first-slice-only `tasks.md` contract (a `needs_split: true` report scopes tasks.md to child #1's slice only); `runDecompose` returns the parsed report instead of `void`. Then implement in `decompose.ts` (design D4). Verify: `bun run test tests/sdd-runner/decompose.test.ts`
- [x] 3.2 Optional `changeName` on plan children + `RunChildRun` pass-through. Red-first in `tests/sdd-runner/plan.test.ts` + `tests/sdd-runner/plan-resume.test.ts` + `tests/sdd-runner/gate-resume-entry.test.ts`: `PlanChildSchema` gains optional `changeName` (old plans parse; `validatePlan` adds no structural rule for it); the `RunChildRun` wrappers in `plan-resume.ts`/`gate-resume-entry.ts` forward the full `PlanChild` to the orchestrator-supplied starter instead of discarding it. Then extend `plan.ts` and fix both wrappers (design D6). Verify: `bun run test tests/sdd-runner/plan.test.ts tests/sdd-runner/plan-resume.test.ts tests/sdd-runner/gate-resume-entry.test.ts`
- [x] 3.3 Tail diversion through `runPlanner` + `runPlanBranch`. Red-first in `tests/sdd-runner/post-review-tail.test.ts`: a `needs_split: true` decompose report diverts the shared tail between decompose and atomicity — no atomicity spawn, no final gate; the diversion re-enters through the landed `runPlanner` with a composed task text (original task + existing change name + drafted-artifact summary + re-scoped tasks.md; child #1 pinned to the existing change, siblings partitioning the remainder), then converts via the landed `runPlanBranch` (state.plan/children seeding, `plan` event, plan-gate presentation with R4-only prelude); a false/absent `needs_split` runs today's tail byte-identically. Then implement in `post-review-tail.ts` (design D5). Verify: `bun run test tests/sdd-runner/post-review-tail.test.ts tests/sdd-runner/decompose.test.ts`
- [ ] 3.4 Continuation start for the `changeName`-carrying child. Red-first in `tests/sdd-runner/orchestrator.test.ts`: the orchestrator-side starter gives a `changeName`-carrying child a continuation start — skips intake/draft/review (no `newChange` collision, no re-draft of reviewed artifacts), inherits the parent's depth, enters the shared post-convergence tail at atomicity, and presents its final gate over an adopted-review surrogate digest with its own task progress and costs; the session-id allocator yields the next free `<slug>-<n>` while `changeName` keeps pointing at the existing folder; the ledger books draft/review spend in the parent's log only (no double count). Then implement the continuation start in `orchestrator.ts` (design D6). Verify: `bun run test tests/sdd-runner/orchestrator.test.ts`

## 4. Live tree lines (design D7–D8; proposal 8.1)

- [ ] 4.1 Renderer tree-event lines. Red-first in `tests/sdd-runner/renderer.test.ts`: `formatEvent` renders the `plan` (child count + digest), `child_spawned` (child + run id), and `child_done` (child + outcome) L2 lines — the only permitted non-TTY additions on composite runs; single-run event streams render byte-identically (pinned). Then implement in `renderer.ts` (design D7). Verify: `bun run test tests/sdd-runner/renderer.test.ts`
- [ ] 4.2 Run-view children block. Red-first in `tests/sdd-runner/run-view.test.ts`: the running screen renders a `## Children` section from `ReplayState.children` — one `<child-id> <status>` line per entry, the active node marked — omitted entirely when the fold is empty (single-run screens unchanged). Then implement in `run-view.ts` (design D8). Verify: `bun run test tests/sdd-runner/run-view.test.ts`

## 5. Report children section (design D9; proposal 8.2)

- [ ] 5.1 Children rows, subtree totals, no parent task counts. Red-first in `tests/sdd-runner/report.test.ts`: a parent's report (`state.plan` present) gains a children section — per-child id, latest spawn's runId, status (live child `state.json`, falling back to the parent's `children` record when unloadable), and cost via `childUsageOf`; a subtree total row from `treeSpend` over the parent's repriced events; unknown/unpriced cost renders the `unknown` marker, never `$0.00`; the parent reports no `### Tasks` section (it owns no change folder); single-run reports pinned byte-identical. Then implement in `report.ts` (design D9). Verify: `bun run test tests/sdd-runner/report.test.ts`

## 6. Plan-gate TUI rows (design D10; proposal 8.3)

- [ ] 6.1 Shared expected content fixes the self-check gap. Red-first in `tests/sdd-runner/tui-gate-session.test.ts`: a plan-gate settle passes the write-then-parse self-check — delete `tui-gate-session.ts`'s local `expectedContent` duplicate in favor of the children-aware builder it mirrors in `gate-session.ts` (duplicates gate). Then fix `tui-gate-session.ts` (design D10). Verify: `bun run test tests/sdd-runner/tui-gate-session.test.ts tests/sdd-runner/gate-session.test.ts`
- [ ] 6.2 Child-row toggle semantics. Red-first in `tests/sdd-runner/tui-gate.test.ts` + `tests/sdd-runner/tui-gate-session.test.ts`: child `C<n>` rows toggle exactly like assumption checkboxes (toggle = checked; unchecked = veto with optional inline redirect collected beneath the row); settle with every child checked → approve; settle with an unchecked child → veto carrying the redirect; `e` (extend) is refused at plan mode; minimal — no other plan-specific TUI affordances; early/final gate screens pinned unchanged. Then adjust view items in `tui-gate.ts` via `gate-session.ts`/`gate-session-state.ts` as needed (design D10). Verify: `bun run test tests/sdd-runner/tui-gate.test.ts tests/sdd-runner/tui-gate-session.test.ts`

## 7. Docs and full gates (proposal 9.1)

- [ ] 7.1 Docs, full verification, commit. Update `docs/architecture/sdd-pipeline.md`: document the tree-aware routing and discovery surfaces (descent into a gate-pending child, mode-bearing listings, parent→child sole-candidate routing), the decompose `needs_split` re-entry (diversion point, planner re-entry, child-#1 continuation start), the live/report tree visibility (per-child lines, children section, subtree totals, no parent task counts), and the plan gate's TUI decidability — extending the existing Composite runs section rather than restating it. Then run one full `bun run test` (≥20 min shell timeout; never two concurrent full runs; query `reports/` via `bun run test:failures`/`test:show` instead of re-running), `bun run typecheck`, `bun run lint` — all green; backward-compat pins confirmed (decompose sidecars without `needs_split` parse; single-run renderer/report/gate bytes unchanged; `resolveRunId` semantics untouched). Commit as `sdd-runner-decomposition(3/3): resume/CLI routing, decompose split, render/report/TUI, docs` with a body noting readiness for the Stryker mutation ratchet (`test:mutate:changed`) over the combined branch diff. Verify: `bun run test && bun run typecheck && bun run lint`
