# task-provider-subscriptions — Session 4: docs, spec delta, full verification

## Goal
Close out the task-provider-subscriptions effort. Sessions 1–3 already landed as code+tests+their own changes: `pin-alerts-to-task-instances` (alert_prompts.task_instance_id FK pin, cancel-on-switch/delete, poller routes by pinned instance), `alert-task-watch` (`task.id` eq pure-watch condition, targeted getTask polling, snapshot-visible-change firing), `alert-activity-condition` ({kind:'activity', taskId, categories?} via getTaskHistory with last_activity_cursor, baseline-then-fire, activities.read creation gating, untrusted-content-wrapped summaries). All referenced code exists (src/deferred-prompts/{poller-alerts, poller-alerts-watch, poller-alerts-activity, poller-alerts-grouping, poller-alerts-summary, activity-gating, condition-eval, tool-handlers}.ts) with covering tests. This session: documentation completion, the consolidated OpenSpec delta, and full-repo verification. **No behavior changes; no webhook code; no refactors beyond what verification demands.**

## Files to touch
1. `docs/architecture/tools.md` — extend the `create_alert` block (line ~14) into a condition reference with concrete JSON examples for all three leaf shapes — field condition ({"field":"task.status","op":"changed_to","value":"Done"}), per-task watch ({"field":"task.id","op":"eq","value":"42"}), activity watch ({"kind":"activity","taskId":"TASK-1","categories":["comment"]}) — and document tool_prefs three-state behavior on create_alert specifically (deny removes it from the resolved set, ask wraps it in the _permission_reason per-call confirmation, allow is default; read src/tools/tool-metadata.ts / deferred tool registration for create_alert's actual risk/domain class and state it). Keep precise, no fluff.
2. `docs/architecture/behaviors.md` — verify the three alert bullets (lines ~48–50: edge-triggering + pure-watch targeted polling; activity watches with cursor/baseline/cooldown-catch-up/capability-recheck; task-instance pinning with capture/evaluation/NULL-pin/auto-cancel/cancel-on-switch-and-delete) against the code; the prior sessions already wrote most of this — fill only genuinely missing pieces the issue names (e.g. any gap in cancel-on-switch/delete semantics, creation-refusal guidance, targeted-vs-whole-list polling) and fix any inaccuracy.
3. `docs/architecture/overview.md` — check module-map/request-flow lines describing alert polling. Current file has no alert-polling-specific line (only the generic `src/index.ts` 'router/scheduler/poller/debug-server start' at line 28 and the generic 'deferred' tools mention at line 19, both still accurate), so the expected outcome is a verified no-op; edit only if an actually inaccurate line is found.
4. `openspec/changes/task-provider-subscriptions/` — scaffold with `openspec new change "task-provider-subscriptions"` (never create the folder by hand; .openspec.yaml must come from the CLI). Write `specs/task-subscriptions/spec.md` using the neighboring delta format (`## ADDED Requirements`, `### Requirement:`, `#### Scenario:` with WHEN/THEN bullets) covering: per-task watch creation+firing (eq-only validation, baseline cycle without firing, fire on snapshot-visible change, missing-task skip); activity-based firing gated on activities.read (condition kind, creation refusal guidance when incapable/unconfigured/mixed trees, cursor baseline-then-edge semantics advancing only after successful delivery, poll-time capability-loss skip, untrusted-content summaries); instance-pin cancel on switch/delete (capture at creation, evaluation against pinned instance, auto-cancel on unresolvable pin, cancel-on-switch scoped to the config context, cancel-before-delete across contexts); cooldown/dedup no-refire for all alert kinds; tool_prefs ask/deny behavior on create_alert. Source of truth: the three landed deltas under openspec/changes/{pin-alerts-to-task-instances,alert-task-watch,alert-activity-condition}/specs/ — consolidate, do not invent new behavior. Also write proposal.md (<500 words, Non-goals section, capability `task-subscriptions` justified per config rules) and tasks.md (each task ends with its verification command; final task is the full verification below); add design.md only if `openspec status`/`instructions` for the spec-driven schema requires it.

## Intended behavior change
None. Documentation and OpenSpec artifacts only. Code is edited solely if verification surfaces a defect in prior sessions' code, and then minimally, followed by re-verification.

## Verification
1. `openspec validate task-provider-subscriptions --strict` — fix until clean.
2. In this order, reading persisted report artifacts instead of re-running to filter output: `bun run test` (full suite; failures via `bun run test:failures` and `bun run test:show <id>` against reports/test/), `bun run typecheck`, `bun run lint`, `bun run format:check`, `bun run check:full` (a failure names the log file under reports/checks/ to open). Shared-host rules per AGENTS.md: never two full suites concurrently; if a shell timeout kills a run, consult `bun run test:status` / `test:log` before restarting. Deliverable: green full suite and checks, accurate docs, validated OpenSpec change.
