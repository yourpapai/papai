# Tasks — sdd-runner-decomposition (part 2 of 3)

Implements the runtime path the proposal names (its tasks 2.3, 4.1–4.3, 5.1–5.4): intake plan branch (§2), plan-gate grammar and prelude (§3–4), child execution + orchestrator/gate-resume wiring (§5–6). Every task is red-first — the failing tests land before the implementation, green before moving on (design.md, Hook / TDD interaction). Group → proposal mapping: §1–2 ≈ 2.3, §3–4 ≈ 4.1–4.3, §5 ≈ 5.1–5.2 + 5.4, §6 ≈ 5.3–5.4. This list is authoritative over the part-1 folder's numbering (design A8).

## 1. Additive schema fields (design D1, D8)

- [x] 1.1 Optional `oversize` verdict on the depth surface. Red-first in `tests/sdd-runner/agent-layer.test.ts` + `tests/sdd-runner/events.test.ts`: `DepthClassificationSchema` validates sidecars with and without `oversize: boolean` (undefined reads false); a `depth` event with optional `oversize` stamps via `stampEvent` and round-trips through `appendEvent`/`readEvents`; pre-change lines and sidecars parse unchanged; estimator prompt teaches the field. Then add the optional field to `agent-layer.ts` and the `depth` variant in `event-schemas.ts`. Verify: `bun run test tests/sdd-runner/agent-layer.test.ts tests/sdd-runner/events.test.ts`
- [x] 1.2 Optional `runId`/`usage` on the child events. Red-first in `tests/sdd-runner/events.test.ts`: `child_spawned` with optional `runId: string` and `child_done` with optional `usage: AgentUsageSchema` stamp and round-trip with and without the new fields; old lines parse unchanged. Then extend both variants in `event-schemas.ts`. Verify: `bun run test tests/sdd-runner/events.test.ts`

## 2. Intake plan branch (design D1–D3)

- [x] 2.1 `runIntake` returns a union with the oversize oracle and scaffold-after-verdict ordering. Red-first in `tests/sdd-runner/intake.test.ts`: a non-oversize estimator returns `{ kind: 'single', … }` with today's fields byte-identical and scaffolds the change folder (now after the estimator spawn); an oversize estimator returns `{ kind: 'plan', children }` with **no** `driver.newChange` call; `--depth` override returns single before the oracle is consulted (today's early return); the `depth` event carries the verdict. Then rework `intake.ts` (move `newChange` into the single branch; emit verdict before any scaffold). Verify: `bun run test tests/sdd-runner/intake.test.ts`
- [x] 2.2 `runPlanner` with the structural replan loop. Red-first in `tests/sdd-runner/intake.test.ts`: planner spawn goes through `runStageAgent` with role `planner`, `outputPath: 'plan.json'`, `outputSchema: PlanSchema`; a structurally invalid draft (duplicate ids / unknown deps / cycle from `validatePlan`/`topoSortChildren`) triggers exactly one replan pass with the validation-error text appended to the prompt (bounded by `PLAN_REPLAN_PASSES`), then a loud failure naming the structural errors; a valid plan returns topo-ordered children; the current plan persists at `sidecars/plan.json`. Then implement `runPlanner` in `intake.ts`. Verify: `bun run test tests/sdd-runner/intake.test.ts`

## 3. Plan-gate grammar (design D4; proposal 4.1–4.2)

- [x] 3.1 `gate-model.ts` accepts child rows. Red-first in `tests/sdd-runner/gate-model.test.ts`: `ExpectedGateContent` gains optional `children`; `C<n>` checkbox rows route by declared-id membership (unknown C-id rejected, prefix alone never routes); every C-box checked approves; an unchecked C-box vetoes that child with optional `→ <redirect>` beneath; `ABORT` aborts; `→ RUN 1 MORE` at plan mode is rejected with the cap-hit-only message; single-run gates parse byte-identically. Then widen the row regexes and finalize logic. Verify: `bun run test tests/sdd-runner/gate-model.test.ts`
- [x] 3.2 `gate-answers.ts` renders the identical grammar. Red-first in `tests/sdd-runner/gate-answers.test.ts`: item kind `'child'` renders `- [x| ] C<n> <text>` rows with redirects; `responseFromAnswers` maps accepted/rejected children to approve/vetoes; render-then-parse self-check — rendered plan answers parse back through `parseGateResponse` to the same response. Then extend `gate-answers.ts`. Verify: `bun run test tests/sdd-runner/gate-answers.test.ts`
- [x] 3.3 `gate-render.ts` widens to plan mode. Red-first in `tests/sdd-runner/gate-render.test.ts`: `decisionConsequences('plan')` returns approve = executes children sequentially in plan order, veto = revises the plan once then re-gates, extend = `null`, abort = aborts the parent before any child runs; `writeGateDigest` mode `'plan'` renders one checkbox row per child with scope/acceptance-signal text and the shared `### Decisions` block; early/final gate bytes pinned unchanged. Then widen `gate-render.ts`. Verify: `bun run test tests/sdd-runner/gate-render.test.ts`

## 4. Plan-gate prelude and settle guards (design D5; proposal 4.3)

- [x] 4.1 `evaluatePlanGate`: R4 only. Red-first in `tests/sdd-runner/gate-prelude.test.ts` + `tests/sdd-runner/gate-settle.test.ts`: a new `evaluatePlanGate(signals)` in `auto-policy.ts` returns `r4FailsClosed(signals) ?? gateDecision('none', …)` with projection `spent + childCount × DEFAULT_ROUND_COST_USD`; no rule can approve/extend/accept-items at plan mode; `autoSettleFinalGate`/`autoExtendRound` refuse plan mode loudly. Then implement. Verify: `bun run test tests/sdd-runner/gate-prelude.test.ts tests/sdd-runner/gate-settle.test.ts`
- [x] 4.2 `presentGateAt` accepts mode `'plan'`. Red-first in `tests/sdd-runner/gate-digest.test.ts`: plan mode presents the gate file (children rows, versioned `gate-<n>.md`, hashes sidecar, `gate presented` event with `mode: 'plan'`) and never settles or extends; a fired R4 still writes the preview block, `auto-policy.jsonl` line, and `auto_decision` event with decided-by attribution; early/final presentations byte-identical. Then widen `gate-digest.ts`. Verify: `bun run test tests/sdd-runner/gate-digest.test.ts`

## 5. Child execution module (design D7–D11; proposal 5.1–5.2, 5.4)

- [x] 5.1 New `sdd-runner/src/children.ts` — `runPlanBranch`. Red-first in new `tests/sdd-runner/children.test.ts`: materializes child files through the landed fs seam, emits the `plan` event (childCount + digest), sets `state.plan` (topo childIds + digest) and seeds `state.children`, persists state, and presents the plan gate; all fs/spawn through injected seams. Then implement `children.ts` (design D7). Verify: `bun run test tests/sdd-runner/children.test.ts`
- [x] 5.2 `runChildren` sequential topo loop with event bookkeeping. Red-first in `tests/sdd-runner/children.test.ts` (fake `runChildRun` + fake child `state.json`): walks `state.plan.childIds` in order, at most one child in flight; skips children already `done`; emits `child_spawned { child, runId }` then `child_done { child, outcome, usage }` with usage aggregated from the child's log; a gate-pending child records `running`, prints the child's concrete `sdd <childRunId>` line, and returns with the parent `running`; resume continues at the next not-done child. Then implement (design D8). Verify: `bun run test tests/sdd-runner/children.test.ts`
- [x] 5.3 Failure and completion semantics. Red-first in `tests/sdd-runner/children.test.ts`: a child ending aborted/failed/stopped stops the loop immediately, emits `child_done` outcome `'failed'`, persists parent `stopped` with an operator line naming the blocking child and its status; parent `completed` exactly when every child id reads `done`; the parent never creates a change folder; an unloadable child state counts as not-done (fail closed). Then implement (design D9). Verify: `bun run test tests/sdd-runner/children.test.ts`
- [ ] 5.4 Aggregate budget ledger fails closed. Red-first in `tests/sdd-runner/children.test.ts` + `tests/sdd-runner/gate-prelude.test.ts`: `treeSpend` = parent done-events + Σ `child_done.usage` with `costKnown: false` when any usage is absent; the loop halts before each `child_spawned` on unknown-or-exceeded spend (parent `stopped`, loud line naming the budget guard); the parent passes `spendBaselineUsd` into nested runs and the child's R4 guard adds the baseline before comparing against the single `budget` (default 0 for top-level runs). Then implement (design D10). Verify: `bun run test tests/sdd-runner/children.test.ts tests/sdd-runner/gate-prelude.test.ts`
- [ ] 5.5 Parent calm-stop is subtree-scoped. Red-first in `tests/sdd-runner/children.test.ts`: a parent stop request while a child is in flight writes the **child's** stop marker; the child honors it at its next boundary; the parent consumes its own marker and settles through `settleStoppedResult` (status `stopped`, both nodes resumable); completed children untouched. Then implement (design D11). Verify: `bun run test tests/sdd-runner/children.test.ts`

## 6. Orchestrator and gate-resume wiring (design D6, D9, D12; proposal 5.3–5.4)

- [ ] 6.1 `runStart` plan branch and parent resume interception. Red-first in `tests/sdd-runner/orchestrator.test.ts`: an oversize intake routes into `runPlanBranch` (no draft/review stages, no parent change folder); `runResume` intercepts parent states (`plan` with pending children) **before** `resumeFromPoint` and drives `runChildren`; `deriveResumeDecision`'s `driver.status` call tolerates the parent's absent change folder; single-run `runStart`/`runResume` output byte-identical. Then wire `orchestrator.ts` (default `runChildRun` = `runStart` with the materialized child task file). Verify: `bun run test tests/sdd-runner/orchestrator.test.ts`
- [ ] 6.2 Plan-gate resume in `runGateResume`. Red-first in `tests/sdd-runner/orchestrator.test.ts`: `mode: 'plan'` builds expected content from `sidecars/plan.json` + `state.plan`; decision flags (`--abort`, `--confirm-all`, `--vetoes`) desugar through the §3 render-then-parse functions; approve → `runChildren`; abort → `finalizeGate('aborted')` before any child exists; `answered` events carry `mode: 'plan'`; the `narrowGateMode` throw is replaced; extend is unreachable (parser rejects first); early/final resume paths byte-identical. Then wire `extend-round.ts` (design D12). Verify: `bun run test tests/sdd-runner/orchestrator.test.ts`
- [ ] 6.3 `settlePlanVeto` — one re-plan per veto round, unbounded rounds. Red-first in `tests/sdd-runner/orchestrator.test.ts`: a veto round re-runs `runPlanner` with the round's redirects, re-validates (replan bound), re-materializes wholesale, emits a fresh `plan` event (replay resets `children`), updates `state.plan`, and re-presents at `gate-<n+1>.md` with `skipPolicy: true`; rounds repeat until approve or ABORT — the only terminals. Then implement in `gate-resume-tail.ts` (design D6). Verify: `bun run test tests/sdd-runner/orchestrator.test.ts`

## 7. Full verification and docs

- [ ] 7.1 Full gates, docs, and artifact reconciliation. Run one full `bun run test` (never two full suites concurrently), `bun run typecheck`, `bun run lint` — all green. Update `docs/architecture/sdd-pipeline.md`: the composite run path (oversize verdict at intake, planner + replan bound, plan gate with R4-only prelude, sequential topo child execution, parent completion/no-change-folder semantics, subtree calm-stop, fail-closed tree budget) replacing the part-1 "data layer only" note where it now undersells reality. Reconcile the part-1 folder's `tasks.md` checkboxes per design A8 (tick by cross-reference or renumber in this commit). Verify: all three commands exit green; `openspec validate sdd-runner-decomposition-2nd --strict` passes.
