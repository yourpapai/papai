<!-- GENERATED by afk-runner from findings/resolutions sidecars - regenerated wholesale per round. -->

## Review

### Round 1

**Verdict**: 1b 4m 7n · 12 resolved · 0 dismissed · open

#### BLOCKER — F1

> Measured on this branch: 20 src files (23 import statements) and 17 test files (24 statements, `fixtures/fake-pipeline.ts` included) import upward.
- **Question**: Task 1.3's exit criterion is "guard unskipped and green" with no allowlist permitted (D2) — what happens to the 7 import statements in 7 test files under tests/afk-runner that resolve to tests/utils/grouped-assertions.js, which is outside the guard's union afk-runner/ ∪ tests/afk-runner/ and is named by no task? As written, the rewire set (D3: review-loop/mutation-improve specifiers only) leaves the guard red at 1.3's verification, so the run cannot proceed past its own gate.
- **Code evidence**: Re-implemented the D2 guard exactly (node walk of every .ts under afk-runner/src and tests/afk-runner, relative specifiers resolved via path.resolve, fence = afk-runner/ ∪ tests/afk-runner/): afk-runner/src yields 20 files / 23 statements (matches the design exactly), but tests/afk-runner yields 22 files / 31 statements, not 17/24. The difference is exactly 7 statements importing ../utils/grouped-assertions.js or ../../utils/grouped-assertions.js in tests/afk-runner/agent-config.test.ts, cli.test.ts, config.test.ts, mcp-servers.test.ts, work/fix-target.test.ts, work/implement.test.ts, work/verify.test.ts (agent-config and implement also import review-loop, so file counts overlap: 17 + 7 − 2 = 22; 24 + 7 = 31). tests/utils/grouped-assertions.ts exists (31 lines, rg over openspec/changes/afk-runner-extract-a/ shows zero mentions of grouped-assertions). D3's rewire enumeration names only review-loop and mutation-improve specifiers; D2 states "The guard never grows a violation allowlist"; Migration 1.3 verify is "bun test tests/afk-runner && bun run typecheck" with the guard green — unreachable. The design's claim "the union is exactly what Phase B renames into one repo root" also fails for these imports (tests/utils/ disappears at the split). Fix direction: task 1.2/1.3 must port or inline the grouped-assertions helper (31 lines) into tests/afk-runner, or otherwise state the disposition.
- **Resolution**: edited — Confirmed by an independent guard-equivalent scan: src 20 files / 23 stmts (matches design), tests 22 files / 31 stmts — the delta over the design's 17/24 is exactly the 7 `tests/utils/grouped-assertions.js` imports (7 files; agent-config and implement overlap both groups). Without a disposition the guard stays red at 1.3 and D2 forbids an allowlist. Fixed in design.md: Context now records the split figures (22 test files / 31 statements total); D3 adds the disposition — `tests/utils/grouped-assertions.ts` (31 lines, zero imports) copies into `tests/afk-runner/grouped-assertions.ts` (copy, not move: papai tests keep the original) with its 7 importers rewired in 1.3; D2's staging and the Risks row carry the corrected offender counts; Migration 1.2/1.3 include the helper copy and its rewire; proposal.md's rewire bullet states the measured set including the 7. With the helper inside the union, D2's 'the union is exactly what Phase B renames into one repo root' claim now holds.

#### MATERIAL — F2

> Nothing under the runner's source root or the runner's test root SHALL import a module from outside the runner's root.
- **Question**: Is "the runner's root" afk-runner/ alone or the union afk-runner/ ∪ tests/afk-runner/? Read literally (afk-runner/ only), the normative sentence outlaws intra-test-root imports — the ported tests importing tests/afk-runner/fixtures/fake-pipeline.ts and each other — which design D2 explicitly requires the guard to allow; the same requirement later says "The guard's scope SHALL be the runner's trees alone", switching between singular "root" and plural "trees". Since this spec is the durable contract that travels with the Phase B split (the proposal's stated reason for the capability), the fence should be stated as the two-root union (or post-split: the repo root containing both trees) so a future guard implementation derived from the spec alone cannot be conformingly stricter than the design.
- **Code evidence**: Design D2 states the fence precisely: "The union, not `afk-runner/` alone, is the fence: tests must reach `afk-runner/src` and each other (`fixtures/fake-pipeline.ts`)". Verified tests/afk-runner/fixtures/fake-pipeline.ts exists and is imported upward-set-internally by tests/afk-runner/graph/pipeline-work.test.ts (../../../review-loop/src/agent-runner.js plus the fixture) — under an afk-runner/-only reading of the spec sentence, that fixture import fails a conforming guard. The spec's scenarios ("escapes the runner root into a sibling workspace") point to the union reading but the normative requirement text does not commit to it.
- **Resolution**: edited — Confirmed: the requirement's normative text said 'outside the runner's root' (singular) while scanning 'both roots' — a literal afk-runner/-only reading would outlaw the intra-test-root imports D2 requires (fixtures/fake-pipeline.ts). Since this spec is the durable contract traveling with the Phase B split, the fence is now stated as the union: requirement retitled 'Runner trees import nothing outside their union', normative text and the first scenario say 'the union of the runner's source root and test root (after the repository split, the repository containing both)' / 'escapes the union of the runner's two roots', and the Purpose sentence matches. A future guard derived from the spec alone can no longer be conformingly stricter than the design.

#### NITPICK — F3

> the five claude-module tests (`backend-select`, `claude-argv`, `claude-stream`, `claude-spawn-dir`)
- **Question**: "Five" names four modules — which is the fifth claude-module test? The consistent arithmetic is four claude-module tests plus the two orphaned modules' (config, worktree) = six deleted of eighteen ported, leaving twelve test files for the twelve testable survivors (progress-log unported per D4).
- **Code evidence**: ls tests/review-loop filtered to closure modules shows exactly backend-select.test.ts, claude-argv.test.ts, claude-stream.test.ts, claude-spawn-dir.test.ts as claude-module test files (plus config.test.ts, worktree.test.ts for the orphans); 18 ported − 6 deleted = 12, matching the 13 survivors minus progress-log (no ported test). No fifth claude-module test exists.
- **Resolution**: edited — Confirmed: exactly four claude-module test files exist in tests/review-loop (backend-select, claude-argv, claude-stream, claude-spawn-dir). design.md D5 now reads 'the four claude-module tests' and states the arithmetic explicitly: 6 of 18 ported files deleted, 12 remaining, matching the 13 survivor modules minus the unported progress-log.

#### NITPICK — F4

> Rewire the 10 src files and 19 test imports from `review-loop/src` to the owned backend; declare `p-limit` in `afk-runner/package.json`; the guard goes green.
- **Question**: The proposal carries the superseded drafting figure (actual: 20 src files / 23 import statements, per both the design's own measurement and my scan) and the design attributes "10 src files and 19 test imports" to the umbrella — but the verbatim string exists only in this proposal; the umbrella artifacts read "10 src + n imports" / "(10 src, n)" / "the n and n imports". Should the proposal's What-Changes bullet be corrected to the measured set (or the guarded-set statement) so the two artifacts of the same change agree?
- **Code evidence**: Guard-equivalent scan counted 20 src files / 23 statements importing review-loop/mutation-improve upward (agent-runner, spawn, progress-log types, claude-argv, diff-guard consumers — exactly the five modules the design enumerates). rg "10 src files|19 test" over openspec/changes/afk-runner-extraction/ returns only "10 src + n imports" (proposal.md), "(10 src, n)" (design.md), and "the n and n imports" (tasks.md, task-phase-a.md); the full phrase appears only in afk-runner-extract-a/proposal.md. Design Context already labels the figure stale and says the guard scan defines the set, so execution risk is nil — internal-consistency cleanup only.
- **Resolution**: edited — Edited, with a correction to the finding's evidence: the stale figure is not unique to this proposal — the umbrella's tasks.md 1.3 and task-phase-a.md carry 'the 10 src files and 19 test imports' too (the finding's rg pattern failed to match them), so the umbrella artifacts stay as they are (inherited-decision material); this change's proposal.md is the artifact that must agree with its own design. Its What-Changes bullet now states the measured set (20 src / 23 statements against review-loop/mutation-improve, plus the 7 grouped-assertions test imports per F1) and that the guard's scan, not any count, defines the set — agreeing with design Context.

#### NITPICK — F5

> A local `tests/afk-runner/agent-backend/test-helpers.ts` carries only the subset the ported tests use (`makeTempDir`, `cleanupTempDirs`, `silentTrace`, `silentReporter`, the claude-context fixtures until 1.4 deletes their consumers)
- **Question**: The stated criterion is "only the subset the ported tests use", but the ported tests use exactly makeTempDir and cleanupTempDirs — silentTrace, silentReporter, and the claude-context fixtures (claudeRunContext, claudeResultLine, claudeRecordingSpawn, promptOutputPath, claudeScratchResponder) have zero uses in the portable set. Trim the list, or drop the "only the subset" claim?
- **Code evidence**: The seven closure tests importing tests/review-loop/test-helpers.js are agent-runner, claude-spawn-dir, config, line-handler, progress-log, trace-log, worktree (rg -l, matching the design's "seven"); progress-log is the non-portable one. All six portable ones import exactly `cleanupTempDirs, makeTempDir` (import lines read). rg -c for silentTrace/silentReporter/claudeRunContext/claudeResultLine/claudeRecordingSpawn/promptOutputPath/claudeScratchResponder across those six files returns nothing — those helpers serve only non-portable tests (progress-log, loop-controller, worker-pool), and the claude machinery the portable tests do use (createClaudeStreamDecoder, inline createClaudeSpawnDir fakes) comes from closure modules or is defined inline, not from test-helpers.
- **Resolution**: edited — Confirmed by reading the import lines of the six portable helper-importing tests (agent-runner, claude-spawn-dir, config, line-handler, trace-log, worktree): all import exactly { cleanupTempDirs, makeTempDir }; silentTrace/silentReporter/claude-context fixtures serve only non-portable tests (progress-log, batch-outcomes). design.md D4's helper list is trimmed to makeTempDir/cleanupTempDirs with the measurement recorded, and D5's deletion note no longer assumes claude fixtures in the helper ('none are carried per D4').

### Round 2

**Verdict**: 1b 4m 2n · 7 resolved · 0 dismissed · open

#### BLOCKER — F1

> The portable set is 17 of the 18 closure tests plus `diff-guard.test.ts` (18 files). Two carve-outs: `progress-log.test.ts` stays in review-loop untouched
- **Question**: What disposition does the ported `diff-stats.test.ts`'s `isTestFile` import from `../../.hooks/tdd/test-resolver.mjs` get — inline the expected booleans for the eight-path agreement row, copy a minimal predicate into the local helper, or drop that row? The carve-out list must name it (making three), and D3's rewire enumeration plus Migration 1.2/1.3 must carry the chosen disposition.
- **Code evidence**: Repo-verified, not repo-answerable (the disposition is product judgment; the inconsistency is a check). tests/review-loop/diff-stats.test.ts:8 imports `{ isTestFile } from '../../.hooks/tdd/test-resolver.mjs'`, used substantively at line 117 (`expect(touchedTestPath([p])).toBe(isTestFile(p))` across 8 paths, in a test named 'agrees with the repo TDD resolver rather than inventing a second rule'), so it cannot silently ride a rewire. `.hooks/` sits at the repo root, outside the union `afk-runner/ ∪ tests/afk-runner/`, so after 1.2's port to tests/afk-runner/agent-backend/ the verbatim specifier both fails to resolve from the new depth (breaking 1.2's `bun test tests/afk-runner tests/review-loop` green claim via module-not-found) and, under any depth-adjusted form, escapes the union so the D2 guard stays red at 1.3's 'guard unskipped and green' verification — the exact failure shape of r1-F1 (grouped-assertions), which was BLOCKER. Exhaustive scan of all relative and dynamic imports across the 18 closure tests plus diff-guard.test.ts confirms this is the only unhandled import outside closure ∪ test-helpers ∪ issue-schema: every other non-closure import is dispositioned (test-helpers → D4 local helper; issue-schema static line 20 + dynamic line 254 → D4 local minimal schema; grouped-assertions → D3 copy). Neither D3's rewire set ('review-loop/mutation-improve specifiers and the 7 grouped-assertions imports') nor D4's 'Two carve-outs' nor the Context's measured classes ('import upward against review-loop/mutation-improve' + 'a further 7 test files … import tests/utils/grouped-assertions.js') mention a third class, and D2 forbids an allowlist.
- **Resolution**: edited — Verified: tests/review-loop/diff-stats.test.ts:8 imports isTestFile from '../../.hooks/tdd/test-resolver.mjs' (repo-root, outside the union) and uses it substantively at :117 across 8 paths. Disposition chosen: inline expected booleans recorded from the resolver at port time — copying the predicate would invent the second rule the row's title forbids, dropping the row loses the coverage. design.md D4's carve-out list is now three and names it; D3 gained the port-time parenthetical; Migration 1.2 lists the isTestFile replacement among the port-time rewires (Migration 1.3 needs no change — the ported tests' specifiers are 1.2's, and 1.3 now says so via the D3/D4 text).

#### MATERIAL — F2

> nothing under `afk-runner/src` or `tests/afk-runner` imports outside `afk-runner/` (relative-up paths forbidden; `node:`/bare allowed)
- **Question**: Should proposal.md's guard bullet state the union fence — `afk-runner/ ∪ tests/afk-runner/` — matching the F2-corrected spec requirement ('Runner trees import nothing outside their union') and design D2 ('The union, not `afk-runner/` alone, is the fence')? As written, a guard built to the proposal's sentence outlaws the intra-test-root imports the design requires.
- **Code evidence**: Consistency check, repo-verified: tests/afk-runner is not under afk-runner/, and the intra-test-root imports D2 requires exist today — tests/afk-runner/run.test.ts:16-17 and five more files import './fixtures/fake-pipeline.js' / '../fixtures/fake-pipeline.js', which resolve inside tests/afk-runner/ but outside afk-runner/; a literal reading of the proposal sentence fails exactly those files. This is the residual instance of the r1-F2 defect class in a different artifact: r1-F2's fix retitled the spec requirement and its scenarios to the union, and design.md D2/D3 were aligned, but proposal.md's What-Changes bullet retains the pre-fix singular phrasing (verified against the artifact text above). New evidence relative to r1-F2: the fix demonstrably did not reach this sentence. Severity is MATERIAL rather than BLOCKER because the spec is the durable contract and D2 governs the implementation, but a reader auditing the change from proposal.md alone derives a conformingly stricter fence than the one the guard implements.
- **Resolution**: edited — Verified: proposal.md's guard bullet retained the pre-r1-F2 singular fence while tests/afk-runner files (run-recovery, resume-event, memo-failed, resume-equivalence, cli-stop, cli-summary, integration/live-run, graph/pipeline-execution) import fixtures/fake-pipeline.ts inside tests/afk-runner/ but outside afk-runner/. The bullet now states the union fence 'afk-runner/ ∪ tests/afk-runner/' with 'relative paths escaping that union forbidden', matching the spec requirement and design D2.

#### NITPICK — F3

> the `AgentBackend` knob's only closure importers are `agent-runner`/`agent-command`
- **Question**: Scope the sentence to the post-deletion survivors (e.g. 'after the named deletions, the knob's only closure importers are agent-runner/agent-command') or name backend-select's own config import — which is it?
- **Code evidence**: Repo-verified: review-loop/src/backend-select.ts:10 reads `import type { AgentBackend, ReviewLoopConfig } from './config.js'` — backend-select is itself a closure module and imports the knob (plus ReviewLoopConfig), so the present-tense closure fact as stated is falsifiable today. The orphan conclusion is invariant: backend-select is co-deleted in D5's first move (named deletion), and after the branch deletions remove agent-runner.ts:21's and agent-command.ts:12's AgentBackend imports, config.ts has zero closure importers; verified separately that config.ts:13's detectGitRoot import from worktree.ts is worktree's only closure importer edge, so the config→worktree orphan chain holds exactly as D5 states. Everything else in the Context's prune-shape paragraph checks out line-for-line (ClaudeRunContext at backend-select.ts:154, scrubCredentialValue at :80, agent-runner.ts:19 importing claude-spawn-dir), which is why this phrasing slip is the only residual.
- **Resolution**: edited — Verified: review-loop/src/backend-select.ts:10 imports AgentBackend from './config.js', so the Context sentence's present-tense 'only closure importers are agent-runner/agent-command' was falsifiable today (agent-runner.ts:21 and agent-command.ts:12 also import it). design.md Context now scopes the fact: backend-select is named as a knob importer co-deleted in the prune's first move, agent-runner/agent-command's knob imports die in the branch deletions, and only then is config.ts orphaned — the orphan-chain conclusion is unchanged.

### Round 3

**Verdict**: 0b 4m 2n · 6 resolved · 0 dismissed · open

#### MATERIAL — F1

> WHEN a run spawns any stage agent — drafter, reviewer, skeptic, resolver, estimator, decomposer, atomicity, or implementer
- **Question**: The scenario's stage-agent roster lists eight roles, but the runner's compiled role vocabulary has nine — 'planner' is missing. Should the roster include planner (or say "any of its agent roles") so a conformance check derived from this spec covers the full AgentRole vocabulary it ships with the split?
- **Code evidence**: Verified: afk-runner/src/config.ts:12-22 AgentRoleSchema enumerates nine roles with 'planner' at line 20; config.ts:41-42 documents PLAN_REPLAN_PASSES as "Structural plan-replan passes the planner agent gets before failing the run (D6)"; modelFor (config.ts:161) serves every AgentRole via the single spawn seam (agent-layer.ts:146, the only modelFor call site). The planner role is exercised on the spawn surface today: tests/afk-runner/mcp-servers.test.ts:553 composes config for role 'planner' ("planner, absent from the narrowing map, carries every base server") and tests/afk-runner/serve/stage-accounts.test.ts:102 records stepFinish('planner', 3, ...). No src call site passes 'planner' to a stage runner yet (rg over graph/drive/kernel/work finds none), but the spec's other role scenario ("for each of its agent roles") is generic, so only this enumeration is falsifiable — against the schema the same split carries.
- **Resolution**: edited — spec.md's stage-agent roster now enumerates all nine AgentRole values: 'planner' added between 'atomicity' and 'implementer', mirroring AgentRoleSchema's enumeration order (afk-runner/src/config.ts:12-22). A conformance check derived from the spec now covers the full role vocabulary that ships with the split.

#### MATERIAL — F2

> - Docs: `docs/architecture/afk-runner.md` (U-ledger Phase A record).
- **Question**: proposal.md's Impact section promises a docs deliverable, but no task produces it: design.md's Migration Plan is explicitly the run's task list ("One runner run, tasks in order") and ends at 1.5's closing gate with no docs step. Which task writes the U-ledger Phase A record, or should Impact drop it?
- **Code evidence**: Verified both task surfaces on disk: openspec/changes/afk-runner-extract-a/ contains no tasks.md (assumptions.md, design.md, proposal.md, review.md, specs/ only), so design.md's Migration Plan tasks 1.1–1.5 are this change's only task enumeration — its terminal step is "5. **1.5** closing gate pass: `bun run check:full && bun run test:mutate:changed`", no docs write anywhere in 1.1–1.5. The runner-facing umbrella list openspec/changes/afk-runner-extraction/task-phase-a.md was read end-to-end: its Scope schedules exactly 1.1–1.5 (guard/copy/rewire/prune/gate) and rg for 'docs|afk-runner.md' over it returns zero hits. The umbrella tasks.md's only afk-runner.md items are post-split (2.2 U-ledger doc in the NEW repo; 4.2 tombstone where the papai doc stood) — neither is the papai-side "U-ledger Phase A record" this Impact line promises. AGENTS.md describes docs/architecture/afk-runner.md as the living U-ledger, so the record is a real deliverable with no producing task in any artifact.
- **Resolution**: edited — design.md's Migration Plan step 1.5 now closes with the U-ledger Phase A record written to docs/architecture/afk-runner.md, giving proposal.md Impact's docs deliverable its producing task (the run's tasks carry no papai write-hook pipeline, so the record is 1.5's closing act, not a later operator's). Impact keeps the line; no task in 1.1-1.5 omits it now.

#### NITPICK — F3

> — the extensions the runtime can actually load, not `.ts` alone —
- **Question**: D2 justifies the guard's file-walk extension list (.ts/.mts/.cts/.js) as exhaustive of runtime-loadable extensions, but Bun also loads .mjs/.cjs/.jsx/.tsx, and this repo already ships .mjs at the root (.hooks/tdd/test-resolver.mjs — the very module D4 carves out). Should the list add .mjs/.cjs, or should the justification claim only the strict-TS convention's set rather than runtime-loadable completeness?
- **Code evidence**: Verified: find over afk-runner/ and tests/afk-runner for *.mjs/*.cjs/*.jsx/*.tsx returns zero files today, so there is no current exposure and the fence holds on the present tree; but the sentence's completeness claim is falsifiable (Bun resolves .mjs/.cjs), the guard is designed as a permanent fence, and a future .mjs upward import under either root would be invisible to the scan. Strict-TS convention makes such a file unlikely, hence NITPICK.
- **Resolution**: edited — D2's walk list extended from .ts/.mts/.cts/.js to .ts/.mts/.cts/.js/.mjs/.cjs/.jsx/.tsx — the full set of extensions Bun loads — so the 'extensions the runtime can actually load' justification is now literally true rather than falsifiable.

### Round 4

**Verdict**: 0b 0m 5n · 2 resolved · 3 dismissed · open

#### NITPICK — F1

> `progress-log.test.ts` is a `runReviewLoop` integration test importing non-closure modules (`loop-controller`, `run-state`, `issue-ledger`, `build-checker`) — not portable
- **Question**: Should the carve-out's non-closure module enumeration be complete (six modules, not four) so a future portability audit reading the design doesn't treat the parenthetical as the full list?
- **Code evidence**: Verified against tests/review-loop/progress-log.test.ts: the file imports two further non-closure modules beyond the four named — `formatDecidedLine, formatFoundLine` from '../../review-loop/src/issue-format.js' (line 12, value imports) and `IssueMatch, ReviewerIssue` types from '../../review-loop/src/issue-schema.js' (line 14). The named four are real (build-checker.js line 11 type-only, issue-ledger.js line 13, loop-controller.js line 15, run-state.js line 17). The conclusion is unaffected — loop-controller alone makes it non-portable, and the carve-out (stays in review-loop untouched) requires no implementer action — so the under-enumeration is cosmetic.
- **Resolution**: dismissed — Verified: the file also imports issue-format.js (line 12) and issue-schema.js (line 14), but the parenthetical names the load-blocking modules, not an exhaustive import inventory — loop-controller alone establishes non-portability and the carve-out (stays in review-loop, untouched) prescribes no implementer action.

#### NITPICK — F2

> The review-loop originals keep every papai-internal consumer (review-loop's own `cli`/`worker-pool`/`build-checker`; mutation-improve's `cost`/`worktree`/`live-format`/`run-stats`/`agent-runner` imports)
- **Question**: Is the 'every papai-internal consumer' parenthetical intended as exhaustive? As written, a reader auditing consumer impact would under-count: mutation-improve consumes nine distinct review-loop modules and review-loop's own in-tree closure consumers include more than the three named.
- **Code evidence**: Verified by scanning mutation-improve/src: besides the five named modules it imports diff-stats (pipeline.ts:10, merge-stats.ts:6), build-checker (cli.ts:9), live-renderer (cli.ts:10), and spawn (cli.ts:12) from review-loop/src — nine distinct modules total. Also, within review-loop itself, issue-matcher.ts:14 and issue-inspector.ts:15 import AgentBackend from './config.js' (closure consumers beyond the named cli/worker-pool/build-checker). The sentence's operative claim — originals keep every consumer because the copy is one-way and originals are untouched (Non-Goal) — is true regardless; no implementer action depends on the enumeration, so this is cosmetic precision only.
- **Resolution**: dismissed — Verified: mutation-improve imports 9 distinct review-loop modules and issue-matcher/issue-inspector also import config.js, but the parenthetical is illustrative — the operative constraint is the one-way copy with originals untouched (a Non-Goal), which holds regardless of consumer count, and no implementer action reads the enumeration.
