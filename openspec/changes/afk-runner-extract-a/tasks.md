<!--
SPDX-License-Identifier: BUSL-1.1
Copyright (c) 2026 Dmitriy Lazarev
Use of this software is governed by the Business Source License 1.1.
See LICENSE in the project root for details.
-->

## 1. Phase A — owned opencode-only agent backend (one runner run, tasks in order, tree green after each; design.md Migration Plan is authoritative — the atomicity pass folded its task 1.1 guard staging into task 1.2 below, so design's 1.2/1.3/1.4/1.5 map to tasks 1.1/1.2/1.3/1.4 here)

- [x] 1.1 Copy the backend and port its tests (design D1/D4, copy — never move): the 18-file closure (`agent-runner`, `spawn`, `progress-log`, `line-handler`, `backend-select`, `claude-stream`, `event-stream`, `cost`, `run-stats`, `config`, `worktree`, `todo-capture`, `live-format`, `trace-log`, `diff-stats`, `claude-argv`, `agent-command`, `claude-spawn-dir`) plus `mutation-improve/src/diff-guard.ts` copy byte-for-byte (SPDX headers intact, module filenames unchanged) into new `afk-runner/src/agent-backend/`; port 18 tests to the mirrored `tests/afk-runner/agent-backend/<name>.test.ts` (the 17 portable closure tests — `progress-log.test.ts` stays in review-loop untouched, a loop-controller integration test over non-closure modules — plus `tests/mutation-improve/diff-guard.test.ts`) with port-time specifier rewires only: impl imports depth-corrected to the copy; `test-helpers` imports → new local `tests/afk-runner/agent-backend/test-helpers.ts` carrying exactly `makeTempDir`/`cleanupTempDirs`; `agent-runner.test.ts`'s two `issue-schema.js` imports (static `ReviewerIssuesSchema` + dynamic `FixerResultSchema`) → a local minimal zod schema; `diff-stats.test.ts`'s `isTestFile` (`../../.hooks/tdd/test-resolver.mjs`, outside the union) → inline expected booleans recorded from the resolver at port time, keeping the eight-path agreement row; copy `tests/utils/grouped-assertions.ts` → `tests/afk-runner/grouped-assertions.ts` (original untouched). Claude-path describes and fixtures ride the port verbatim — their deletion is task 1.3's. Review-loop/mutation-improve originals untouched; existing afk-runner upward imports stay unrewired (task 1.2's). Verify: `bun test tests/afk-runner tests/review-loop tests/mutation-improve` — all three suites green together
- [x] 1.2 Land the boundary guard and rewire every upward import to the owned backend (design D2/D3): land `tests/afk-runner/no-upward-imports.test.ts` with the final assertion body — self-contained walk of every `.ts/.mts/.cts/.js/.mjs/.cjs/.jsx/.tsx` file under `afk-runner/src` and `tests/afk-runner` (skipping `node_modules`), import specifiers extracted by a plain regex (NOT line-anchored — it must span the newlines oxfmt puts inside wrapped import clauses) over `import … from '…'`, `export … from '…'`, bare `import '…'`, dynamic `import('…')`, and `require('…')`, type-only included; leg 1 fails naming each offending file and specifier when a relative specifier resolves (via `path.resolve` against the importing file) outside the union `afk-runner/ ∪ tests/afk-runner/`; `node:`-prefixed and bare specifiers pass leg 1; leg 2 asserts every bare specifier imported under `afk-runner/src` is declared in `afk-runner/package.json` dependencies (package name extracted from the specifier). The guard imports nothing from papai (no `src/ts-ast` seam, no oxc) and has no allowlist, ever. Run the landed body red once — it must fail listing the offenders (measured before task 1.1's copy: 20 src files / 23 statements and 22 test files / 31 statements importing upward against `review-loop`/`mutation-improve`/`tests/utils`, plus the undeclared `p-limit`; the guard's scan, not any count, defines the set) — then, over that same body, rewire: specifier replacement only at the importing file's depth (`…/review-loop/src/<mod>.js` → `…/afk-runner/src/agent-backend/<mod>.js`, `…/mutation-improve/src/diff-guard.js` → `…/afk-runner/src/agent-backend/diff-guard.js`), including the 7 `tests/utils/grouped-assertions.js` importers → `tests/afk-runner/grouped-assertions.js`; no signature or call-site changes anywhere. Declare `p-limit` in `afk-runner/package.json` at the root pin (`^7.3.2`) and run `bun install` to commit the regenerated `bun.lock`. The guard goes green over the same body that ran red, both legs (fence + manifest). Verify: `bun test tests/afk-runner && bun run typecheck` — guard green, tree green
- [x] 1.2.1 Verify and land the boundary-guard slice (hand re-target: the 1.2 bracket exhausted mid-observation while its work is fully applied in the working tree — guard green on both legs, `bun test tests/afk-runner` 1830/1830, typecheck clean): re-run the task's verification over the applied tree, then land the slice per the runner's protocol — the checked box and the applied work in one commit. Verify: `bun test tests/afk-runner && bun run typecheck`
- [x] 1.3 Prune to opencode-only (design D5, **BREAKING** — the run's last code-changing task): delete the six copied modules `claude-argv.ts`, `claude-stream.ts`, `backend-select.ts`, `claude-spawn-dir.ts`, `config.ts`, `worktree.ts` (13 survivors: `agent-runner`, `agent-command`, `spawn`, `progress-log`, `line-handler`, `event-stream`, `cost`, `run-stats`, `todo-capture`, `live-format`, `trace-log`, `diff-stats`, `diff-guard`); branch deletions — in `agent-command`: `claudeCommand`, `claudeChildEnv`, `STRIPPED_ENV_NAMES`, `PROFILE_CREDENTIAL`, `refuseOpencodeRouteKnobs`, `loadClaudeConventions`, `ClaudeSpawnContext`, and the claude-only option fields (`backend`, `claude`, `effort`, `systemPrompt`, `AgentCommand.stdin`); in `line-handler`: the claude decoder injection, the credential-scrub sink, the `credentialValue` threading; in `agent-runner`: the claude branches (spawn-dir creation, decoder re-arm, the exit-0 result-outcome gate, the conventions system prompt, the stderr scrub) and the `ClaudeSpawnDir`/`ClaudeRunContext` re-exports; in `todo-capture`: the `TodoWrite` alias and its normalization shape; `opencodeCommand`, `opencodeEnv`, `extraArgs`, and the continuation mapping stay untouched. Rehome `MAX_ARG_STRLEN` (131_072) verbatim from the dying `claude-argv.ts` into its one surviving consumer `afk-runner/src/agent-config.ts`, with `tests/afk-runner/agent-config.test.ts` re-importing from there and still pinning the value and refusal message; `modelFor` unchanged in body. Delete the 6 matching ported tests (`backend-select`, `claude-argv`, `claude-stream`, `claude-spawn-dir`, `config`, `worktree` — 12 ported files remain, matching 13 survivors minus the unported `progress-log`) plus the claude-path describes inside the surviving tests. Golden real-run fixtures (`tests/afk-runner/fixtures/real/`, replayed by `tests/afk-runner/parity/golden-replay.test.ts`) must stay green. Verify: `bun test tests/afk-runner && bun run typecheck`
- [x] 1.4 Closing gate pass and U-ledger record (design D7): full gate over the branch — `bun run check:full` — plus the mutation ratchet — `bun run test:mutate:changed` over the branch diff (seeds baselines for the new `afk-runner/src/agent-backend/**` targets; dilution WARNs pass, kill-count regressions fail); then write the U-ledger Phase A record to `docs/architecture/afk-runner.md` (proposal.md Impact's docs deliverable — this run's closing act, not a later operator's). Verify: `bun run check:full && bun run test:mutate:changed` both exit 0, and the Phase A record is present in `docs/architecture/afk-runner.md`
