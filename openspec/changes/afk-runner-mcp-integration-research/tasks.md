This change is docs-only research (skip_specs: true; design.md records no Open Questions — the genuinely unknown items, merge semantics and claude-route viability, are in-scope experiment outcomes below, not deferred decisions). No product code, no tests: order of work is evidence-first (design D2) — rig and live evidence before any drafting — and every behavioural claim lands with a **verified** / **by inspection** label.

## 1. Rig and deliverable skeleton

- [x] 1.1 Create `docs/architecture/afk-runner-mcp-research.md` as a skeleton: SPDX header (licence-header gate), evidence-first section order (§1 injection surfaces, §2 scoping, §3 catalogue, §4 credentials/security/degradation, §5 recommendation + follow-ups), the verified/by-inspection labelling convention, and the D8 boundary statements as section stubs. Verify: `bun run format:check && bun run lint` (SPDX header accepted).
- [x] 1.2 Build the throwaway experiment rig in a temp dir (not committed): minimal trace-logged stub MCP server exposing two tools, placeholder token values only, a pid-recording wrapper on every child, and a teardown that kills recorded pids only. Verify: recorded rig transcript shows a `connected` status and `<server>_<tool>` naming through one `opencode run --dir <cwd>` spawn, and `ps -p <recorded pids>` is empty after teardown.

## 2. Live evidence through afk-runner's spawn shape

- [ ] 2.1 Run the merge-vs-override experiment, **ambient-only** configuration (afk-runner's current state): does a repo-local `opencode.json` under the spawn cwd / `--dir` load at all, and what reaches the binary (proposal 1b; design D3). Verify: evidence recorded in the doc with a **verified** or **by inspection** label and file:line anchors; `rg -n 'ambient' docs/architecture/afk-runner-mcp-research.md` finds the section.
- [ ] 2.2 Run merge-vs-override, **content-set** configuration: discovered file vs `OPENCODE_CONFIG_CONTENT` carrying provider + deny-by-default permission blocks — merge, override, or lose (proposal 1a+1b; design D3). Verify: outcome recorded in the doc; any unrunnable configuration carries the unverified label forward into §5's ranking; `bun run format:check`.
- [ ] 2.3 Drive the claude native-profile spawn shape (`--strict-mcp-config --mcp-config` with a non-empty doc replacing `defaultCreateClaudeSpawnDir`'s empty one) through review-loop's claude path; record what reaches the child and the threading prerequisite (proposal 1d; design D2) — nothing designed. Verify: recorded evidence in the doc; `bun run format:check`.
- [ ] 2.4 Re-ground the degrade-never-hang and unattended-grant claims through the rig: a failing server command, a hung child (30s bound), an `enabled: false` entry, and an `ask` grant under afk-runner's inherited-env shape. Verify: each outcome labelled and recorded in §4 of the doc; recorded pids clean after teardown.

## 3. Analysis, ranking, and boundaries (design-grounded, no runtime)

- [ ] 3.1 Write the injection-surface comparison: options (a) `OPENCODE_CONFIG_CONTENT` built by afk-runner — including what it clobbers of ambient config, (b) repo-local file, (c) `AGENT_MCP_SERVERS`-style env knob, (d) claude `--mcp-config` — scored with the §2 evidence and the on-record opencode-agent mechanics, each claim labelled (proposal 1; design D1–D3). Verify: `rg -n 'verified|by inspection' docs/architecture/afk-runner-mcp-research.md` covers every option row; `bun run format:check`.
- [ ] 3.2 Write the scoping comparison: per-role vs per-stage vs per-depth vs global, scored on the five fixed D4 dimensions (trust edge, blast radius — checking servers for reviewer/skeptic vs work servers for drafter/decomposer, config surface against the strict five-key `RunnerConfigSchema`, grant composition incl. `modelFor` composition, cost profile). Verify: one table, dimensions identical across axes; `bun run format:check`.
- [ ] 3.3 Write the server catalogue: structural code index, papai-hosted remotes (`/mcp/plugin/<pluginId>`, context-vault — streamable-http constraint vs local stdio routes), task-tracker access, web search; each entry with transport, install/pinning on ephemeral runners, startup/timeout behaviour, anchored to repo docs/code file:line (proposal 3; design D5). Verify: every catalogue entry carries a repo anchor; `bun run format:check`.
- [ ] 3.4 Write the credentials/security/degradation section: S3-9 model-readability on the content route, claude child-env readability, rejection of untrusted-input-defined servers (task files, chat, issue bodies), allow-or-absent grants, degrade-never-hang, and the L0/L1 emission guidance for a dead server (proposal 4; design D6). Verify: `rg -n 'untrusted|allow|30s' docs/architecture/afk-runner-mcp-research.md` finds the doctrine restated per surface; `bun run lint`.
- [ ] 3.5 Write the ranked recommendation, the named follow-ups (credential containment, per-server opt-out), and the follow-up implementation-change outline: capability named at feature-domain granularity, config-surface and injection-point sketch — proposal-shaped, nothing implemented (proposal 5; design D7). Verify: ranking derives from the §3.1/§3.2 tables and states which conclusions any unverified label changes; `bun run format:check`.
- [ ] 3.6 Fill the D8 boundary statements in full: gating (opencode permission maps / claude `--mcp-config` vs papai-core `tool_prefs` — and the papai-hosted-server bypass consequence), scope model (no persisted state; follow-up config keys at runner level, outside papai's storage/config-context, platform-instance, and user ids), dependency surface (none — no DB, no drizzle, no packages), hook surface (docs are not gateable implementation code; no TDD pair). Verify: `bun run typecheck && bun run lint && bun run format:check`.

## 4. Rollout and gates

- [ ] 4.1 Add the one-line pointer in `docs/architecture/afk-runner.md` and the docs-table row in `CLAUDE.md` (proposal Files to touch). Verify: `bun run format:check && bun run lint`.
- [ ] 4.2 Final: run full `bun test`, `bun run typecheck`, `bun run lint`, and confirm the affected docs pages are updated and consistent (`docs/architecture/afk-runner-mcp-research.md`, `docs/architecture/afk-runner.md`, `CLAUDE.md`). Verify: `bun test && bun run typecheck && bun run lint && bun run format:check`.
