# Tasks — tests-consolidation

This file is also the change's **evidence ledger** (design D5 / Migration steps 2 and 5): the
before/after audit numbers, per-file wall times, `test:slowest` deltas, row-by-row accountings, and the
six per-source mutation scores are recorded here as the rollout evidence. `reports/` is gitignored —
numbers must be copied into the task when the producing run completes.

**Deferred open questions (design.md, not baked in as assumptions):** CI wiring for `test:audit`
(informational job) and `test:status`-style audit query commands are explicitly deferrable; neither is
in scope for this change. The spec gates rollout on cited numbers in follow-on changes, not on a CI
job or query tool.

## 1. Grouped-assertion helper (design D1/D2, TDD)

- [x] 1.1 Write failing `tests/utils/grouped-assertions.test.ts` pinning the helper contract: zero-failure pass-through; multi-failure aggregation reports **every** failed row with its label and input/expected pair plus the matcher's Expected/Received text; a failing row at position k is identified by its own label (not the first row, not the group); async `run` rows are awaited sequentially; a non-assertion throw inside a row is re-reported with its row label, never swallowed. SPDX header. Verify: `bun test tests/utils/grouped-assertions.test.ts` fails on the missing module (red).
- [x] 1.2 Implement `tests/utils/grouped-assertions.ts`: `assertEach<T>(rows: readonly Row<T>[], run: (row: T) => void | Promise<void>): Promise<void>` with `Row<T>` = `label: string` + case fields; sequential execution; one aggregated `Error` listing every failed row. Plain `bun:test` + stdlib, no new dependency, SPDX header. Do **not** touch `tests/utils/test-helpers.ts` or any frozen file (standing qualification freeze). Verify: `bun test tests/utils/grouped-assertions.test.ts` green.

## 2. Fragmentation audit (design D3, TDD)

- [x] 2.1 Write failing `tests/scripts/test-audit/fragmentation.test.ts` against an in-memory file map via the injected `read`/`scan`/`exists` deps interface: `test(`/`it(` cases counted only with a literal first argument; `test.each(`/`it.each(` counted at literal row count when the first argument is a static array literal (1 when computed); matcher calls (`expect(`, `node:assert` `assert.*(`, `schemaValidates(`, `expectAppError(`) attributed to the enclosing case segment; per-file case count, matcher-call count, single-or-zero-assert share; `heuristicVersion` field present; scan set excludes `tests/{stories,e2e,client,visual,operational,smoke,platform}/**`. SPDX header. Verify: `bun test tests/scripts/test-audit/fragmentation.test.ts` fails before the implementation exists (red).
- [x] 2.2 Implement `scripts/test-audit/fragmentation.ts` as pure analysis over the injected fs deps (regex + DI, mirroring `scripts/test-import-graph.ts`), heuristic v2 with `heuristicVersion` recorded in the report. Verify: `bun test tests/scripts/test-audit/fragmentation.test.ts` green.
- [x] 2.3 Implement `scripts/test-audit/cli.ts` wiring real fs (Bun `Glob`) and add the `"test:audit"` script to `package.json` pointing at it (the CLI and its script are one trivially coupled unit — neither is verifiable without the other). Verify: `bun run test:audit` exits 0, writes `reports/test-audit/fragmentation.json`, modifies no test file / runner config / gate (read-only), and no test verdict changes.
- [x] 2.4 Record the audit **before-numbers** for the four pilot files (case count, matcher-call count, single-assert share) plus the run's `heuristicVersion` into this file — v2 numbers are not comparable with the v1 figures in design Context; the before/after pair must share this version. Verify: re-run `bun run test:audit` and confirm the four rows recorded here match `reports/test-audit/fragmentation.json` (same `heuristicVersion`).

**Audit before-numbers (heuristic v2, whole-run totals: 1,506 files / 15,928 cases / 34,542 matcher calls / 48.0% single-or-zero share):**

| File | Cases | Matcher calls | Single-or-zero share |
| --- | --- | --- | --- |
| `tests/live-status/tool-status-labels.test.ts` | 43 | 44 | 0.9767 (42/43) |
| `tests/plugins/manifest-schema.test.ts` | 54 | 54 | 0.8704 (47/54) |
| `tests/plugins/task-provider-youtrack/classify-error.test.ts` | 77 | 96 | 0.8052 (62/77) |
| `tests/review-loop/summary.test.ts` | 56 | 84 | 0.6786 (38/56) |

## 3. Wall-time baseline (design Migration step 2 — must precede any rewrite)

- [x] 3.1 Run one full `bun run test` and capture into this file: the `bun run test:slowest` summary and per-file `bun test <file>` wall times for the four pilot files (`tests/live-status/tool-status-labels.test.ts`, `tests/plugins/task-provider-youtrack/classify-error.test.ts`, `tests/review-loop/summary.test.ts`, `tests/plugins/manifest-schema.test.ts`). `reports/` is gitignored — this run is the only producer of the delta's before-half; skipping it forfeits the evidence. Verify: `bun run test:status` shows the completed run (not stale) and `bun run test:slowest` reproduces the recorded per-file figures.

**Wall-time baseline (before any rewrite; 2026-08-24):**

- Full run: 16,851 tests across 1,552 files — 16,843 pass, 1 fail (pre-existing flake `tests/opencode-agent/shell.test.ts` "streams stdout line by line", 15.1 s under parallel load; re-ran green solo in 5.96 s), 7 skip, 125,174 expect() calls, 233.83 s (parallel). `bun run test:status`: freshness current, fingerprint 49cd9f8474698a64.
- Per-file `bun test <file>` wall (`/usr/bin/time -p`, child of `bun test`):
  - `tests/live-status/tool-status-labels.test.ts` — real 0.66 s
  - `tests/plugins/task-provider-youtrack/classify-error.test.ts` — real 0.63 s
  - `tests/review-loop/summary.test.ts` — real 0.64 s
  - `tests/plugins/manifest-schema.test.ts` — real 0.64 s
- `bun run test:slowest` (top 10 of 20 recorded, in-test time): 72.9 s review-loop/loop-controller · 45.7 s review-loop/worker-pool · 39.4 s review-loop/cli · 32.3 s review-loop/issue-processor · 31.1 s opencode-agent/git · 29.1 s sdd-runner/orchestrator · 25.0 s scripts/story-manifest · 24.7 s review-loop/worktree · 23.1 s review-loop/issue-processor-batch. No pilot file is in the top 10 (all four are ~0.6 s standalone).

## 4. Pilot consolidation, one file at a time (design D4 / Migration step 3)

Ordered lowest-stakes first; `summary.test.ts` (covered source baseline = 1.0, the binding bar) last,
after the mechanism is proven. For each file: re-verify eligibility (same fixture, order-independent,
exact-equality matrices) → rewrite per-value cases into one `test('…matrix')` using `assertEach` (fold
`test.each` row generators into rows — row generators are not consolidation) → every former case's
input/expected pair survives as a row with the same matcher strength, boundary/fallback cases included
→ record in this file: before/after case count + share, the row-by-row accounting (former cases → rows),
and the (by-construction lower) matcher-call count as the fragmentation signal. Each task's trailing
Verify is the gate for that file.

- [x] 4.1 Consolidate `tests/live-status/tool-status-labels.test.ts` (42/43 single-assert, incl. 40/41-char truncation boundary rows) and record its evidence here. Verify: `bun test tests/live-status/tool-status-labels.test.ts` green, then `bun run test:audit` shows that file's case count below its task 2.4 before-count.

  Evidence: audit before → after: cases 43 → 6, matcher calls 44 → 7 (by-construction drop — the fragmentation signal), share 0.9767 → 0.8333. Runner view: 76 former cases → 6 (74 rows + 2 kept standalone), expect() calls preserved exactly at 77 (before/after). Row-by-row accounting: `formatToolStatus` rendering matrix = former 33 single-expect cases (incl. 40/41-char truncation boundaries, non-record/array/null/number/function inputs, host-with-port, quote:false forms) as 33 rows; REGISTRY matrix = former 34-iteration loop (each a runner case) as 34 rows, labels `${name} renders ${JSON.stringify(expected)}`; reminder/alert = 2 rows; ru locale matrix = 5 rows (incl. ru truncation); kept standalone verbatim: `cancel_reminder` (2 partial-match expects, not an exact-equality row) and `explicit en === default` (cross-call equality, not a literal expected value). Every former input/expected pair survives as a row with the same `.toBe` strength.
- [x] 4.2 Consolidate `tests/plugins/manifest-schema.test.ts` (37/43; the only pilot file containing `test.each` — its literal rows become matrix rows) and record its evidence here. Verify: `bun test tests/plugins/manifest-schema.test.ts` green, then `bun run test:audit` shows that file's case count below its task 2.4 before-count.

  Evidence: audit before → after: cases 54 → 7, matcher calls 54 → 0 (by-construction extreme — the shared `runManifestMatrix`/`rejectionsOf` callbacks sit at module scope, outside every case segment; the fragmentation signal, never the preservation discriminator), share 0.8704 → 1.0. Runner view: 54 cases → 7 (one per describe), expect() calls preserved exactly at 88 (before/after). Row-by-row accounting: scope matrix 4 rows (incl. provider-array defaults row carrying the former two `toEqual([])` as one combined-object `toEqual`); strict-validation matrix 20 rows (Windows-absolute split into drive/UNC rows — former 2-expect case; the original file's two exact-duplicate case pairs preserved as duplicate rows labeled "duplicate case in the original file, preserved"); contribution-permission matrix 4 rows; transformer/config-host matrix 8 rows; host-allowlist matrix 4 rows; field-pattern matrix 11 rows — the former `test.each` row generators (4 semver accepts, 5 non-semver rejects with exact message/path, 2 validator-name rejects) folded into literal rows; parsed-defaults matrix 6 rows (storageScope's former 3 expects as 3 rows; boolean-flags/list/contributes defaults rows with combined-object picks, `toEqual` ≡ former `toBe` for primitives). Every former input/expected pair survives with the same matcher strength; `rejectionsOf` (with its internal success assert) kept verbatim.
- [x] 4.3 Consolidate `tests/plugins/task-provider-youtrack/classify-error.test.ts` (63/77, incl. fallback-path rows) and record its evidence here. Verify: `bun test tests/plugins/task-provider-youtrack/classify-error.test.ts` green, then `bun run test:audit` shows that file's case count below its task 2.4 before-count.

  Evidence: audit before → after: cases 77 → 16, matcher calls 96 → 96 (flat — the row `check` closures sit at describe scope between case sites, so the textual heuristic attributes them to the preceding segment; the count is the fragmentation signal only, never the preservation discriminator), share 0.8052 → 0.1875 (decrease). Runner view: 77 cases → 16 (one matrix per describe), expect() calls preserved exactly at 93 (before/after) plus the 3 `node:assert` `assert.equal` calls kept verbatim in their rows. Row-by-row accounting: 77 former cases → 77 rows across 16 describes (HTTP status 11, context 5 incl. the `taskId: 'unknown'` fallback row, network detection 6, generic handling 6 incl. null/undefined/string inputs, result type 3 incl. already-classified pass-through (`toBe(original)`), workflow validation 3 incl. Russian/smart-quote extraction, user messages 4, body validation 9, field-name cleaning 5, stopword filter 5, separator rewrite 1, pattern matching 11 incl. both stopword-drop fallback rows, workflow context 2, invalid 400 body 1, not-found fallbacks 4 incl. saved-query `resourceId: 'unknown'`, error name 1). Every former input/expected pair survives verbatim inside its row's `check` with the same matcher strength, multi-expect cases intact (e.g. taskId row keeps all 3 expects).
- [x] 4.4 Consolidate `tests/review-loop/summary.test.ts` (38/56; covered source `review-loop/src/summary.ts` holds the perfect 1.0 mutation baseline — any lost kill fails the 5.3.4 re-measure) and record its evidence here. Verify: `bun test tests/review-loop/summary.test.ts` green, then `bun run test:audit` shows that file's case count below its task 2.4 before-count.

  Evidence: audit before → after: cases 56 → 16, matcher calls 84 → 84 (flat — row bodies live at describe scope between case sites, attributed to the preceding segment by the textual heuristic; fragmentation signal only), share 0.6786 → 0. Runner view: 56 cases → 16 (one matrix per describe), expect() calls preserved exactly at 84 (before/after). Row-by-row accounting: 56 former cases → 56 rows across 16 describes, every former test body preserved **verbatim** inside its row's `body` closure (no matcher or expected-value edits anywhere — the binding 1.0 baseline forbids strength drift); the file's `beforeEach` idCounter reset is replicated per row by `runBodyRow` so every row's ledger mints the exact record ids (`#00000001`…) its assertions expect; fixtures (makeRecord/ledgerOf/zeroMetric/busyMetric/cachedMetric/inputOf/MINIMAL_SUMMARY/lineStartingWith) unchanged. Describes: verdict 5, zero suppression 4, timing/cost 4, rounds/pool 2, issue groups 3 (incl. 21-record cap), artifacts 1, stats line 4, buildMetricsJson 3, verdict edges 3, exact structure 4, inspector 2, stats/issues edges 4 (incl. exactly-20 boundary), aggregation 5, exposure 4, check-behind 4, per-kind 3.

## 5. Gates and explicit re-measures (design D5 / Migration step 4)

- [x] 5.1 Full `bun run test` green after all four rewrites; capture summary (cases/files/duration) here. Verify: `bun run test:failures` reports no failures and `bun run test:status` matches the captured summary.

  Summary (2026-08-24): parallel run — 1,552 files · 16,633 tests · 16,624 pass · 2 fail (load flakes `tests/mutation-improve/contracts.test.ts` + `tests/sdd-runner/gate-resume-tail.test.ts`, both re-run green solo 14/14) · 7 skip · 250.43 s; the -218 case delta vs the 3.1 baseline exactly equals the four pilots' runner-case reduction (70+47+61+40). Clean serial run — 1,552 files · 16,262 tests · 16,255 pass · **0 fail** · 7 skip · 161.62 s; `test:failures`: "no failing tests in the last run"; `test:status`: PASS, freshness current (fingerprint a24805a887bfc8a1).
- [x] 5.2 Run `bun test:coverage` (full-suite run — cannot be scoped to pilot sources; bun's denominator spans every discovered production file). Verify: `bun coverage:ratchet` exits 0 with the aggregate floor unchanged.

  Summary: plain local invocation hits a pre-existing host defect — Bun 1.3.13 on this macOS 26.5 box fails every `--coverage` finalization with "An internal error occurred (WriteFailed)", including on untouched master test files (verified `tests/utils/datetime.test.ts`), so it is not change-related. The CI invocation shape (`CI=true bun test --coverage`) writes lcov fine: full serial run 1,552 files · 16,262 tests · 0 fail · 210.44 s → `bun coverage:ratchet` exit 0, measured lines 94.28% / functions 93.14% vs floor 90% / 90%; `scripts/coverage/floor.json` untouched (0-line diff).

**5.3 Per-source mutation re-measures** — one run per pilot-covered source, six independent tasks.
Shared rules: record each score in this file against its `scripts/mutation/baseline.json` entry with
**no baseline adjustment** — baselines are summary.ts = 1.0 (binding), errors.ts = 0.9876,
tool-status-labels.ts = 0.9764, classify-error.ts = 0.949, plugins/types.ts = 0.57; client.ts has no
entry (first-touch — record to seed a future baseline, no exemption). Paired-run exit gates on
`--threshold` (default 0), so the baseline comparison is recorded evidence, not the tool's exit
status. Budget: each run within the mutation job's existing ceiling.

- [x] 5.3.1 Re-measure `src/errors.ts` (baseline 0.9876) and record the score here. Verify: `bun test:mutate:file src/errors.ts` — recorded score ≥ 0.9876. **Score: 0.9875776397515528** (killed=159 survived=2) — equal to baseline.
- [x] 5.3.2 Re-measure `src/live-status/tool-status-labels.ts` (baseline 0.9764) and record the score here. Verify: `bun test:mutate:file src/live-status/tool-status-labels.ts` — recorded score ≥ 0.9764. **Score: 0.9764150943396226** (killed=207 survived=5) — equal to baseline.
- [x] 5.3.3 Re-measure `src/plugins/types.ts` (baseline 0.57) and record the score here. Verify: `bun test:mutate:file src/plugins/types.ts` — recorded score ≥ 0.57. **Score: 1** (killed=143 survived=0) — above baseline.
- [x] 5.3.4 Re-measure `review-loop/src/summary.ts` (baseline 1.0, binding — any lost kill is a failure) and record the score here. Verify: `bun test:mutate:file review-loop/src/summary.ts` — recorded score = 1.0. **Score: 1** (killed=221 survived=0) — binding bar holds, zero lost kills.
- [x] 5.3.5 Re-measure `plugins/task-provider-youtrack/classify-error.ts` (baseline 0.949) and record the score here. Verify: `bun test:mutate:file plugins/task-provider-youtrack/classify-error.ts` — recorded score ≥ 0.949. **Score: 0.9490909090909091** (killed=261 survived=14) — equal to baseline.
- [x] 5.3.6 Re-measure `plugins/task-provider-youtrack/client.ts` (no baseline entry — first-touch) and record the score here to seed a future baseline. Verify: `bun test:mutate:file plugins/task-provider-youtrack/client.ts` — score recorded here. **Score: 0.5630252100840336** (killed=67 survived=48 noCoverage=4) — first-touch, recorded to seed a future baseline; no baseline adjustment made to any entry.
- [x] 5.4 Run `bun test:mutate:changed` once — expected outcome on this tests-only diff is the green "No changed mutation targets … nothing to measure" message; the mutation evidence is task 5.3, not this gate. Verify: `bun test:mutate:changed` prints that message and exits 0; record the observed output here. — Observed on the final branch shape (master + this change only, commit f3b585450): `No changed mutation targets vs origin/master; nothing to measure.` exit 0. First attempt failed on the pre-rebase branch, which carried two stacked `sdd-runner-verify-convergence` commits (`1e64ba642`, `737e00faa` — preserved on branch `sdd-runner-verify-convergence`); the gate measured those sdd-runner/src files, none of which this change touches. Resolution: this change committed and rebased onto `origin/master` (`ad05f321a`) per the tests-only-diff assumption in design D5. Post-rebase full serial suite: 1,551 files · 16,271 tests · 16,264 pass · 0 fail · 161.87 s, freshness current (fingerprint 8005055cc12e156a) — the small count delta vs the 5.1 capture (16,262/1,552) is master drift (dependabot #351 + baseline ratchet f1fb09d9a), not this change; the six baseline entries this change compares against are byte-identical on master. Audit pilot rows unchanged (6/7/16/16 cases).

## 6. Authoring guidance and wrap-up (design D6 / Migration steps 5–6)

- [x] 6.1 Add a "Grouped assertion tests" subsection to `tests/CLAUDE.md`: the `assertEach` pattern; the eligibility checklist (same fixture, order-independent, no per-case isolation or timing dependence); explicit non-mechanisms (`test.each` is not consolidation); lane exclusions naming `tests/operational/**`, `tests/smoke/**`, `tests/platform/**` (inside default `bun test` discovery yet belonging to excluded lanes), plus the already-excluded stories/e2e/client/visual trees; and the one-test-per-behavior default stated explicitly (today only the de facto convention of the New Test File Pattern example). Verify: `rg -n "Grouped assertion tests" tests/CLAUDE.md` finds the new subsection. — Verified at tests/CLAUDE.md:145; root CLAUDE.md table and README testing section gained a one-line `test:audit` mention.
- [x] 6.2 Record the final after-numbers here: audit after-run totals for the four files, `test:slowest` delta vs the task 3.1 baseline, and the overall case-count reduction; this plus 5.3 is the rollout evidence follow-on changes must cite. Verify: re-run `bun run test:audit` and `bun run test:slowest` — both match the after-numbers recorded here.

  **Final after-numbers (heuristic v2):**

  - Audit after-run (whole scan set: 1,506 files / 15,743 cases / 34,450 matcher calls / 47.4% share — before: 15,928 cases / 48.0%): `tool-status-labels.test.ts` 6 cases / 7 matchers / 0.8333 · `manifest-schema.test.ts` 7 / 0 / 1.0 · `classify-error.test.ts` 16 / 96 / 0.1875 · `summary.test.ts` 16 / 84 / 0.
  - Case-count reduction: four pilots 230 → 45 audit cases (-185, -80%); runner view 226 → 8 cases (-218, matching the 5.1 full-suite delta exactly); whole scan set -185 cases.
  - Wall time (per-file `bun test <file>`, before → after): tool-status-labels 0.66 s → 0.63 s · classify-error 0.63 s → 0.62 s · summary 0.64 s → 0.63 s · manifest-schema 0.64 s → 0.62 s — within noise, as the proposal's micro-benchmark predicted (~0.16 ms/case; the payoff is case-count/readability, not wall time). `test:slowest`: no pilot file in the top 10 before or after; fresh full serial run (1,552 files · 16,262 tests · 0 fail · 159.93 s) current, fingerprint 0281db773ec7142c.
- [x] 6.3 Confirm every new file (`tests/utils/grouped-assertions.ts`, `tests/utils/grouped-assertions.test.ts`, `scripts/test-audit/fragmentation.ts`, `scripts/test-audit/cli.ts`, `tests/scripts/test-audit/fragmentation.test.ts`) carries the SPDX header. Verify: `rg --files-without-match "SPDX-License-Identifier" tests/utils/grouped-assertions.ts tests/utils/grouped-assertions.test.ts scripts/test-audit/fragmentation.ts scripts/test-audit/cli.ts tests/scripts/test-audit/fragmentation.test.ts` prints nothing. — Verified: the command prints nothing (rg exit 1 = no matches = every file carries the header).
- [x] 6.4 Repo gates green after all of the above. Verify: `bun check` exits 0 (or `bun check:full`, reading `reports/checks/` on failure). — `bun check` exit 0; `bun check:full`: 7/8 ✓ (lint, typecheck, format:check, license-headers, knip, test:client, duplicates), the `test` check's single failure is the load-class flake `tests/review-loop/worktree.test.ts` "createWorktree creates a linked worktree on a new branch" (15.1 s timeout under parallel load) — re-run green solo 18/18 in 7.65 s per the shared-host flake policy; the fresh full serial run on the final tree is green and current (16,262 tests · 0 fail · fingerprint 0281db773ec7142c). Not a regression from this change.
