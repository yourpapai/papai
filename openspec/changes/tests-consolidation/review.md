<!-- GENERATED by sdd-runner from findings/resolutions sidecars - regenerated wholesale per round. -->

## Review

### Round 1

**Verdict**: 0b 0m 5n · 4 resolved · 1 dismissed · converged

#### NITPICK — F1

> the Write/Edit TDD pipeline gates only `src/` + `client/` impl files
- **Question**: Should the design state the full gateable root set (src/, client/, plugins/, review-loop/src/, sdd-runner/src/) instead of only src/ + client/, so the parenthetical is accurate for future edits under plugins/ or the workspaces?
- **Code evidence**: Read .hooks/tdd/test-resolver.mjs: the gateable-impl check (comment at line 23) accepts src/, client/, plugins/, review-loop/src/, and sdd-runner/src/, and line 174 notes scripts/ is explicitly NOT a gateable source root. The design's conclusion for this change ('it never fires for this change's edits') still holds — the change edits only tests/**, scripts/test-audit/**, tests/CLAUDE.md, and package.json, none of which are gateable — so only the enumerated list is incomplete, not the derived safety claim.
- **Resolution**: edited — design.md Hook/TDD confirmations now enumerate the full gateable root set — src/, client/, plugins/, review-loop/src/, sdd-runner/src/ (scripts/ explicitly not gateable) — matching .hooks/tdd/test-resolver.mjs:23-30 and :174. The unchanged conclusion ('never fires for this change's edits') still holds: the change touches only tests/**, scripts/test-audit/**, tests/CLAUDE.md, and package.json, none of which are gateable.

#### NITPICK — F2

> `tests/utils/test-helpers.ts`, `tests/setup.ts`, `tests/mock-reset.ts`, `bunfig.toml` are byte-frozen while a story-lane refactor qualification is in flight
- **Question**: Should the design cite the full standing frozen set (adding tests/utils/logger-mock.ts, tests/stories/**, scripts/story/**, and the scripts/coverage enforcement modules) rather than a four-file list framed as contingent on one in-flight qualification?
- **Code evidence**: scripts/story/inputs.ts:11-15 lists the frozen inputs as bunfig.toml, tests/mock-reset.ts, tests/setup.ts, tests/utils/test-helpers.ts (plus tests/utils/logger-mock.ts); tests/CLAUDE.md:167 documents the freeze as the standing qualification model ('Refactor qualification freezes every regular file under tests/stories/**; scripts/story/**; plus bunfig.toml, tests/setup.ts, tests/mock-reset.ts, tests/utils/test-helpers.ts, tests/utils/logger-mock.ts, and the scripts/coverage/ modules...'), established on master and in force for any qualification, not just one currently in flight. The design's operative decision — a NEW file tests/utils/grouped-assertions.ts, invisible to the snapshot — is unaffected either way.
- **Resolution**: edited — design.md Context and D2 now cite the standing frozen set — adding tests/utils/logger-mock.ts, tests/stories/**, scripts/story/**, and the scripts/coverage/ modules — and reframe the freeze as the standing story-refactor qualification model per scripts/story/inputs.ts:10-33 and tests/CLAUDE.md:167, not a single contingent in-flight qualification. The D2 decision (new file tests/utils/grouped-assertions.ts, invisible to the qualification snapshot) is unchanged.

#### NITPICK — F3

> 1,499 files, 15,274 cases, 47.2% of cases wrap at most one assertion
- **Question**: Should the audit implementation pin its exact regex/segmentation variant so the D4 claim that the selection rule is 'reproducible by the audit itself' reproduces the cited before-numbers exactly?
- **Code evidence**: Reimplemented the stated heuristic (test(/it( literal-first-argument case segmentation; expect(, assert.* calls, schemaValidates(, expectAppError( counted per segment) over the bunfig.toml-defined unit lane (tests/**/*.test.ts minus stories/e2e/client/visual): got 1,529 files / 15,353 cases / 46.2% single-or-zero share. Per-file: tool-status-labels 42/43 exact, summary 38/56 exact, manifest-schema 37/43 exact, orchestrator 58 single-assert at 21% exact, auth.test.ts 32 cases exact; classify-error measured 62/77 vs the cited 63/77. The D4 top-4 ranking reproduces exactly (next candidate tests/analytics/normalizer.test.ts at 35/43, far below the cut), and migration step 2 regenerates the definitive before-numbers from the implemented audit itself, so the discrepancy is within the imprecision the design already disclaims and changes no decision.
- **Resolution**: dismissed — Already covered by the design itself: D3 pins heuristicVersion and disclaims absolute-share imprecision, and Migration step 2 records the before-numbers from the implemented audit (internally consistent by construction); the reviewer's own reimplementation reproduces the D4 top-4 ranking exactly (next candidate 35/43, far below the cut), so no decision changes.

#### NITPICK — F4

> are recorded in the change's `tasks.md` as the rollout evidence
- **Question**: Will tasks.md be generated before apply, since the design makes it the evidence ledger for the rollout gate?
- **Code evidence**: ls openspec/changes/tests-consolidation shows only design.md, proposal.md, and specs/ — no tasks.md yet; the sibling change lighter-unit-tests-under-load carries tasks.md, and openspec/config.yaml apply/archive guidance assumes it ('Check off tasks.md checkboxes as each task completes'; 'confirm all tasks.md checkboxes are complete'). The reviewed artifacts only reference tasks.md forward-looking (migration steps 2 and 5), so this is a pipeline-stage consistency note, not a defect in the artifacts as stated.
- **Resolution**: evidence-answered — Yes — tasks.md is a standard change artifact created before apply in this repo's OpenSpec flow: openspec/config.yaml:114-125 apply/archive guidance walks tasks.md checkboxes, the auto-sdd schema shows the pipeline generates tasks.md (openspec/schemas/auto-sdd/schema.yaml:325), and sibling changes (e.g. lighter-unit-tests-under-load) already carry one. The reviewed artifacts reference it only forward-looking (Migration steps 2 and 5), so nothing in them needs changing.

#### NITPICK — F5

> Coverage is verified by one full `bun test:coverage` + `bun coverage:ratchet` run before finishing (pilot files' sources only).
- **Question**: Should the parenthetical '(pilot files' sources only)' be dropped or reworded, since bun's coverage denominator spans every discovered production file and the ratchet gates the aggregate floor — there is no pilot-sources-scoped coverage check?
- **Code evidence**: tests/CLAUDE.md coverage-floor section: 'bun's coverage denominator spans every discovered production file, not just the subset's imports, so a subset run measures far below the floor'; the ratchet (scripts/coverage/ratchet.ts, floor in scripts/coverage/floor.json) gates the unweighted per-file mean aggregate. The command as written (full run) is the only valid shape and is correct; only the parenthetical could be misread as scoping the run to pilot sources, which would produce a spurious floor failure.
- **Resolution**: edited — design.md D5 reworded: the coverage run is explicitly full-suite and cannot be scoped to the pilot's sources (bun's coverage denominator spans every discovered production file and the ratchet gates the aggregate floor — tests/CLAUDE.md:219-221), with the pilot's coverage risk confined to the pilot files' sources. The command itself was already correct; only the ambiguous parenthetical is gone.

### Round 2

**Verdict**: 1b 0m 1n · 2 resolved · 0 dismissed · converged

#### BLOCKER — F1

> matcher calls per case: `expect(`, `node:assert` `assert.*(`, `schemaValidates(`, `expectAppError(` attributed to the enclosing case segment — versus migration step 3: "re-run audit for that file; case count drops, matcher-call count does not" and spec scenario "THEN its case count and single-assertion share decrease while its assertion count does not"
- **Question**: How can a static regex count of textual matcher call-sites stay non-decreasing across consolidation, when D2's grouped form (Row<T> = label + fields, one shared run callback) collapses N per-case expect() call sites into ~1 textual site executing N times? Every compliant pilot rewrite will show the audited assertion count collapsing (e.g. classify-error.test.ts: 96 textual expect/assert sites today → ~2 after an assertEach rewrite), so migration step 3's acceptance check fails, the spec's 'Consolidation is visible in the audit' scenario is unsatisfiable, and the rollout gate's discriminator ('audit measurements for those files demonstrating that assertion counts did not decrease'; 'a decreased audited assertion count' = non-compliant) flags every legitimate consolidation as assertion-losing. The Risks mitigation 'before/after matcher-call counts from the audit surface assertion drops' is likewise non-functional under this heuristic.
- **Code evidence**: Repo-verified, not repo-resolvable — it is a contradiction internal to the artifacts. Verified: (1) each pilot case today carries its own textual expect() site (tests/plugins/task-provider-youtrack/classify-error.test.ts: 77 test sites, 96 expect/assert sites); (2) D2 fixes the grouped shape as rows-of-data plus one shared run callback, whose body contains one expect per matcher kind, not per row; (3) D3 pins the audit to regex call-site counting (mirroring scripts/test/import-graph.ts's regex+DI precedent, verified at scripts/test/import-graph.ts:26-30) with no row-literal counting; (4) the spec's audit scenario and rollout-gate requirement both key non-compliance off the audited assertion count decreasing. No existing repo mechanism (scripts/coverage lcov, scripts/mutation scores) supplies an execution-counted assertion metric that the design wires in. Fix before apply: count grouped rows as assertions (e.g. row-literal entries in the rows array attributed to the enclosing case), or count executed assertions, or re-anchor preservation evidence on git-diff review + the mutation gate and reword the audit scenario and migration step 3 accordingly.
- **Resolution**: edited — Repo-confirmed contradiction, fixed in both artifacts by re-anchoring preservation evidence. Verified: classify-error.test.ts has 77 test( sites and 93 textual expect( sites today; D2's assertEach(rows, run) shape executes N row assertions through one shared callback, so D3's regex call-site count collapses by construction under any compliant rewrite — making design.md migration step 3 ('matcher-call count does not'), the spec's 'Consolidation is visible in the audit' scenario ('assertion count does not [decrease]'), and the rollout gate's 'decreased audited assertion count' discriminator all unsatisfiable as written. Edits: (1) spec Assertion-preservation requirement and its scenarios now key compliance off every merged case's input/expected pair surviving as a row with at least the same matcher strength, explicitly stating the audit's textual matcher-call count decreases by construction and is not the preservation discriminator; (2) spec 'Case count drops while assertions persist' and 'Consolidation is visible in the audit' scenarios reworded accordingly (audit scenario: case count and share decrease, textual count drop is the expected fragmentation signal); (3) spec rollout requirement + 'Assertion-losing rewrite' scenario replaced the audit-count check with a row-by-row accounting + mutation re-measure; (4) design D3 documents the textual count's by-construction drop and its fragmentation-signal-only role; D5, Risks bullet, and migration step 3 reworded to the row-by-row accounting acceptance (case count drops, every former case's input/expected pair accounted for as a surviving row).

#### NITPICK — F2

> The hermetic story lane and its harness, the E2E suites, and the smoke/platform-adapter lanes SHALL NOT have their scenarios consolidated
- **Question**: The enumeration names story/E2E/smoke/platform lanes but omits the client (tests/client/**), visual (tests/visual/**), and operational (tests/operational/) lanes — should the exclusion list name them, since D6's tests/CLAUDE.md guidance ('the lane exclusions') will inherit whatever enumeration the spec carries?
- **Code evidence**: Answered from the repo: bunfig.toml [test] pathIgnorePatterns excludes tests/e2e/**, tests/client/**, tests/visual/**, tests/stories/** from unit discovery, and tests/operational runs under the separate test:operational script (package.json). The requirement's confinement clause ('Grouping SHALL be confined to the in-process unit-test lane') already excludes those lanes, so this is cosmetic — only the non-exhaustive enumeration could mislead the D6 guidance author.
- **Resolution**: edited — Enumeration completed in the spec's Lane exclusions requirement ('the client and visual suites, the operational suite' added) and its scenario retitled 'Non-unit lanes keep one case per scenario' listing all five lane families, so D6's tests/CLAUDE.md guidance inherits an exhaustive list. Repo evidence for the lane names: bunfig.toml:8 pathIgnorePatterns excludes tests/{e2e,client,visual,stories}/**; package.json:90 runs tests/operational under the separate test:operational script.

### Round 3

**Verdict**: 2b 3m 1n · 6 resolved · 0 dismissed · converged

#### BLOCKER — F1

> design.md D5: "Pilot test-file edits change test-content hashes, so `test:mutate:changed` re-measures the covered source files against their existing baselines automatically — the spec's \"re-measured rather than exempted\" is already how the fingerprint cache works." Migration step 4: "then `bun test:mutate:changed` for the pilot-covered sources (no baseline adjustment)." Spec 'Gates are not weakened' scenario: "**WHEN** the changed-file mutation gate runs on a branch whose diff contains consolidated test files — **THEN** those files are re-measured because their fingerprints changed, and must clear their existing baselines without any baseline adjustment." proposal.md: "consolidated suites simply re-measure under its existing fingerprint rules." design.md Risks: "the mutation gate catches lost kills" / "mutation baselines bind".
- **Question**: On this change's tests-only branch diff, does `bun test:mutate:changed` select and re-measure anything at all, and is the spec's re-measure scenario satisfiable as written?
- **Code evidence**: Repo-answered; the claims are mechanically false for a tests-only diff. scripts/mutation/changed-files.ts:125-137 `selectChangedMutationTargets` takes `git diff --name-only --diff-filter=ACMRT base...HEAD` and filters through `deps.isGateableImpl` (= .hooks/tdd/test-resolver.mjs `isGateableImplFile`, true only for impl files under src/, client/, plugins/, review-loop/src/, sdd-runner/src/). This change's diff is tests/** rewrites, scripts/test-audit/**, tests/CLAUDE.md, package.json (design Non-goals: "nothing under `src/`, `client/`, `plugins/` production code") — every path fails the filter, so changed-files.ts:205-211 hits `if (targets.length === 0) { ... log(\"No changed mutation targets vs ${input.baseRef}; nothing to measure.\") ... return null }`: the gate passes while measuring nothing. The pilot-covered sources (verified imports: src/plugins/types.ts, src/live-status/tool-status-labels.ts, plugins/task-provider-youtrack/client.ts, src/errors.ts, review-loop/src/summary.js) are not in the diff, so they are never selected regardless of gateability. The fingerprint premise itself is accurate — scripts/mutation/score-fingerprint.ts:43,91 hashes candidate test-file content, so cached scores would be invalidated — but cache invalidation only applies to targets that get selected, which on a tests-only diff is none. The spec scenario is therefore unsatisfiable for this change and for every follow-on pure-consolidation change (same tests-only shape), and the design's mutation-backstop claims (D5, Risks bullets 1–2, spec Assertion-preservation 'and the mutation gate') are vacuous as written. The tool that can actually re-measure chosen sources regardless of diff is package.json:82 `"test:mutate:file": "bun scripts/mutation/paired-run.ts"` (tests/CLAUDE.md mutation section: 'use `bun test:mutate:file <path>` for focused work'), whose paired run re-measures because the test-content hash changed. Fix direction: D5/migration step 4 switch to `bun test:mutate:file` runs for each pilot-covered source (no baseline adjustment), and the spec scenario/rollout requirement reword to an obligation ('the consolidated files' covered sources SHALL be re-measured — e.g. via paired per-file runs — and clear existing baselines') rather than an automatic property of the changed-file gate.
- **Resolution**: edited — Repo-verified and fixed. Verified scripts/mutation/changed-files.ts:124-136 (selectChangedMutationTargets = git diff --name-only --diff-filter=ACMRT base...HEAD filtered through isGateableImplFile from .hooks/tdd/test-resolver.mjs:22-34, true only for impl files under src/, client/, plugins/, review-loop/src/, sdd-runner/src/), :206-212 (targets empty -> 'No changed mutation targets ... nothing to measure', returns null), :275-277 (null -> exit 0), :162-168 (positional args rejected as 'unexpected positional argument', so migration step 4's 'bun test:mutate:changed for the pilot-covered sources' was doubly unrunnable), and .github/workflows/ci.yml:303-306 (PR gate runs exactly test:mutate:changed --base=..., nothing else). This change's tests-only diff selects no target, so D5's 're-measures automatically' claim and the spec's re-measure scenario were vacuous. Edits: design.md harness-constraints bullet now states cache invalidation only applies to selected targets and a tests-only diff selects nothing; D5 retitled ('mutation evidence is an explicit per-source re-measure') and rewritten around one 'bun test:mutate:file <source>' per pilot-covered source — src/errors.ts, src/live-status/tool-status-labels.ts, src/plugins/types.ts, review-loop/src/summary.ts, plugins/task-provider-youtrack/client.ts (the verified imports of the four pilot files; baseline.json entries exist for the first four, client.ts is first-touch with no entry) — recording each score against its scripts/mutation/baseline.json entry in tasks.md, noting paired-run's exit gates on --threshold (default 0, verified scripts/mutation/paired-run.ts:287-291) not baselines; migration step 4 now runs the per-file re-measures and treats the changed-file gate's 'No changed mutation targets' as the expected outcome; spec 'Gates are not weakened' requirement and its scenario (retitled 'Covered sources are re-measured and still bind') reworded to an explicit re-measure obligation; spec Assertion-preservation and Rollout requirements plus proposal.md Modified Capabilities line re-pointed from 'the mutation gate' to the explicit per-source re-measure; design Risks bullets 1-2 updated accordingly.

#### MATERIAL — F2

> design.md D3 counting heuristic: "cases: `test(`/`it(` call sites with a literal first argument;" combined with the imprecision disclaimer "residual imprecision (computed assertions in loops, exotic matchers) shifts absolute shares slightly but never the *comparison*" — `test.each` call sites match neither pattern, so row-generator cases are invisible to the audit.
- **Question**: Can the fragmentation audit — the spec's rollout-gate evidence instrument — see `test.each`-generated runner cases at all, and can it evidence the case-count reduction of a row-generator → grouped rewrite?
- **Code evidence**: Repo-answered; the blind spot is real and the 'never the comparison' claim fails for one rewrite class. 60 unit-lane `*.test.ts` files under tests/ contain `test.each` (rg count); `test.each(` does not match a `test(`/`it(` call-site regex, so those files' row-generated runner cases contribute zero to the audit's case counts. The spec explicitly defines converting away from row generators as in-scope consolidation ("Row generators are not consolidation": a rewrite is non-compliant precisely when 'the runner reports each row as its own passing or failing case' — so removing that shape reduces runner case count and is the largest available reduction for such files), yet the audit shows 1 case before and 1 case after such a rewrite: zero measured reduction. The rollout requirement's 'audit measurements for those files showing the achieved case-count reduction' is then unsatisfiable (or certifies nothing) for that whole class, and D4's deterministic selection rule ('>= 20 cases') can never surface a test.each-heavy file however fragmented its rows. Recommend before apply: count `test.each` call sites (ideally their literal rows) in the heuristic under a bumped heuristicVersion, or explicitly scope the capability/rollout gate to exclude row-generator conversions so the instrument's boundary matches the governed behavior.
- **Resolution**: edited — Repo-verified blind spot, fixed by extending the counting heuristic. Verified 60 unit-lane *.test.ts files under tests/ contain test.each (rg -l count), that 'test.each(' matches no 'test('/'it(' literal-first-arg call-site pattern, that the runner counts each row as a case (design's own verified fact), and that pilot file tests/plugins/manifest-schema.test.ts itself contains test.each call sites (lines 649/653/660) — so a row-generator -> grouped rewrite (the largest available case-count reduction for such files, and in-scope per the spec's 'Row generators are not consolidation' scenario) showed zero (or negative) measured reduction in the audit, making the rollout requirement's 'audit measurements showing the achieved case-count reduction' unsatisfiable for that rewrite class, and D4's >=20-cases rule blind to each-heavy files. Edits: design.md D3 case heuristic now counts test.each(/it.each( row generators at their literal row count when the first argument is a static array literal (1 when computed); the imprecision paragraph marks this as the v1->v2 heuristic change (Context/proposal figures are v1, not comparable, recorded via heuristicVersion); D4 notes the ranking was derived under v1 and row-heavy files surfaced by v2 are follow-on candidates (only manifest-schema.test.ts of the four contains test.each, so v2 can only grow its counted cases); spec audit requirement now states the measured test-case count includes runner cases generated by test.each/it.each row generators.

### Round 4

**Verdict**: 0b 1m 1n · 1 resolved · 1 dismissed · converged

#### MATERIAL — F1

> one `bun test:mutate:file <source>` per source over `src/errors.ts`, `src/live-status/tool-status-labels.ts`, `src/plugins/types.ts`, `review-loop/src/summary.ts`, and `plugins/task-provider-youtrack/client.ts` (the imports of the four pilot files)
- **Question**: D5 defines the pilot's mutation re-measure set as "the imports of the four pilot files" and enumerates five sources — is that enumeration complete? The spec's Gates requirement ("every source a consolidated test file covers SHALL be re-measured rather than exempted") and its 'Covered sources are re-measured and still bind' scenario ("every source that test file covers is re-measured") bind whatever the imports actually are, so an incomplete list makes the design's execution plan violate the spec it cites, and migration step 4 ("one `bun test:mutate:file <source>` per pilot-covered source") inherits the same under-scope.
- **Code evidence**: Repo-verified incomplete: the set is six sources, not five. tests/plugins/task-provider-youtrack/classify-error.test.ts:9-12 carries a multi-line value import — `import { classifyYouTrackError, YouTrackClassifiedError, } from '../../../plugins/task-provider-youtrack/classify-error.js'` — the primary unit under test of that pilot file, alongside client.js (line 13) and src/errors.js (line 14); a single-line `^import .* from` verification misses it, which is evidently how the prior round's 'verified imports' list (review.md:70-71) went wrong — new evidence, not a re-raise. The other three pilot files' production imports check out exactly: src/live-status/tool-status-labels.js (tool-status-labels.test.ts:8, sole import), review-loop/src/summary.js (summary.test.ts:10, the only value import — its other imports are `import type`), src/plugins/types.js (manifest-schema.test.ts:9). The omitted plugins/task-provider-youtrack/classify-error.ts is a gateable impl root (plugins/, per .hooks/tdd/test-resolver.mjs isGateableImplFile) and HAS an existing baseline entry — scripts/mutation/baseline.json:136 `"plugins/task-provider-youtrack/classify-error.ts": 0.9490909090909091`, the second-highest bar of the six (verified entries: src/errors.ts baseline.json:412, src/live-status/tool-status-labels.ts :420, src/plugins/types.ts :456, review-loop/src/summary.ts :230; plugins/task-provider-youtrack/client.ts absent as D5 states). Following D5 as written would skip re-measuring the pilot's most-covered source against its 0.949 floor; the paired-run tool itself accepts it (package.json:82 test:mutate:file -> scripts/mutation/paired-run.ts, positional sourceFiles). Fix: add the sixth source to D5's list (its baseline entry exists, so the 'client.ts is the only first-touch' note stays true).
- **Resolution**: edited — Verified against the repo: the pilot re-measure set is six sources, not five. tests/plugins/task-provider-youtrack/classify-error.test.ts:9-12 value-imports { classifyYouTrackError, YouTrackClassifiedError } from '../../../plugins/task-provider-youtrack/classify-error.js' (the file's primary unit under test, hidden from a single-line import scan by the multi-line form); baseline.json:136 carries "plugins/task-provider-youtrack/classify-error.ts": 0.9490909090909091. Re-scanning all four pilot files for multi-line and dynamic imports confirms no seventh source (summary.test.ts and manifest-schema.test.ts extras are import-type only; node:assert is stdlib), and client.ts remains absent from the baseline as D5 stated. Edited design.md D5 to enumerate plugins/task-provider-youtrack/classify-error.ts, note the multi-line import that hid it, and record its 0.949 entry as the highest existing bar of the six; the 'client.ts has no entry: first-touch' note stays true. Migration step 4 says 'per pilot-covered source' generically, so it inherits the corrected set without further edit.

#### NITPICK — F2

> its test-case count (including the runner cases generated by `test.each`/`it.each` row generators), its assertion count, and the share of cases that wrap at most one assertion
- **Question**: The Fragmentation-audit requirement names the persisted field "assertion count" (requirement text and the 'Per-file report is persisted' scenario: "the case count, assertion count, and single-assertion share"), but design D3 defines the same field as a textual "matcher-call count" that "drops by construction across a compliant rewrite", and the spec's own 'Consolidation is visible in the audit' scenario calls it "its textual matcher-call count" — should the requirement use one name so no reader of the requirement alone treats the persisted number as an assertion-count-preservation measure the rest of the spec explicitly disclaims?
- **Code evidence**: Repo-answered as a naming inconsistency only, no decision changes. spec.md:101/106 say "assertion count"; design.md D3 says "matcher calls per case … This is a *textual* call-site count … It is the fragmentation signal, never the preservation discriminator"; the spec's audit scenario already says "textual matcher-call count also decreases by construction". No repo artifact provides an executed-assertion metric (scripts/coverage measures lcov, scripts/mutation measures scores — verified earlier rounds), so the implementable field is the textual call-site count and the requirement's "assertion count" is the only outlier name. Design.md already pins the implementer to the correct semantics; renaming in the requirement (e.g. "matcher-call (textual assertion-site) count") is cosmetic alignment.
- **Resolution**: dismissed — The quoted gap's substance is answered verbatim elsewhere in the same spec: the 'Consolidation is visible in the audit' scenario pins the field as "its textual matcher-call count also decreases by construction — one shared callback executes per row — which is the expected fragmentation signal, not evidence of lost assertions", and the Assertion-preservation requirement repeats "not by the fragmentation audit's textual matcher-call count" — the finding itself concedes "no decision changes" and "cosmetic alignment".

### Round 5

**Verdict**: 0b 2m 1n · 3 resolved · 0 dismissed · converged

#### MATERIAL — F1

> `classify-error.ts` clears the highest existing bar of the six at 0.949
- **Question**: D5 asserts classify-error.ts's 0.949 is the highest existing baseline bar among the six re-measured sources, but the baseline entries for the other sources are higher — should the design instead identify review-loop/src/summary.ts (baseline 1, a perfect score) as the binding bar, followed by src/errors.ts (0.9876) and src/live-status/tool-status-labels.ts (0.9764), so risk attention lands on the summary.test.ts rewrite where any single lost kill fails the recorded comparison?
- **Code evidence**: Read scripts/mutation/baseline.json entries for all six D5 sources: review-loop/src/summary.ts = 1 (line 230), src/errors.ts = 0.9875776397515528 (line 412), src/live-status/tool-status-labels.ts = 0.9764150943396226 (line 420), plugins/task-provider-youtrack/classify-error.ts = 0.9490909090909091 (line 136), src/plugins/types.ts = 0.572463768115942 (line 456), plugins/task-provider-youtrack/client.ts absent. Sorted, 0.949 is the second-lowest of the five existing entries, not the highest; the claim is false against the repo. The per-source procedure (record each score against its own entry) is complete and runnable — verified scripts/mutation/paired-run-cli.ts accepts positional sourceFiles and gates exit only on --threshold (default 0), and paired-run.ts never consults baseline.json for its exit — so no step breaks, but the sentence inverts which pilot file faces the tightest acceptance bar and should be corrected before apply. Note: the round-4 resolved finding's own summary carried this error ('record its 0.949 entry as the highest existing bar of the six') and the edit implemented it verbatim; this is new evidence correcting it.
- **Resolution**: edited — design.md D5 no longer claims classify-error.ts holds the highest bar; the parenthetical now identifies review-loop/src/summary.ts (baseline 1, perfect score — any lost kill in the summary.test.ts rewrite fails the recorded comparison) as the binding bar, followed by src/errors.ts (0.9876) and src/live-status/tool-status-labels.ts (0.9764), with classify-error.ts (0.949) and src/plugins/types.ts (0.57) listed below it.

#### MATERIAL — F2

> The scan set is the unit lane minus `tests/operational/**`
- **Question**: The scan-set subtraction fixed only the operational lane's discovered file, but tests/smoke/** and tests/platform/** also contain *.test.ts files inside default bun test discovery (bunfig ignores only stories/e2e/client/visual) that belong to the smoke/platform-adapter lanes the spec's Lane-exclusions requirement places off-limits — should the scan set subtract tests/smoke/** and tests/platform/** as well, so the audit never presents those lane files as unit-lane consolidation material?
- **Code evidence**: Verified bunfig.toml:8 pathIgnorePatterns excludes only tests/{e2e,client,visual,stories}/**, so default discovery includes the smoke/platform trees. Enumerated the discovered *.test.ts files there: tests/smoke/catalog-crosscheck.test.ts, tests/smoke/scenarios/catalog.test.ts, six tests/smoke/harness/*.test.ts (container, docker, fake-llm-server, fake-mattermost-server, image, story-markers), tests/platform/catalog-crosscheck.test.ts, three tests/platform/harness/*.test.ts (fake-discord-client, fake-kontur-talk-server, fake-telegram-bot). Read tests/smoke/catalog-crosscheck.test.ts:14-19 — it imports SMOKE_STORIES/SMOKE_STORY_IDS and harness lane-census, i.e. per-scenario contractual structure exactly like the operational file D3 carves out. The spec's Lane-exclusions requirement ('the smoke/platform-adapter lanes SHALL NOT have their scenarios consolidated; their per-scenario structure is contractual (manifest scenario identity, catalog claims, per-scenario isolation)') forbids consolidating them regardless, and the four pilot files are unaffected, so this is the round-3 F4 defect class with new files as evidence — same instrument-boundary rationale ('the audit must never present that file as unit-lane consolidation material'), incomplete subtraction. The Context's '.smoke.ts'/'.platform.ts' suffixes phrase is accurate (tests/smoke/scenarios/container-p.smoke.ts, tests/platform/scenarios/*.platform.ts exist and are non-discovered).
- **Resolution**: edited — design.md D3's scan set is now the unit lane minus tests/operational/**, tests/smoke/**, and tests/platform/**, naming the discovered *.test.ts files in those trees (both catalog-crosscheck files plus the smoke scenario/harness and platform harness tests) as inside default bun test discovery yet belonging to lanes the spec's Lane exclusions requirement bars; D6's guidance sentence now names all three lane trees for the same reason. Pilot set and all other steps unaffected.

#### NITPICK — F3

> [`proposal.md`](../proposal.md)
- **Question**: design.md links proposal.md one directory too high — both files are siblings in openspec/changes/tests-consolidation/, so should the link target be `proposal.md` (the spec link in the same sentence already uses the correct same-dir relative form)?
- **Code evidence**: Verified openspec/changes/tests-consolidation/ contains design.md, proposal.md, specs/ as siblings, and openspec/changes/proposal.md does not exist, so ../proposal.md from design.md resolves to a nonexistent path. Sibling designs reference the same-dir file (e.g., openspec/changes/diagnostics-tool-domain/design.md:3 'See `proposal.md` for motivation', openspec/changes/ledger-dimension-tiers/design.md:12); the adjacent link in this design.md, specs/test-case-consolidation/spec.md, is correctly same-dir relative. Cosmetic navigation-only defect; no content depends on it.
- **Resolution**: edited — design.md line 6 link changed from ../proposal.md to proposal.md.

### Round 6

**Verdict**: 0b 1m 1n · 2 resolved · 0 dismissed · converged

#### MATERIAL — F1

> each re-measured score clears its existing baseline entry without any baseline adjustment
- **Question**: How does a consolidation change satisfy the binding re-measure acceptance for a covered source that has no baseline.json entry (first-touch) — plugins/task-provider-youtrack/client.ts is one of this change's own six pilot-covered sources — record-to-seed, vacuous pass, or exemption? The spec scenario (and the requirement tail 'record each score against the existing baselines', plus proposal.md's 're-measures its covered sources (per-file paired runs) against the existing baselines') is unsatisfiable as written for that source and for any future first-touch source a consolidated test file covers.
- **Code evidence**: Answered from the repo before asking: scripts/mutation/baseline.json carries entries for exactly five of the six D5-enumerated sources — plugins/task-provider-youtrack/classify-error.ts = 0.9490909090909091 (line 136), review-loop/src/summary.ts = 1 (line 230), src/errors.ts = 0.9875776397515528 (line 412), src/live-status/tool-status-labels.ts = 0.9764150943396226 (line 420), src/plugins/types.ts = 0.572463768115942 (line 456) — and no entry for plugins/task-provider-youtrack/client.ts (rg over the file returns no match). Re-verified the six-source set by import scan of the four pilot files: classify-error.test.ts:9-14 value-imports classify-error.js (multi-line), client.js, and errors.js; summary.test.ts:10 summary.js; tool-status-labels.test.ts:8; manifest-schema.test.ts:9; all remaining imports are `import type` or node:assert/strict (stdlib). So the design (D5: 'client.ts has no entry: first-touch, its score is recorded for the seed job') already made the product judgment; the defect is purely that the binding spec requirement/scenario and the proposal's Modified Capabilities line still key acceptance off an entry that verifiably does not exist for one of this change's own re-measure obligations — a consistency gap the design resolved but the binding artifacts lag. Suggested wording: 'clearing its existing baseline entry where one exists; sources with no entry (first-touch) have their score recorded to seed a future baseline, without exemption from the re-measure'.
- **Resolution**: edited — Verified against the repo: scripts/mutation/baseline.json carries entries for exactly five of the six D5 sources (classify-error.ts :136 = 0.9490909090909091, review-loop/src/summary.ts :230 = 1, src/errors.ts :412 = 0.9875776397515528, src/live-status/tool-status-labels.ts :420 = 0.9764150943396226, src/plugins/types.ts :456 = 0.572463768115942) and none for plugins/task-provider-youtrack/client.ts, so the binding acceptance wording was unsatisfiable for one of this change's own re-measure obligations. Edited specs/test-case-consolidation/spec.md in all three places that keyed acceptance off an entry: the Rollout requirement (line 120), the Gates requirement tail (line 134), and the 'Covered sources are re-measured and still bind' scenario (line 144) — each now reads 'clearing its existing baseline entry where one exists; a first-touch source with no entry has its score recorded to seed a future baseline, without exemption from the re-measure'. Also edited proposal.md's Modified Capabilities line (line 24) to carry the same first-touch clause so the binding artifacts agree with design.md D5's existing resolution ('client.ts has no entry: first-touch, its score is recorded for the seed job'), which needed no edit. The chosen semantics are record-to-seed, not vacuous pass or exemption — matching D5.

#### NITPICK — F2

> THEN E2E, client, visual, operational, and platform-adapter suites keep one runner case per scenario
- **Question**: Should the 'Non-unit lanes keep one case per scenario' scenario enumerate the smoke lane? Its parent requirement was updated (round 5) to bind 'the smoke/platform-adapter lanes', but the scenario's THEN list was not extended to include smoke, leaving the requirement and its own scenario enumerating different lane sets.
- **Code evidence**: Answered from the repo before asking: verified on-disk spec.md:87 — the Lane exclusions requirement binds 'the smoke/platform-adapter lanes' — while the scenario at spec.md:97 lists only 'E2E, client, visual, operational, and platform-adapter'; the story lane has its own dedicated scenario (spec.md:89-92), so smoke is the sole lane named in the requirement but missing from the scenario. Lane reality confirmed: find tests/smoke tests/platform -name '*.test.ts' | wc -l = 12, all inside default discovery (bunfig.toml:8 pathIgnorePatterns excludes only tests/{e2e,client,visual,stories}/**), including tests/smoke/catalog-crosscheck.test.ts which design.md D3 names. The requirement text still binds smoke regardless, so no decision changes — this is enumeration completeness in the verification surface only, the same defect class as the round-2 scenario-enumeration fix, re-introduced by the round-5 requirement edit.
- **Resolution**: edited — Verified on-disk: spec.md:87's Lane-exclusions requirement binds 'the smoke/platform-adapter lanes' while the scenario's THEN listed only 'E2E, client, visual, operational, and platform-adapter', so the requirement and its own verification surface enumerated different lane sets. Edited spec.md:97 to add 'smoke' to the THEN list, restoring enumeration parity with the requirement text. Not dismissed: the finding carries a verbatim gap quote that is not answered elsewhere, so the nitpick-eligible dismissal criteria do not apply, and the one-word fix removes a real (if low-stakes) verifier blind spot — the requirement still binds smoke regardless, so no decision changes.

### Round 7

**Verdict**: 0b 1m 1n · 2 resolved · 0 dismissed · converged

#### MATERIAL — F1

> The unit-test lane (default `bun test` discovery: `tests/**` minus `tests/{stories,e2e,client,visual}/**` per `bunfig.toml`, plus the non-discovered `.smoke.ts`/`.platform.ts` suffixes which belong to other lanes) reproduces the proposal's fragmentation measure: **1,499 files, 15,274 cases, 47.2% of cases wrap at most one assertion** (v1 regex scan, see D3; proposal's wider count includes other lanes).
- **Question**: The Context attributes the headline evidence tuple (1,499 files / 15,274 cases / 47.2%) to the unit-test lane as it parenthetically defines it — default discovery minus the four bunfig-excluded trees. That universe contains 1,529 *.test.ts files on this tree (1,512 with at least one test(/it( site), so the stated evidence is not reproducible from the stated definition: the numbers reconcile exactly only with D3's audit scan set, i.e. the lane minus tests/operational/**, tests/smoke/**, tests/platform/**, counting files with >=1 case — which yields precisely 1,499. Which universe produced the numbers, and should the Context attribute them to the D3 scan set (which post-dates the Context, having been subtracted in a later review round) rather than to the lane as defined? No gate keys off these numbers (the rollout gate cites the audit run itself), but the design's only quantitative motivation anchor fails a reproduction attempt as written, and D3's 'applied identically before and after' comparability story leans on the audit's before-run matching this Context figure.
- **Code evidence**: Measured on this worktree (branch base beb21c5; `git rev-list --count beb21c5..origin/master` = 0, so no tree drift — the mismatch is not staleness): tests/** has 1,882 *.test.ts files; minus the four bunfig trees (rg --files with -g '!tests/{stories,e2e,client,visual}/**') = 1,529, of which 1,512 have >=1 test(/it( site (rg -l); further minus tests/operational (1 file), tests/smoke (8), tests/platform (4) = 1,516 raw, and exactly 1,499 files with >=1 test(/it( site — an exact match to the Context's figure, ruling out the lane-as-defined (1,512) or a zero-case filter alone. Corroborating the case count: raw test(/it( sites over the minus-7 set = 15,321 vs the claimed 15,274, the 47 delta being consistent with v1's literal-first-argument filter. bunfig.toml:8 confirms only the four trees are excluded from discovery, and D3 itself defines the audit scan set as the lane minus the three additional trees. The one-sentence fix is to attribute the tuple to D3's scan set (files with at least one counted case), not to the lane as the Context parenthetical defines it.
- **Resolution**: edited — design.md Context re-attributed: the 1,499 files / 15,274 cases / 47.2% tuple is now stated as measured over D3's audit scan set (the lane minus tests/{operational,smoke,platform}/**, counting case-bearing files only), with the lane-as-defined count (1,512 case-bearing *.test.ts files) stated so the figure is reproducible as written. Independently re-measured on this tree: tests/** has 1,882 *.test.ts files; minus the four bunfig.toml-excluded trees = 1,529, of which 1,512 carry at least one test(/it( site; further minus tests/operational (1 file), tests/smoke (8), tests/platform (4) — all 13 case-bearing — = exactly 1,499 case-bearing files, an exact match that rules out the lane-as-defined universe (1,512). Raw test(/it( sites over the scan set measure ~15.3k, consistent with the claimed 15,274 after v1's literal-first-argument filter. The audit's before-run (run over the same scan set, D3/Migration step 2) therefore matches the Context figure by construction, restoring the comparability story.

#### NITPICK — F2

> `tests/CLAUDE.md` gains the grouped-assertion authoring pattern alongside the existing one-test-per-behavior guidance.
- **Question**: Where is the 'existing one-test-per-behavior guidance' that the D6 edit is supposed to sit alongside? tests/CLAUDE.md contains no such guidance today, so the premise is false as stated; design.md D6's parallel phrasing ('One-test-per-behavior remains the default') similarly implies an established documented default that is only a de facto convention (the New Test File Pattern example). Either the wording should say the D6 subsection establishes/states the default alongside the existing authoring-pattern sections, or D6's subsection text should itself carry the one-test-per-behavior default explicitly. No decision changes — the edit target and content are otherwise well-specified.
- **Code evidence**: Read tests/CLAUDE.md in full (248 lines): its sections are Running the suite, Parallel Execution & Isolation, Test Helpers, Mocking Rules, Mock Reset Model, Important Reality Check, New Test File Pattern, Legacy Module-Mock Pattern, Schema and Tool Tests, E2E Testing, Coverage floor, Mutation testing — none states a one-test-per-behavior rule; the closest content is the single-test 'New Test File Pattern' example (tests/CLAUDE.md:115-143). Repo-wide grep of every CLAUDE.md/AGENTS.md for 'one test|per.behavior|per-behavior|single behavior' finds only mutation-improve/CLAUDE.md:13 'one test per mutant class' — unrelated. The root AGENTS.md's tests/CLAUDE.md description ('helpers, mocks, mock reset, E2E guidance') confirms no behavior-granularity guidance is documented there either.
- **Resolution**: edited — False premise removed from both artifacts. proposal.md now reads: tests/CLAUDE.md gains the grouped-assertion authoring pattern, stating the one-test-per-behavior default explicitly (today it is only the de facto convention of the New Test File Pattern, not documented guidance). design.md D6 changed from 'One-test-per-behavior remains the default' to 'The subsection states one-test-per-behavior as the default — it is currently only the de facto convention of the New Test File Pattern example, not documented guidance'. Verified: tests/CLAUDE.md (248 lines) contains no one-test-per-behavior rule; the closest content is the single-test New Test File Pattern example (tests/CLAUDE.md:115-143), so the subsection itself now carries the default explicitly, per the finding's second suggested fix.

### Round 8

**Verdict**: 0b 0m 2n · 2 resolved · 0 dismissed · converged

#### NITPICK — F1

> Consolidation pilot on a bounded set of top fragmented pure-function suites (e.g. the four files cited above), preserving **every** assertion.
- **Question**: Which four files does the proposal mean by "the four files cited above", given its own Why section names only two?
- **Code evidence**: Read proposal.md's Why section in full: it cites exactly two files — tests/live-status/tool-status-labels.test.ts (42/43) and tests/plugins/task-provider-youtrack/classify-error.test.ts (63/77) — no other test file is named anywhere in the proposal. The actual four are pinned by design.md D4's selection table (adding tests/review-loop/summary.test.ts 38/56 and tests/plugins/manifest-schema.test.ts 37/43); all four verified present on disk under tests/. Internal count mismatch in an "e.g." clause of the summary doc; the binding pilot set is D4's, so no decision changes — a one-word fix ("two" or cite all four) resolves it.
- **Resolution**: edited — proposal.md now reads "e.g. the two files cited above plus the next two by the same selection rule; design D4 pins all four", removing the internal count mismatch; the binding pilot set remains design D4's table.

#### NITPICK — F2

> Code: pilot test files under `tests/`; `tests/CLAUDE.md`; one audit script under `scripts/`.
- **Question**: Does the proposal's Impact enumeration cover the change's actual file surface as the design defines it?
- **Code evidence**: Verified against repo and design: package.json (read in full) currently has no test:audit script, so the change does edit package.json — a manifest edit whose repo-wide mutation-cache invalidation consequence design.md D5 already budgets (that was the round-3 MATERIAL finding, resolved there). Design D2 also adds tests/utils/grouped-assertions.ts plus its test — a new shared utility, not a "pilot test file" — and D3 creates two modules (scripts/test-audit/fragmentation.ts and cli.ts, verified absent today), not "one audit script". tests/scripts/ exists with *.test.ts precedent, so the test placement is fine. Enumeration completeness only in the summary doc; every substantive item and consequence is stated precisely in design.md, so no decision changes.
- **Resolution**: edited — proposal.md Impact now enumerates the full surface from design D2/D3/D5: tests/utils/grouped-assertions.ts + its test, scripts/test-audit/fragmentation.ts + cli.ts, and the package.json test:audit script edit with its budgeted mutation-cache invalidation.
