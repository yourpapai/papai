<!-- GENERATED by afk-runner from findings/resolutions sidecars - regenerated wholesale per round. -->

## Review

### Round 1

**Verdict**: 0b 2m 3n · 5 resolved · 0 dismissed · open

#### MATERIAL — F1

> **`knip.config.ts`**: drop `'afk-runner'` from `ignoreWorkspaces` (R5 swept knip ignore; a stale entry references a workspace that no longer resolves).
- **Question**: D1.7 instructs only the ignoreWorkspaces entry edit, but knip.config.ts carries a second live afk-runner reference in the comment directly above it (knip.config.ts:23-24: "The review-loop, mutation-improve, opencode-agent and afk-runner workspaces are standalone developer tools with their own check suites (<workspace>:lint/typecheck/format:check/test) run separately in check:full."). This is present-tense normative text describing afk-runner as a current papai workspace — exactly D4's update class — yet no inventory item covers it, and the grep gate excludes knip.config.ts, so nothing forces it. Should D1.7 be amended to also drop "and afk-runner" from that comment so the file ends the change with zero afk-runner references?
- **Code evidence**: Repo-answerable; checked directly. Read knip.config.ts: ignoreWorkspaces (line 27) matches the census, and the comment at lines 23-26 names afk-runner as a present workspace. Ran the design's grep gate file list: it covers package.json, .gitignore, scripts/mutation/baseline.json only — knip.config.ts is outside the arbiter. Checked the R5 precedent the design itself cites: `git show --stat 41fa25b6a` shows `knip.config.ts | 4 +-` (2 lines changed, not 1), i.e. R5's knip sweep edited the comment together with the entry — the design's own precedent updates both. Also swept the whole repo with `rg -l afk --no-ignore --hidden`: aside from the enumerated surfaces, false positives ("Kafka" in docs/research), ignored files (.opencode/package-lock.json hash substring, .afk-runner/config.json), and kept-class provenance comments (review-loop/src + tests/review-loop "(afk-runner-agent-mcp D3)" citations), the knip comment is the only unenumerated present-tense survivor.
- **Resolution**: edited — D1.7 now instructs dropping 'afk-runner' from both ignoreWorkspaces and the standalone-workspaces comment above it (knip.config.ts:23-26), citing R5 commit 41fa25b6a whose knip sweep edited comment and entry together; the Context census line was extended to name the comment. Verified against knip.config.ts and `git show 41fa25b6a -- knip.config.ts`.

#### MATERIAL — F2

> the four afk describe/test groups from the resolver's own suite
- **Question**: D1.6 enumerates only the afk *tests* in `.hooks/tests/tdd/test-resolver.test.ts`; the file also carries a five-line comment block (lines 104-108) that is half present-tense normative — "review-loop/src/ and afk-runner/src/ ARE gated on this branch: the TDD write hooks on afk-runner/src/** are a declared never-relaxed gate (docs/architecture/afk-runner.md), and review-loop/src/ rides the same predicate." — and half merge-history ("master's narrow-mutation-gate-scope narrowed to product code in a history that never had afk-runner; the merge keeps the superset."). The artifacts give contradictory dispositions for it: the Non-goals keep-list ("resolver-test merge-history comments) — these mention past states and stay") reads as keeping the whole block, while D4's rule ("A mention is **updated** when it describes the current repo") requires updating the present-tense half. Kept verbatim, the hook's own test suite retains prose asserting a phantom gate and a doc citation of the removed page — the exact "code asserting a root that no longer exists" wrongness D1.6's rejected alternative names. Should D1.6 name this comment block's disposition (drop the afk present-tense sentences, keep the merge-history sentence)?
- **Code evidence**: Repo-answerable; checked directly. Read .hooks/tests/tdd/test-resolver.test.ts:85-151 — the afk tests are three cases at lines 117-126 inside the isGateableImplFile describe, immediately preceded by the mixed comment block at 104-108; afk tests also sit in the suggestTestPath (178-186), findTestFile (296-302), and resolveImplPath (375-383) describes, confirming the "four describe blocks" census. `rg -l afk .hooks --no-ignore` confirms the resolver and its test are the only .hooks files involved. Cross-checked the two governing artifact rules: design Non-goals "No cleanup of historical prose (CHANGELOG, the README's PR-#431 anecdote, `review-loop` comments describing the extracted consumer, `opencode-agent/src/pricing.ts`, resolver-test merge-history comments) — these mention past states and stay" vs D4 "A mention is **updated** when it describes the current repo" — the block at 104-108 satisfies both predicates on different lines, and no task line resolves the tie.
- **Resolution**: edited — D1.6 now names the split disposition for the .hooks/tests/tdd/test-resolver.test.ts:104-108 comment block: the afk present-tense sentences (the 'ARE gated' claim and the docs/architecture/afk-runner.md citation) are D4 update-class and go, the block reworded to name review-loop/src/ alone; the master-merge-history sentences stay per the Non-goals keep-list. Resolves the Non-goals-vs-D4 contradiction with an explicit task-line rule.

#### NITPICK — F3

> `.claude/commands/sdd-auto.md` and `.opencode/commands/sdd-auto.md` (identical today)
- **Question**: The claimed identity is factually wrong — is any plan step depending on it (e.g. copy-one-to-the-other) worth stating, or should the evidence sentence just be corrected?
- **Code evidence**: Repo-answerable; ran `diff .claude/commands/sdd-auto.md .opencode/commands/sdd-auto.md`: they differ at line 11 — the `.claude` copy adds "or the `--execute` flag (arm the run to execute its approved tasks.md through implement/verify/release)" to the depth-flag sentence; `.opencode` lacks it. No operational impact: D3 replaces both files' content with the same stub regardless, so this is an evidence inaccuracy, not a plan defect.
- **Resolution**: edited — D3's '(identical today)' claim corrected: the design now records the one-sentence --execute-flag difference between the two command files (verified by diff; the .claude copy adds the flag clause) and states that no plan step depends on the identity since the stub replaces both contents. No plan step added — D3 already writes both stubs independently.

#### NITPICK — F4

> the main checkout's store with six terminal runs
- **Question**: D6's location evidence appears misstated: the main checkout (`/Users/ki/Projects/yourpapai/papai`, currently on branch openspec/degraded-turn-recovery) has no `.afk-runner/` directory, and the umbrella notes describe the six terminal runs as "all worktree-pinned". Should D6 name the actual papai-side `.afk-runner/` locations (worktrees afk-runner-extract/-retire/-saas/-u13, agent-mcp-live-target, plus `~/.afk-runner/projects`) so the operator cleanup note targets the right paths?
- **Code evidence**: Repo-answerable; checked directly. `ls /Users/ki/Projects/yourpapai/papai/.afk-runner` → No such file or directory; same for `.worktrees/master`. `ls -d .worktrees/*/.afk-runner` → five worktree dirs plus this one; `ls ~/.afk-runner` → `projects`. The run count is grounded: umbrella notes.md:76-77 records "2026-09-10 — drain gate (task 4.1) confirmed: papai store roster all terminal (6 runs, none running, all worktree-pinned — exempt by D4)". D6's action (cleanup note after terminal state; never `git add -A`) is phrased generically over "papai-side `.afk-runner/` dirs", so the mislocation changes no behavior — evidence hygiene only.
- **Resolution**: edited — D6's location evidence corrected: the six terminal runs' stores live under .worktrees/*/.afk-runner/ (every run worktree-pinned per notes.md:76-77) and the main checkout has no .afk-runner/ — verified by ls. Volatile worktree names deliberately not enumerated (they go stale); the cleanup note keeps its stable 'papai-side .afk-runner/ dirs' target.

#### NITPICK — F5

> the `sdd-pipeline.md` header note and its two "see afk-runner.md" links
- **Question**: sdd-pipeline.md contains three `[afk-runner.md](afk-runner.md)` links, not two: lines 13 (header note) and 37 (Execution-half section) are the two D4 re-points, but line 259 — "the live verbs are `afk-runner`'s (`start`/`resume`/`status`/`stop`/`report`/`runs`), see [`afk-runner.md`](afk-runner.md)" — sits inside the explicitly-historical R4 blockquote and is kept per D4, resolving through the pointer file. Should the enumeration say "two non-historical links" so an implementer running the 4.3 widened grep (`rg -l afk --no-ignore --hidden`) doesn't trip over the unclassified third hit?
- **Code evidence**: Repo-answerable; checked directly. `rg -n 'afk-runner\.md\]\(' docs/architecture/sdd-pipeline.md` → links at lines 13, 37, 259. Read the surrounding text: lines 12-16 are the header blockquote (updated per D4, incl. "afk-runner/` implements the same pipeline"), line 259 is inside the "> Historical: the `sdd-runner` CLI surface below ... retired with the R4 cut-over" blockquote — squarely D4's kept class, and D2's pointer file keeps it resolving, so there is no dangling-link risk; the finding is enumeration precision only.
- **Resolution**: edited — D4 now says 'two non-historical links' and explicitly classifies the third link (inside the R4 'Historical' blockquote) as kept-class resolving through the pointer file — an expected hit in the 4.3 widened grep. The three links at sdd-pipeline.md lines 13/37/259 were verified; only enumeration precision changed, no dangling-link risk ever existed.

### Round 2

**Verdict**: 0b 1m 2n · 3 resolved · 0 dismissed · open

#### MATERIAL — F1

> the `sdd-pipeline.md` header note and its two non-historical "see afk-runner.md" links (a third link sits inside the R4 "Historical" blockquote near the file's end — kept class, resolves through the pointer file, and is an expected hit in the 4.3 widened grep)
- **Question**: Does D4's update-class enumeration for sdd-pipeline.md cover every non-historical afk mention in the file, so the 4.3 completion grep (rg minus known-historical classes) has a disposition for every hit?
- **Code evidence**: Repo-answered — it does not. `rg -n 'afk' docs/architecture/sdd-pipeline.md` returns exactly: L12-16 (header blockquote — covered), L37 (non-historical link — covered), L162 ("The review loop itself keeps reporting `cap-hit`; in the afk runner the reclassification is fold-derived (`reviewOutcomeOf` reads the open set; `runReviewWork` routes)."), L259 and L274 ("> Historical:" blockquotes — kept per F5). L162 is present-tense normative body prose in the "Severity-based convergence" section — neither the header note, nor a link, nor inside a Historical blockquote — and it is named by neither D4's enumeration nor D1.8's "sdd-pipeline.md header/links" tombstone line. It is also not in any keep class D4 lists (CHANGELOG, README zod anecdote, review-loop, opencode-agent, resolver-test merge-history, openspec/), so the design's own completion check flags it with no recorded disposition — the exact gap class r1-F5 closed for the third link, but update-class rather than kept-class, so following the enumerated inventory verbatim leaves a live present-tense afk-runner reference in a retained architecture doc. Fix: add L162's disposition to D4 (update to re-point at the new repo / reword to "the runner", or record the keep rationale).
- **Resolution**: edited — Verified sdd-pipeline.md:162 ('in the afk runner the reclassification is fold-derived') is present-tense body prose in 'Severity-based convergence', covered by no D4 update class and no keep class. design.md edited: D4's update enumeration now names the L162 sentence with its disposition (reworded to name the external runner), and D1.8's tombstone line now reads 'sdd-pipeline.md header/links/body-prose sentence (D4)'. Every afk hit in the file now has a recorded disposition for the 4.3 widened grep: L12-16 header + L37 link + L162 body = update-class; L259/L274 Historical blockquotes = kept-class.

#### NITPICK — F2

> Docs: `docs/architecture/afk-runner.md` + `afk-runner-mcp-research.md` removed; `AGENTS.md`/`CLAUDE.md` docs index and `docs/architecture/{commands,sdd-pipeline}.md` references updated.
- **Question**: Is the proposal's Impact line consistent with design D2, under which neither doc path is removed — both are replaced by one-line pointer files at the same paths?
- **Code evidence**: Repo-answered — inconsistent with the design, harmless to the plan. Design D2 keeps one-line pointer files at both paths ("so does `afk-runner-mcp-research.md`") precisely because retained openspec history links them; verified `rg -l 'docs/architecture/afk-runner\.md' openspec | wc -l` = 95 and `rg -l 'afk-runner-mcp-research\.md' openspec | wc -l` = 13, satisfying the design's "ten-plus"/"five-plus" rationale. The proposal's What Changes "(both `afk-runner*.md` docs go, per D4)" plus this Impact "removed" line read as the paths vanishing, which would dangle those 95/13 retained links and contradict the design Goal "no retained openspec link dangles". The design is the operative doc and explicitly records the resolution, so NITPICK: reword the proposal to "replaced by one-line pointer files".
- **Resolution**: edited — Verified 95 retained openspec files link docs/architecture/afk-runner.md and 13 link afk-runner-mcp-research.md, so 'removed' contradicted design D2's pointer files. proposal.md edited in both places: the What Changes parenthetical now reads 'both afk-runner*.md docs become one-line pointer files, per design D2 — retained openspec links keep resolving', and the Impact Docs line now reads 'replaced by one-line pointer files at the same paths (design D2)'.

#### NITPICK — F3

> `scripts/mutation/baseline.json` afk entries + its README mention
- **Question**: Does the proposal's singular "README mention" match the number of normative afk mentions in scripts/mutation/README.md that must be edited?
- **Code evidence**: Repo-answered — under-specified. `rg -n 'afk' scripts/mutation/README.md` finds exactly three mentions: L45 (`afk-runner/src/event-schemas.ts` inside the PR-#431 zod anecdote — keep class, matches D1.5's "line-45" pin), L139 (mapping-table row `afk-runner/src/foo.ts -> tests/afk-runner/foo.test.ts` — normative, goes), and L223 (gateable-roots sentence "`review-loop/src/`, `afk-runner/src/` or `opencode-agent/src/`" — normative, goes). Two normative mentions must be edited, not one; the grep gate does not cover README, so nothing else catches a half-done edit except the 4.3 widened grep. Design D1.5 disposes both precisely, so this is proposal-precision only — NITPICK.
- **Resolution**: edited — Verified scripts/mutation/README.md has exactly three afk mentions: L139 (mapping-table row) and L223 (gateable-roots sentence) normative, L45 (PR-#431 anecdote) historical. proposal.md edited: 'its README mention' now reads 'its two normative README mentions (mapping-table row, gateable-roots sentence)', matching design D1.5.

### Round 3

**Verdict**: 0b 0m 1n · 1 resolved · 0 dismissed · converged

#### NITPICK — F1

> docs-index rows in `AGENTS.md` + `CLAUDE.md` (kept byte-identical to each other — they share content today)
- **Question**: Should D1.8 record that AGENTS.md is a symlink to CLAUDE.md — one file reachable by two paths, not two files held in sync by discipline — so the executor knows a single edit to CLAUDE.md updates every tombstone surface (docs-index rows, /sdd:auto routing row, Testing-Notes roots list) and that the 4.3 widened rg (which does not follow symlinks) will list only CLAUDE.md?
- **Code evidence**: ls -la AGENTS.md CLAUDE.md shows 'AGENTS.md -> CLAUDE.md' (9-byte symlink); both names resolve to one inode, so the rows cannot diverge and 'kept byte-identical to each other' misdescribes the mechanism as manual sync. Verified no plan step depends on two-file identity: editing either path edits the same file, the grep gate (package.json/.gitignore/baseline.json) does not name either path, and rg -l with default no-follow lists CLAUDE.md only while still proving both paths cleared (they share content). The stated outcome is achievable and unambiguous either way, so the imprecision cannot break an action — cosmetic only; the same one-edit fact applies to the proposal's 'AGENTS.md/CLAUDE.md docs index' Impact line, which stays true as written.
- **Resolution**: evidence-answered — Verified in this worktree: AGENTS.md is a 9-byte symlink to CLAUDE.md (ls -la: 'AGENTS.md -> CLAUDE.md'), so D1.8's 'AGENTS.md + CLAUDE.md' is one file reachable by two paths and the tombstone edit is a single edit to CLAUDE.md covering all three surfaces (docs-index rows, /sdd:auto routing row, Testing-Notes roots list). 'Kept byte-identical to each other — they share content today' (design.md:48) is mechanism-imprecise but literally true under the symlink and action-invariant: no plan step depends on two-file identity, the grep gate names neither path (package.json/.gitignore/baseline.json), and the 4.3 widened rg listing CLAUDE.md alone still proves both paths cleared since they share content. Recording the symlink mechanism in D1.8 is therefore unnecessary; no artifact change.
