<!--
SPDX-License-Identifier: BUSL-1.1
Copyright (c) 2026 Dmitriy Lazarev
Use of this software is governed by the Business Source License 1.1.
See LICENSE in the project root for details.
-->

Sections 1–4 are umbrella task 4.2 (one commit-shaped deletion); section 5 is umbrella task 4.3. Design D5 ordering is binding: the pin-sync (1.1) precedes the tree deletion (2.1) — reversed, `UNROUTED_GATEABLE_ROOTS` certifies a root the predicate no longer gates, which is red. Nothing this change writes is a gateable product file (`.hooks/` sources, tests, docs, JSON/YAML config), so the Write/Edit TDD hook pipeline gates none of these edits; the discipline is the 1.1 mirror-set pin-sync — its intermediate red stays inside the task — plus each task's Verify command. The tree is green after every task.

## 1. Resolver mirror-set pin-sync (design D1.6, D5 — the change's red→green cycle)

- [x] 1.1 Remove every afk-runner branch from the TDD resolver mirror set as ONE edit unit — the check.test.ts pin derives gateable roots from the live predicate and fails red on any half-edit, so branches, resolver tests, and pin entry land together: `.hooks/tdd/test-resolver.mjs` (roots-list comment :23, `isAfkRunner` const :30 and its use, `suggestTestPath` arm :64–69, `findTestFile` arm :128–135, `resolveImplPath` arm :233–236, and the three `afk-runner/src/foo.ts → tests/afk-runner/foo.test.ts` comments at :64/:128/:233); `.hooks/tests/tdd/test-resolver.test.ts` (afk cases in all four describes — :117–126 `isGateableImplFile`, :178–186 `suggestTestPath`, :296–302 `findTestFile`, :375–383 `resolveImplPath` — plus the :104–108 comment block: drop the afk present-tense sentences, the "ARE gated" claim and the `docs/architecture/afk-runner.md` citation, reword the block to name `review-loop/src/` alone, keep the master-merge-history sentences per the Non-goals keep-list); `tests/scripts/check.test.ts` (`'afk-runner/src/'` out of `UNROUTED_GATEABLE_ROOTS` :729, and the :723–728 comment reworded to `plugins/` alone). The other five gateable roots and their arms are untouched — reference removal, not re-scoping. Verify: `bun test .hooks/tests/tdd/test-resolver.test.ts tests/scripts/check.test.ts` green; `rg -n "afk" .hooks/tdd/test-resolver.mjs .hooks/tests/tdd/test-resolver.test.ts tests/scripts/check.test.ts` empty.

## 2. Deletion core (umbrella 4.2; design D1.1–D1.3, D6)

- [x] 2.1 `git rm -r afk-runner tests/afk-runner` (the no-upward-imports guard test goes with its tree); in root `package.json` drop the `"afk-runner"` workspace entry (:11) and all five `afk-runner:*` scripts (:46–50: `start`/`test`/`typecheck`/`lint`/`format:check`); run `bun install` to regenerate `bun.lock` with the workspace gone — lockfile churn beyond the afk workspace is a stop-and-inspect signal, never something to sweep in; in `.gitignore` drop the `# afk-runner state` comment and `.afk-runner/` line (:72–73). The ignore removal exposes untracked `.afk-runner/` state dirs — operator debris per D6, never commit material: stage explicit paths only, never `git add -A`. Verify: `grep -rn "afk-runner" package.json .gitignore` empty; `bun install` exits 0; `bun test tests/scripts .hooks/tests` green; `bun run typecheck && bun run lint` green; `bun run test:affected` green.

## 3. Gate-config sweep (design D1.4, D1.5, D1.7)

- [ ] 3.1 Mutation baseline purge: drop all 119 `afk-runner/src/**` keys from `scripts/mutation/baseline.json` (dead keys are dead weight; the monotonic floor never consults missing files). Verify: `grep -rn "afk-runner" package.json .gitignore scripts/mutation/baseline.json` empty — the full design grep gate; `bun run test:affected` green.
- [ ] 3.2 Mutation README mentions: in `scripts/mutation/README.md` remove the two normative mentions — the mapping-table row (:139, `afk-runner/src/foo.ts -> tests/afk-runner/foo.test.ts`) and the gateable-roots sentence (:223, drop `afk-runner/src/` from the list) — while the :45 PR-#431 zod anecdote stays (historical keep-class). Verify: `rg -n "afk" scripts/mutation/README.md` returns only the :45 anecdote.
- [ ] 3.3 Knip sweep: in `knip.config.ts` drop `'afk-runner'` from `ignoreWorkspaces` (:27) AND reword the standalone-workspaces comment above it (:23–26) so the file ends with zero afk references — comment and entry together, exactly as R5's `41fa25b6a` knip sweep did; the grep gate does not cover this file, only the inventory forces it. Verify: `rg -n "afk" knip.config.ts` empty; `bun run knip` green.

## 4. Tombstones (design D1.8, D2, D3, D4)

- [ ] 4.1 Pointer docs: replace the bodies of `docs/architecture/afk-runner.md` and `docs/architecture/afk-runner-mcp-research.md` with one-line pointers — keep the SPDX header, body becomes a single blockquote line naming `yourpapai/afk-runner` and the same-named doc there (D2's shape: `> Moved to yourpapai/afk-runner — see docs/architecture/<name>.md there`). The paths must remain files: 95 and 13 retained openspec files link them, and the pointer keeps every link resolving. Verify: both paths exist, contain the SPDX header and the pointer line; `rg -l "Moved to" docs/architecture/afk-runner*.md` lists both.
- [ ] 4.2 Command stubs: reduce `.claude/commands/sdd-auto.md` and `.opencode/commands/sdd-auto.md` to a short tombstone — the SDD pipeline moved to `yourpapai/afk-runner`; clone-and-run there; no runner in papai. The two files differ today only in the `.claude` `--execute` clause; the stub replaces both contents, so write each independently. Verify: `rg -n "afk-runner:start|config.json|budget" .claude/commands/sdd-auto.md .opencode/commands/sdd-auto.md` empty and both name `yourpapai/afk-runner`.
- [ ] 4.3 CLAUDE.md rows and lists — `AGENTS.md` is a symlink to `CLAUDE.md`, so one edit covers both paths: collapse the `afk-runner` (:26) and `afk-runner MCP research` (:27) docs-index rows into a single tombstone row pointing at `yourpapai/afk-runner`; update the SDD-pipeline row's "the `sdd-runner/` workspace was deleted at R5 — `afk-runner/` implements the pipeline" clause (:25) to name the external repo; reword the Pi-Workflow `/sdd:auto` routing row (:136) — the pipeline moved to the new repo, the command no longer exists in papai; drop `afk-runner/src/` from the Testing-Notes mutation-roots prose (:79). Verify: `rg -n "afk" CLAUDE.md` — every hit names `yourpapai/afk-runner` or the pointer files; none describes a papai tree as present.
- [ ] 4.4 `docs/architecture/sdd-pipeline.md` — every update-class hit in the file (design D4): the header blockquote (:10–16) gains the R5 retirement shape — `afk-runner/` was also deleted from papai, the live engine lives in `yourpapai/afk-runner`, rollback is `git revert` of the deletion commit — re-pointing the :13 `afk-runner.md` link at the new repo; re-point the second non-historical link (:37); reword the :162 body sentence ("in the afk runner the reclassification is fold-derived") to name the external runner. The two "Historical" blockquotes (:259, :274) stay verbatim — kept-class, resolving through the pointer file, expected hits in the 5.2 census. Verify: `rg -n "afk" docs/architecture/sdd-pipeline.md` — every remaining hit is new-repo-pointing or inside those two blockquotes.
- [ ] 4.5 `docs/architecture/commands.md`: drop `afk-runner/src/` from the gateable-roots list in the TDD-hooks Scope paragraph (:81); the rest of the paragraph (mapping note, `client/` mirror) is untouched. Verify: `rg -n "afk" docs/architecture/commands.md` empty.
- [ ] 4.6 `tests/CLAUDE.md` — `tests/AGENTS.md` is a symlink, one edit covers both: drop `afk-runner/src/` from the mutation-floors roots sentence (:299, "Mutation testing" section). Verify: `rg -n "afk" tests/CLAUDE.md` empty.

## 5. Verification, widened census, cleanup note (umbrella 4.3; design D4–D6)

- [ ] 5.1 Full gate pass: `bun run test` (full suite), `bun run lint`, `bun run typecheck`, `bun run check:full` (knip/duplicates/format legs), and the design grep gate `grep -rn "afk-runner" package.json .gitignore scripts/mutation/baseline.json` — empty. Verify: every command exits 0; failures are read from the persisted `reports/` artifacts, not re-run blind.
- [ ] 5.2 Widened census with dispositions (design D4 completion check): run `rg -l afk --no-ignore --hidden` over the repo; every hit must fall in a recorded class — removed by tasks 1–4, or keep-class: `openspec/**` (inherited history), `CHANGELOG.md` and the `scripts/mutation/README.md:45` anecdote, `docs/research/**` historical research, `review-loop/**` + `tests/review-loop/**` provenance comments ("afk-runner-agent-mcp D3"), `opencode-agent/**` (pricing.ts comment + docs), the resolver-test merge-history sentences, `.afk-runner/**` state, lock/hash substrings (`.opencode/package-lock.json`), `node_modules`, `.git`. A hit outside these classes is a survivor: fix it if present-tense normative papai text, else record the keep rationale in the change's notes.md. Verify: census recorded; zero unclassed hits.
- [ ] 5.3 D6 cleanup note: append to `openspec/changes/afk-runner-extract-c-retire/notes.md` (create if absent) the operator note — papai-side `.afk-runner/` dirs (worktree-pinned run stores under `.worktrees/*/.afk-runner/`; the main checkout has none) are post-run debris: archive or delete only after this run reaches a terminal state (resumes read the store; this run is worktree-pinned); commits come from explicit paths, never `git add -A`; `git revert` of the deletion commit restores the ignore line and never touches the untracked dirs. Verify: the note exists in notes.md and carries the wait-for-terminal-state condition.
