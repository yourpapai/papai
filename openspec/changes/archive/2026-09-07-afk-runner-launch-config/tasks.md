<!-- SPDX-License-Identifier: BUSL-1.1 -->
<!-- Copyright (c) 2026 Dmitriy Lazarev. Use of this software is governed by the Business Source License 1.1. See LICENSE in the project root for details. -->

# Tasks

Order is the design's test-first order: red tests (groups 1–2) land before the hook-gated impl edits (groups 3–4), then docs + pin, then ignore coverage, then whole-suite verification. The deferred Open Question (launch-provenance recording) is out of scope by design — no task bakes it in.

Atomicity pass: old 2.2 split into precedence (2.2) and autonomy-reach (2.3); old 3.1 de-bundled so the constant extraction is behavior-preserving and the default flip lives only in 3.2; old 4.1+4.2 merged (the assembler signature change cannot compile without the `cliMain` resolution call); old 5.1+5.2 merged (byte-identical command twins, one mirror edit verified by one diff). Every task ends with its verification command.

## 1. Config ladder — red tests (`tests/afk-runner/config.test.ts`)

- [x] 1.1 Flip the defaulted-`workDir` expectation from `.sdd-runner` to `.afk-runner` in the "defaults workDir and budget when omitted" case (the explicit-`.sdd-runner`-input case resolves input→output and stays as-is). Verify: `bun run test tests/afk-runner/config.test.ts` — exactly this case is red.
- [x] 1.2 Add the `resolveRunnerConfig` happy-path rung matrix against a temp `repoRoot` + injected env record: present file at `<repoRoot>/.afk-runner/config.json` is wholesale-authoritative (its five keys govern — `repoRoot`/`workDir` resolution and mkdir included, env never consulted per-key); absent file → `{ repoRoot, workDir: resolve(root, .afk-runner), model: env['AFK_RUNNER_MODEL'], budget: 5 }`, no `deadline`; absent file + unset entry → compiled defaults for everything. One fixture, one matrix — the rungs are one ladder, not three tasks. Verify: `bun run test tests/afk-runner/config.test.ts` — the new rung cases are red (`resolveRunnerConfig` not yet exported).
- [x] 1.3 Add loud-failure cases to the rung matrix: present-but-invalid file (removed key `budgetUsd`, unknown key, wrong shape, bad JSON) throws out of the loader with its named-key/pointer contract — only file **absence** falls through, never an error. Verify: `bun run test tests/afk-runner/config.test.ts` — the failure rungs are red.

## 2. CLI resolution — red tests (`tests/afk-runner/cli.test.ts`)

- [x] 2.1 Add resolution tests: every verb — `start`, `resume`, `status`, `stop`, `report`, `runs`, `analyze` — launches through one resolution; file at the work dir governs the launch; absent file keeps env+defaults behavior byte-for-byte (today's shape, `budget: 5`, no deadline). Verify: `bun run test tests/afk-runner/cli.test.ts` — the per-verb resolution cases are red.
- [x] 2.2 (split from old 2.2 — precedence half) Add source-precedence tests: file `model` beats `AFK_RUNNER_MODEL` beats the compiled default. Verify: `bun run test tests/afk-runner/cli.test.ts` — the precedence cases are red.
- [x] 2.3 (split from old 2.2 — reach half) Add front-door reach tests: file `budget: null` launches unmetered from the front door; numeric `budget` and `deadline` reach the run's autonomy derivation. Verify: `bun run test tests/afk-runner/cli.test.ts` — the reach cases are red.
- [x] 2.4 Add rejection tests: an invalid config file fails the verb before any run work, naming the offending key/value, with no silent fallback — including for `status`/`report`/`runs`/`analyze`, which previously could not fail pre-work. Verify: `bun run test tests/afk-runner/cli.test.ts` — the rejection cases are red.
- [x] 2.5 Update `agent-seam.test.ts`'s direct `defaultCliDeps('/tmp')` caller for the takes-resolved-config signature. Verify: `bun run test tests/afk-runner/agent-seam.test.ts` — red against the current signature until task 4.1 lands.

## 3. Config implementation — green (`afk-runner/src/config.ts`, D1–D5)

- [x] 3.1 (de-bundled — extraction only, zero behavior change) Extract `DEFAULT_MODEL = 'opencode'`, `DEFAULT_BUDGET_USD = 5`, `DEFAULT_WORK_DIR = '.sdd-runner'` — **today's values**; the `.afk-runner` flip is 3.2's alone — as exported constants, wired into `FiveKeySchema`'s `.default(...)` calls and the `AUTONOMY_DEFAULTS` budget literal (retiring the duplicate `5`): one constant per default, no orphan literals. The absent-file fallback inherits the constants when 3.3 builds it. Verify: `bun run test tests/afk-runner/config.test.ts` (all green — extraction is behavior-preserving) && `bun run afk-runner:typecheck`.
- [x] 3.2 Flip `DEFAULT_WORK_DIR` `.sdd-runner` → `.afk-runner`; `.sdd-runner` disappears from the schema (it survives only as a valid explicit input). Verify: `bun run test tests/afk-runner/config.test.ts` — the 1.1 case is green, the explicit-input case still green.
- [x] 3.3 Implement exported async `resolveRunnerConfig(repoRoot, env)`: candidate is always `<repoRoot>/<DEFAULT_WORK_DIR>/config.json` (file-declared `workDir` never relocates the lookup); present → the resolution **is** `loadRunnerConfig(candidate)`; absent → the today-shape built from `DEFAULT_*` + `env['AFK_RUNNER_MODEL']`, no `deadline`, no mkdir beyond what the run itself does. Verify: `bun run test tests/afk-runner/config.test.ts` — the 1.2/1.3 rung matrix is green.

## 4. CLI wiring — green (`afk-runner/src/cli.ts`, D7) — merged: the two edits cannot compile or verify apart

- [x] 4.1 (old 4.1+4.2) In one edit: turn `defaultCliDeps` into a pure sync assembler taking the resolved `RunnerConfig` (spawn seam, `execGit`, driver cwd from `config.repoRoot`, gate-wait wiring unchanged; no env read inside), and in `cliMain` await `resolveRunnerConfig(process.cwd(), process.env)` once before dispatch for the seven config-consuming verbs; `runCli` (bare run-dir fold) stays untouched. Verify: `bun run test tests/afk-runner/cli.test.ts tests/afk-runner/agent-seam.test.ts` — the group 1–2 reds are all green.

## 5. Command docs + pin (D9)

- [x] 5.1 (old 5.1+5.2 merged — byte-identical twins, one mirror edit) Add the launch-configuration section to both `.claude/commands/sdd-auto.md` and `.opencode/commands/sdd-auto.md`: file path + five-key shape, `AFK_RUNNER_MODEL` entry, compiled defaults, file-over-environment-over-defaults precedence, `budget: null` ⇒ unmetered, optional `metered` override (explicit value beats the derivation), `deadline` in minutes arming the resume waiter, untracked-per-checkout note — prose names **no** flag form. Verify: `diff .claude/commands/sdd-auto.md .opencode/commands/sdd-auto.md` (empty) && `bun run test tests/afk-runner/cli.test.ts` (pin green, inventory unchanged).
- [x] 5.2 Extend the doc-flag pin in `tests/afk-runner/cli.test.ts` to read both twins and keep the exact `['--depth']` inventory assertion over each (extractor reach unchanged). Verify: `bun run test tests/afk-runner/cli.test.ts` — the pin is green over both twins.
- [x] 5.3 Update `docs/architecture/afk-runner.md` CLI/config rows: the resolution ladder, `config.json` at `.afk-runner/`, and the standing-default alignment (`docs/architecture/commands.md` deliberately untouched). Verify: `grep -n 'config.json' docs/architecture/afk-runner.md` shows the updated rows.

## 6. Ignore coverage (D5)

- [x] 6.1 Add (verified via the hand-commit blob / the verify boundary) `.afk-runner/` to `.gitignore` beside the retained `.sdd-runner/` entry. Verify: `git check-ignore .afk-runner/config.json` (exit 0) && `grep -n 'sdd-runner/' .gitignore`.

## 7. Verification

- [x] 7.1 Whole (verified via the hand-commit blob / the verify boundary) afk-runner suite green after the impl greens. Verify: `bun run test tests/afk-runner`.
- [x] 7.2 Full (verified via the hand-commit blob / the verify boundary) suite + afk-runner typecheck + full checks pass. Verify: `bun run test && bun run afk-runner:typecheck && bun check:full`.
- [x] 7.3 Mutation (verified via the hand-commit blob / the verify boundary) gate holds for the two edited impl files against `scripts/mutation/baseline.json` (config.ts 0.729/86, cli.ts 0.497/164): kill regressions get stronger tests before any baseline move; dilution-only scores (kills held) print WARN and pass. Verify: `bun run test:mutate:file afk-runner/src/config.ts && bun run test:mutate:file afk-runner/src/cli.ts`.
- [x] 8.1 (Hand re-target of 1.1+3.2 (landed via the operator surgical completion after 1.1 burned its attempts — red-first pairs merged per the F-P2 lesson.) Flip the defaulted-workDir expectation in the "defaults workDir and budget when omitted" case of tests/afk-runner/config.test.ts from .sdd-runner to .afk-runner AND flip DEFAULT_WORK_DIR .sdd-runner to .afk-runner in afk-runner/src/config.ts (the schema default; .sdd-runner survives only as a valid explicit value) in ONE item — red observed on the test flip, green after the constant flip. Verify: bun test tests/afk-runner/config.test.ts green
- [x] 8.2 (Hand re-target batch: the red-first section-1 (landed via the operator surgical completion tests merged with their section-3 implementations per the F-P2 lesson.) In ONE item: add the resolveRunnerConfig happy-path rung matrix (temp repoRoot + injected env record; present file at the default workDir path; env-only fallback; compiled defaults) and the loud-failure cases (present-but-invalid file, unknown key, wrong shape, bad JSON — each naming the offending key/value) to tests/afk-runner/config.test.ts, AND extract DEFAULT_MODEL/DEFAULT_BUDGET/DEFAULT_WORK_DIR plus implement the exported async resolveRunnerConfig(repoRoot, env) resolution ladder in afk-runner/src/config.ts per design D1-D5 — red observed on the test additions, green after the implementation. Verify: bun test tests/afk-runner/config.test.ts green

- [x] 8.3 (Hand re-target batch: the red-first section-2 (landed via the operator surgical completion tests merged with the section-4 wiring.) In ONE item: add the CLI resolution tests (every verb launches through one resolved-config seam; source precedence — file model beats AFK_RUNNER_MODEL beats default, file budget/deadline ride through; file budget null launches unmetered; invalid config fails the verb before any run work naming the key) plus the defaultCliDeps signature change in tests/afk-runner/cli.test.ts and agent-seam.test.ts, AND turn defaultCliDeps into a pure sync assembler taking the resolved RunnerConfig through the seam in afk-runner/src/cli.ts — red observed, then green. Verify: bun test tests/afk-runner/cli.test.ts tests/afk-runner/agent-seam.test.ts green
- [x] 8.4 (Hand re-target: 5.1+5.2 merged (landed via the operator surgical completion — the doc edit and its pin extension are one unit; the pin extracts every backticked flag token from the twins, so editing the prose without extending the pin is structurally red.) Add the launch-configuration section to both sdd-auto.md twins AND extend the doc-flag pin in tests/afk-runner/cli.test.ts to read both twins and accept the exact documented flag/config surface in ONE item. Verify: bun test tests/afk-runner/cli.test.ts green
- [x] 8.5 Fix the serial-mode cwd leak in the CLI resolution tests: restore the original process cwd in afterEach (import.meta.dir leaked tests/afk-runner as cwd into every later serial file — 34 env-shaped failures across scripts/, opencode-agent/, hooks/). The fix is applied in the tree; this item walks it to the checked box. Verify: bun run test -- --serial green
