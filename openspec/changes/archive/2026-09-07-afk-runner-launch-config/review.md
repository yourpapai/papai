<!-- GENERATED by afk-runner from findings/resolutions sidecars - regenerated wholesale per round. -->

## Review

### Round 1

**Verdict**: 0b 4m 2n · 6 resolved · 0 dismissed · open

#### MATERIAL — F1

> All verbs — `start`, `resume`, `status`, `stop`, `report`, `runs` — SHALL resolve through the same ladder, so a run's configuration is one resolution, not a per-verb shape.
- **Question**: The spec delta enumerates six verbs while design D7 says 'the seven verbs that consume `CliDeps`' — is `analyze` meant to resolve through the ladder or not?
- **Code evidence**: afk-runner/src/cli.ts:261-274 dispatches defaultCliDeps() for exactly seven verbs (start/status/resume/report/stop/runs/analyze); runAnalyzeCommand consumes deps.config.workDir (cli.ts:192) and deps.config.repoRoot (cli.ts:198); the main spec's verb table (openspec/specs/afk-runner-cli/spec.md:11-16) includes `analyze`. Repo answer: analyze consumes the resolved config, so the delta's 'All verbs' enumeration is incomplete and contradicts the design's seven-verb statement and the proposal's 'Every verb'.
- **Resolution**: edited — specs/afk-runner-cli/spec.md's 'All verbs' enumeration now includes `analyze`, matching the design's seven-verb statement and the proposal's 'Every verb'. Evidence: afk-runner/src/cli.ts:261-274 dispatches defaultCliDeps() for exactly seven verbs (start/status/resume/report/stop/runs/analyze); runAnalyzeCommand consumes deps.config.workDir (cli.ts:192) and deps.config.repoRoot (cli.ts:198); the main spec's verb table (openspec/specs/afk-runner-cli/spec.md:11-16) includes analyze.

#### MATERIAL — F2

> The mutation ratchet stays uninvolved (afk-runner workspace keeps its suites, no per-file floor).
- **Question**: Does the mutation ratchet really hold no floor for afk-runner, given both files this change edits carry baselined records?
- **Code evidence**: scripts/mutation/baseline.json carries 76 afk-runner/src entries including afk-runner/src/config.ts {score 0.7288, killed 86, scored 118} and afk-runner/src/cli.ts {score 0.4970, killed 164, scored 330}; docs/architecture/commands.md:81 states the mutation gate shares isGateableImplFile whose scope includes `afk-runner/src/`. Repo answer: afk-runner is floored — the design's no-floor claim (repeated in Context as 'the mutation ratchet keeps its no-floor stance for the afk-runner workspace') is false, and the plan's verification story omits a gate that will measure both edited files.
- **Resolution**: edited — design.md corrected in both places: the Context bullet now states the mutation ratchet floors afk-runner/src (76 baselined records, config.ts and cli.ts among them), and the Test-first order's closing line now states both edited files are measured against their baselines (config.ts 0.729/86 killed, cli.ts 0.497/164 killed) with the WARN-vs-fail semantics. Evidence: scripts/mutation/baseline.json carries afk-runner/src/config.ts {score 0.7288, killed 86, scored 118} and afk-runner/src/cli.ts {score 0.4970, killed 164, scored 330} among 76 afk-runner entries; docs/architecture/commands.md:81 puts afk-runner/src/ inside the mutation gate's isGateableImplFile scope.

#### MATERIAL — F3

> Docs: `docs/architecture/afk-runner.md` (CLI/config rows), `docs/architecture/commands.md`.
- **Question**: Which artifact owns landing the two architecture-doc updates the proposal declares — the design's landing order ('3. Docs + pin: the two command docs (D9), then the pin extension.' / '4. `.gitignore` entry (D5).') and Migration Plan never mention them?
- **Code evidence**: docs/architecture/afk-runner.md:25 documents the CLI verb row with no launch-config resolution and docs/architecture/commands.md documents the runner hook/mutation scope; neither mentions the ladder, so both need the declared edits. The design's Test-first order and Migration Plan enumerate code, command docs, pin, and .gitignore but never afk-runner.md or commands.md — an implementer following the design lands the change without the proposal's declared doc scope.
- **Resolution**: edited — design.md's Test-first order step 3 now includes the two architecture docs the proposal's Impact declares (docs/architecture/afk-runner.md CLI/config rows, docs/architecture/commands.md) beside the command docs and pin extension. Evidence: docs/architecture/afk-runner.md:25 documents the CLI verb row with no launch-config resolution, so the declared doc edit had no landing step an implementer could follow.

#### MATERIAL — F4

> Tests: `tests/afk-runner/cli.test.ts` (resolution/precedence/rejection, pin total); `tests/afk-runner/config*.test.ts` contracts unchanged.
- **Question**: Is the proposal's test-touch list accurate, given the design itself plans edits to config.test.ts and agent-seam.test.ts?
- **Code evidence**: tests/afk-runner/config.test.ts:66 asserts the defaulted workDir as .sdd-runner — the D5 default flip breaks it, so a config*.test.ts contract does change (the design's own Risks row plans the update); tests/afk-runner/agent-seam.test.ts:75 calls defaultCliDeps('/tmp') and must change for the D7 takes-config signature, yet agent-seam.test.ts appears in no Impact list. Repo answer: the Impact line understates the test surface the design itself plans.
- **Resolution**: edited — proposal.md's Impact Tests line now reads: cli.test.ts (resolution/precedence/rejection, pin total); config.test.ts default-flip expectation updated (config-strict.test.ts strict-rejection and metered contracts unchanged); agent-seam.test.ts updated for the takes-config defaultCliDeps signature. Evidence: tests/afk-runner/config.test.ts:66 asserts the defaulted workDir as .sdd-runner and breaks under the D5 flip; tests/afk-runner/agent-seam.test.ts:75 calls defaultCliDeps('/tmp') and must change for D7.

#### NITPICK — F5

> `tests/afk-runner/config.test.ts` asserts the defaulted `workDir` as `.sdd-runner` (two cases)
- **Question**: Are there really two defaulted-workDir assertions to update in the red step?
- **Code evidence**: Only config.test.ts:66 ('defaults workDir and budget when omitted') asserts the defaulted value; line 56 asserts explicit-input resolution (.sdd-runner in, .sdd-runner out) and survives the default flip unchanged, and line 205 is a schema fixture input, not a default assertion. One defaulted case, not two.
- **Resolution**: edited — design.md's risk row corrected from '(two cases)' to '(one case, the "defaults workDir and budget when omitted" test — the explicit-.sdd-runner-input case resolves input to output and survives the flip)'. Evidence: config.test.ts:66 is the only defaulted-workDir assertion — line 56 asserts explicit-input resolution (workDir: '.sdd-runner' in, out) and survives the flip, line 205 is a schema fixture input; config-strict.test.ts uses .sdd-runner only as explicit inputs.

#### NITPICK — F6

> **THEN** both resolve the work dir to the same standing default path
- **Question**: Does 'the same standing default path' hold when the present config file declares a repoRoot other than cwd?
- **Code evidence**: config.ts:106-107 resolves the schema-defaulted workDir against the file's repoRoot, which D3 explicitly lets differ from cwd; with repoRoot ≠ cwd the two launches resolve different absolute paths sharing only the .afk-runner leaf. The scenario is universally true only when the file's repoRoot equals cwd — wording should say 'the same standing default' or pin repoRoot=cwd in the scenario.
- **Resolution**: edited — specs/afk-runner-cli/spec.md scenario 'One default stands in both places' WHEN clause now pins the roots equal: 'a config file whose `repoRoot` equals the standing root omits `workDir`, and another launch from that same root has no config file at all', making 'the same standing default path' literally true. Evidence: afk-runner/src/config.ts:106-107 resolves the schema-defaulted workDir against the file's own repoRoot, which D3 lets differ from cwd.

### Round 2

**Verdict**: 0b 1m 1n · 2 resolved · 0 dismissed · open

#### MATERIAL — F1

> Rollback: `git revert` of the change — the ignored `.afk-runner/` directory any launched runs left behind stays covered by the ignore entry (kept per D5 even after revert, since the entry is one line and old checkouts depend on the sibling `.sdd-runner/` line anyway).
- **Question**: Does a plain `git revert` of this change leave a leftover `.afk-runner/` work dir ignore-covered, as the Migration Plan claims?
- **Code evidence**: Answered from the repo: no. The change's own edit list adds `.afk-runner/` to `.gitignore` (proposal Impact Code line; design D5 '.gitignore adds `.afk-runner/` under the runner-state section'), and `.gitignore` today carries only `.sdd-runner/` (line 70) — the design Context itself states '`.gitignore` covers `...`.sdd-runner/` but not `.afk-runner/`'. `git revert` of the change reverses the .gitignore hunk too, restoring exactly that uncovered pre-change state, so a leftover `.afk-runner/` dir surfaces as untracked dirt — the precise failure the requirement 'Standing default work dir is ignore-covered' exists to prevent. The parenthetical's justification ('old checkouts depend on the sibling `.sdd-runner/` line') argues for `.sdd-runner/`, which revert does keep, but that line never covers `.afk-runner/`; 'kept per D5 even after revert' names no D5 decision that survives revert. The plan should state the real rollback step: re-add the one ignore line (or delete the leftover dir) after reverting, or revert keeping the .gitignore hunk.
- **Resolution**: edited — Confirmed from the repo: .gitignore carries only .sdd-runner/ (line 70, no .afk-runner/ match), and the change itself adds the .afk-runner/ entry (proposal Impact; design D5), so git revert reverses the ignore hunk and the old Migration Plan claim ('stays covered by the ignore entry kept per D5 even after revert') was false. Edited openspec/changes/task/design.md Migration Plan step 3 to state the real rollback: git revert, then re-add the .afk-runner/ ignore line (or delete the leftover work dir), explicitly noting the surviving .sdd-runner/ line never covered .afk-runner/.

#### NITPICK — F2

> The front-door command doc (`.claude/commands/sdd-auto.md` and its `.opencode` twin) SHALL document only flag forms the `start` argument parsing accepts — over its whole body, the launch-configuration prose included.
- **Question**: Does the pin as extended by D9 enforce the whole-body claim over every flag form the doc could name, or only backticked lowercase tokens?
- **Code evidence**: Answered from the repo: only backticked lowercase tokens. The pin's extractor documentedFlags() in tests/afk-runner/cli.test.ts:195-203 matches the regex /`(--[a-z][a-z0-9-]*(?: [^`]*)?)`/gu — backtick-delimited, first char strictly [a-z] — and the exact-inventory assertion (line 231) runs over its output, so a plain-text (non-backticked) or uppercase `--token` in the new launch-configuration prose passes the pin while violating the 'over its whole body' clause. D9 extends the pin to read both twins but says nothing about broadening extraction; the design's risk row covers only the opposite failure (prose that does backtick a `--` token). Pre-existing mechanism limitation inherited, not introduced — hence NITPICK; fixing it is either one regex broadening or softening the requirement's 'over its whole body' to the pin's operational reach.
- **Resolution**: edited — Confirmed from the repo: documentedFlags (tests/afk-runner/cli.test.ts:195-203) matches /`(--[a-z][a-z0-9-]*(?: [^`]*)?)`/gu — backtick-delimited, lowercase-initial only — so the pin enforces only backticked lowercase -- tokens and a plain-text or uppercase -- token in the new prose would pass while violating the spec's whole-body clause; a pre-existing, inherited limitation. Edited design.md D9 to record the pin's lexical reach explicitly (extractor-bound enforcement floor; the requirement's whole-body clause remains the doc-authoring bound), rather than softening the spec requirement or broadening the regex outside this artifact set.

### Round 3

**Verdict**: 0b 1m 1n · 2 resolved · 0 dismissed · open

#### MATERIAL — F1

> Docs: `docs/architecture/afk-runner.md` (CLI/config rows), `docs/architecture/commands.md`.
- **Question**: What row or section of docs/architecture/commands.md does this change edit? The Impact Docs line and design Test-first order step 3 ("the two architecture docs the proposal's Impact declares (`docs/architecture/afk-runner.md` CLI/config rows, `docs/architecture/commands.md`)") both schedule a commands.md edit, but unlike afk-runner.md (whose landing spots are named — the CLI row at docs/architecture/afk-runner.md:25 and the config.ts row at line 21), neither artifact states what changes in commands.md. If no edit is needed there, the declaration should drop it; if one is intended (e.g., adding the missing `afk-runner:start` script row with its launch-config surface), the artifact should say so, exactly as r1 F3 required for afk-runner.md.
- **Code evidence**: Answered from the repo as far as it goes, and the answer is 'no landing spot exists': rg 'afk-runner' docs/architecture/commands.md yields exactly one match, line 81 — the TDD-hook/mutation scope predicate (isGateableImplFile path list), which launch-configuration resolution cannot affect (the change adds no path outside afk-runner/src/ already in scope). The doc is 105 lines under headings Commands / Hermetic story qualification / TDD Enforcement (Hooks) / Stop hooks; it documents bun test/check scripts only, has no `afk-runner:start` row (package.json:50 defines the script; it is documented nowhere in commands.md), no runner launch/config section, and no `.sdd-runner`/`.afk-runner` content (rg confirms zero matches). rg 'sdd-auto|sdd:auto' over the same file: zero matches. Nothing in commands.md becomes stale or incomplete due to this change, so the declared edit names no content an implementer could write — the mirror image of the r1 F3 gap, now specifically for the commands.md half that r1 F3's fix pulled into step 3 without naming its rows.
- **Resolution**: edited — Dropped the commands.md edit from both artifacts: proposal.md's Impact Docs line now names only docs/architecture/afk-runner.md (CLI/config rows) and records why commands.md needs no edit; design.md Test-first step 3 now schedules one architecture doc with the same in-place justification. Reviewer's evidence independently verified: commands.md's only 'afk-runner' match is line 81 (the isGateableImplFile hook-scope path list, unaffected by launch-config resolution — the change adds no path outside afk-runner/src/); the doc's 105 lines cover bun test/check/build script semantics, story qualification, TDD hooks, and stop hooks, with zero matches for sdd-auto, sdd:auto, .afk-runner, or .sdd-runner — nothing goes stale, so the declaration named no writable content. Adding an afk-runner:start row was declined as scope expansion: the change does not alter that script's semantics (the ladder changes what the CLI resolves, not what the script runs), and documenting an undocumented script is not this change's work.

#### NITPICK — F2

> the file path and five-key shape
- **Question**: Should the launch-configuration section (D9) also document the optional `metered` key? The loader's accepted file shape is five keys plus the optional `metered` (afk-runner/src/config.ts:55, FiveKeySchema), and an explicit value overrides the derivation (`autonomyOf`, config.ts:116-125) — `metered: false` with a numeric budget launches unmetered, `metered: true` with `budget: null` launches metered. The D9 doc-content enumeration documents `budget: null` ⇒ unmetered but omits the explicit override key, and the spec delta likewise says only "the metered derivation where `budget: null` means unmetered" — so a real, load-bearing file key stays undiscoverable from the front-door doc this change exists to create.
- **Code evidence**: Answered from the repo: config.ts:49-56 shows FiveKeySchema carries the sixth optional `metered` key inside the strict object; tests/afk-runner/config.test.ts:212-217 ('accepts the optional metered key and a null budget') and tests/afk-runner/config-strict.test.ts:48-57 (the metered:false fixture) pin both acceptance and effect, delivered by the archived afk-runner-metered-budget change ('the optional `metered` flag (derived `budget !== null`)' per docs/architecture/afk-runner.md:144). The five-key naming is inherited, not introduced — the main spec's own 'Five-key strict schema' requirement (openspec/specs/sdd-runner-config/spec.md:11) carries the same blind spot — and the ladder takes the file wholesale so behavior is unaffected; hence NITPICK, a doc-completeness omission in the D9 enumeration rather than an incorrect statement.
- **Resolution**: edited — design.md D9's doc-content enumeration now includes 'the optional `metered` override key (an explicit value beats the `budget !== null` derivation)' beside 'budget: null ⇒ unmetered', so the front-door doc this change creates documents every key the file accepts. Verified from the repo: FiveKeySchema carries the optional sixth key (afk-runner/src/config.ts:55), autonomyOf honors an explicit override via 'config.metered ?? config.budget !== null' (config.ts:120), acceptance and effect are pinned by tests/afk-runner/config.test.ts:212-217 and tests/afk-runner/config-strict.test.ts:48-57, and docs/architecture/afk-runner.md:144 already uses the matching 'optional metered flag (derived budget !== null)' phrasing, so the edit follows established wording rather than coining new surface. No pin impact: `metered` is a backticked config key, not a --flag token, so the exact ['--depth'] inventory assertion is unaffected.

### Round 4

**Verdict**: 0b 1m 2n · 2 resolved · 1 dismissed · open

#### MATERIAL — F1

> Tests: `tests/afk-runner/cli.test.ts` (resolution/precedence/rejection, pin total); `tests/afk-runner/config.test.ts` default-flip expectation updated (the `config-strict.test.ts` strict-rejection and metered contracts unchanged); `tests/afk-runner/agent-seam.test.ts` updated for the takes-config `defaultCliDeps` signature.
- **Question**: Does proposal.md stay under the 500-word limit the proposal rules set, and which of its sentences are design-level detail that already lives in design.md and should move (or trim) to satisfy it?
- **Code evidence**: Answered from the repo as a consistency check: it does not. Counting openspec/changes/task/proposal.md with the SPDX comment lines excluded gives 594 whitespace tokens, 569 after dropping pure markdown-punctuation tokens — over the limit either way. openspec/config.yaml rules.proposal states verbatim: "Keep proposals under 500 words; detail belongs in design.md". The quoted Impact Tests line and the Docs-line commands.md justification ("`docs/architecture/commands.md` needs no edit: it documents bun-script semantics, story qualification, and hook policy — its only `afk-runner` mention is the hook-scope path list (`isGateableImplFile`), which this change does not touch") are both restated nearly verbatim in design.md (Test-first order steps 2–3 and the in-place commands.md justification), so trimming them from the proposal loses no information and brings it under the rule. Not a known concern from r1–r3; the class is MATERIAL only because the rule carries a hard number — correctness and implementability are unaffected.
- **Resolution**: edited — openspec/changes/task/proposal.md trimmed from 594 to 498 words (SPDX lines excluded, reviewer's method), satisfying openspec/config.yaml rules.proposal 'Keep proposals under 500 words'. Removed design-level detail restated in design.md: the Tests-line parentheticals (default-flip expectations, config-strict carve-outs, takes-config signature) and the Docs-line commands.md justification (both live verbatim in design.md Test-first order steps 1-3), plus tightened What-Changes/capability/Non-goals phrasing. Every rules.proposal clause still holds: scope-impact line, Non-goals section, per-capability without-it justifications, existing-capability references, and specs/docs references are all retained.

#### NITPICK — F2

> loaded through the five-key contract unchanged — strict unknown-key rejection with the offending key named and its replacement pointer given, and the metered derivation where `budget: null` means unmetered
- **Question**: Should the newly-ADDED sdd-runner-config requirement name the optional `metered` override key (as design D9 and docs/architecture/afk-runner.md:144 already do), given the merged main spec's Five-key strict schema requirement still reads as accepting exactly five keys?
- **Code evidence**: Answered from the repo: the divergence is real but pre-existing, so NITPICK. The loader accepts a sixth optional key — afk-runner/src/config.ts:55 `metered: z.boolean().optional()`, honored via `config.metered ?? config.budget !== null` at config.ts:120, pinned by tests/afk-runner/config.test.ts:212-217 and tests/afk-runner/config-strict.test.ts:48-57 — while openspec/specs/sdd-runner-config/spec.md:11 (Five-key strict schema) says "SHALL accept exactly `repoRoot`, `workDir`, `model`, `budget` (USD, default 5), and `deadline`". That gap was created by the archived afk-runner-metered-budget change (which modified afk-runner-autonomy's R4 requirement, not this one) and this change's delta leaves it untouched while D9's front-door doc will advertise the key. The delta's "unchanged" claim is literally accurate, and the metered derivation is mentioned; only the override key goes unnamed. Distinct from r3 F2, which fixed design.md D9's doc enumeration, not this spec delta's wording.
- **Resolution**: dismissed — Pre-existing divergence outside this delta's scope: the stale five-key enumeration lives in the merged Five-key strict schema requirement, which the archived afk-runner-metered-budget change left untouched when it added the optional `metered` key (afk-runner/src/config.ts FiveKeySchema), and this change's ADDED precedence requirement deliberately claims the contract 'unchanged' — while the override key is already named verbatim in design.md D9 and docs/architecture/afk-runner.md:144, so restating schema enumeration inside the precedence requirement would duplicate the Five-key requirement's job without repairing its stale text.

#### NITPICK — F3

> the repository's ignore file SHALL cover that path
- **Question**: Which scheduled check verifies the ignore-coverage half of the new 'Standing default work dir is ignore-covered' requirement, or is that half accepted as review-only repo state?
- **Code evidence**: Answered from the repo: nothing scheduled verifies it. design.md's Test-first order (steps 1–4) and proposal.md's Impact Tests line name cli.test.ts, config.test.ts, agent-seam.test.ts, the pin extension, the two command docs, and the `.gitignore` entry itself, but no executable check reads `.gitignore`; rg over tests/ and scripts/ finds no test asserting gitignore coverage of any path (the 'gitignore' matches are scripts/check.sh, test-audit, stryker-bin, and unrelated review-loop/opencode fixtures). The requirement's other half — schema default and CLI fallback resolving to the same path — is naturally covered by the planned `resolveRunnerConfig` rung matrix plus the config.test.ts default-flip expectation (both rungs read DEFAULT_WORK_DIR per D2/D4), and the tree-clean scenario ("THEN all of that bookkeeping sits under the standing default work dir, which the ignore file covers") follows mechanically from that equality plus the one-line diff. NITPICK: repo state, not behavior; flagging so the verifier records the scenario as diff-reviewed rather than test-pinned.
- **Resolution**: evidence-answered — No scheduled check verifies ignore coverage, confirmed against the repo: no test under tests/ or scripts/ asserts .gitignore content (all 'gitignore' matches are scripts/check.sh, stryker-bin, run-actionlint, test-audit, and unrelated review-loop/opencode-agent fixtures), and design.md's Test-first order names no such check — step 4 lands the .gitignore entry as a reviewed one-line diff. The requirement's other half (schema default, CLI fallback, and lookup path all reading DEFAULT_WORK_DIR) is pinned by the planned resolveRunnerConfig rung matrix plus the config.test.ts default-flip expectation; the ignore-coverage half is accepted as review-only repo state, recorded here so the verifier treats the tree-clean scenario as diff-reviewed rather than test-pinned.
