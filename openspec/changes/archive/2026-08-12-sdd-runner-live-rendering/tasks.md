## 1. Reporter adapter (design D1)

- [x] 1.1 Failing test in `tests/sdd-runner/agent-reporter.test.ts`: `createAgentReporter(label, emit)` returns a `ProgressReporter` whose `slot('resolver-r1', 'resolver-r1 ▶ readFile foo.ts · 4s · 3 tools')` emits `{ altitude: 'L0', type: 'tool_use', agent: 'resolver-r1', tool: 'readFile', arg: 'foo.ts' }` on the passed `emit` callback; `slot(key, null)` emits nothing; an unrecognized slot shape emits `{ tool: '(unknown)' }` rather than throwing. `usage({ input, output, reasoning, cost })` emits `{ altitude: 'L0', type: 'step_finish', agent: label, tokens: { input, output, reasoning }, costUsd: cost }`. `dynamic` returns `false`. `event`/`log`/`live`/`clearLive`/`diff`/`issue`/`statusSuffix` are no-ops (do not call `emit`). Verify: `bun test tests/sdd-runner/agent-reporter.test.ts` (fails)
- [x] 1.2 Implement `sdd-runner/src/agent-reporter.ts`: `createAgentReporter(label, emit)` returning a `ProgressReporter` per design D1. Tolerant regex parse of the slot line; fall back to `(unknown)` tool with full line as `arg`. Verify: `bun test tests/sdd-runner/agent-reporter.test.ts`; `bun run typecheck`

## 2. Wire reporter at the runAgent call (design D1)

- [x] 2.1 Failing test in `tests/sdd-runner/agent-layer.test.ts`: when `runStageAgent` calls `runAgent`, the call options include a `reporter` constructed from the agent's `label` and the layer's `emit`. Assert via a spy on the spawn path that captures the prompt AND via DI of `emit` (the existing test fixture captures emit calls; extend it to assert that spawning the `estimator` agent produces at least one `tool_use` event for that label when the fake spawn simulates an opencode stdout line). Verify: `bun test tests/sdd-runner/agent-layer.test.ts` (fails)
- [x] 2.2 Wire the adapter in `sdd-runner/src/agent-layer.ts:140-155`: construct `const reporter = createAgentReporter(options.label, deps.emit)` and pass `reporter` in the `runAgent({...})` options. No other changes to the call site. Verify: `bun test tests/sdd-runner/agent-layer.test.ts`; `bun run typecheck`; `bun test tests/sdd-runner/` (no regressions)

## 3. Render DoneEvent usage + split LineRenderer (design D6)

- [x] 3.1 Update `tests/sdd-runner/renderer.test.ts`: existing `renderEvent` test for `done` asserts the new shape `${agent} done · in ${inputTokens} out ${outputTokens} · $${costUsd.toFixed(4)}` (use `formatTokenCount`-style abbreviation, e.g. `12k`). Also add a failing test: `createRenderer(stream, 'normal', { dynamic: false })` returns a `Renderer` whose `renderEvent` output is byte-identical to today (append-only, no ANSI escapes) regardless of `stream.isTTY`. Verify: `bun test tests/sdd-runner/renderer.test.ts` (fails)
- [x] 3.2 Refactor `sdd-runner/src/renderer.ts`: extract current `renderEvent` body into a `LineRenderer` class; update `formatEvent`'s `done` branch to render usage; `createRenderer` becomes a picker (design D2) that returns `LineRenderer` when `opts?.dynamic !== true || stream.isTTY !== true || verbosity === 'brief'`, otherwise `DynamicRenderer` (placeholder import — `DynamicRenderer` lands in §4). Verify: `bun test tests/sdd-runner/renderer.test.ts`; `bun run typecheck`

## 4. DynamicRenderer (design D3 + D4)

- [x] 4.1 Failing test in `tests/sdd-runner/live-renderer.test.ts`: a `DynamicRenderer` constructed with an in-memory stream (`isTTY: true`, `columns: 80`) and default `ReplayState` redraws a multi-line block on each event. Assertions on the written chunk after a `stage_enter review` + `round_open 1/3` + `tool_use resolver-r1 readFile foo.ts` + `done resolver-r1` sequence: the chunk contains the pipeline map (`▶ review active (round 1/3)`), the slot line (`resolver-r1 ▶ readFile foo.ts`), and the status line (elapsed + tokens). Asserts `\r` and ANSI `\u001b[2K` appear in the output. Also: with `isTTY: false`, the renderer writes no ANSI escapes. Verify: `bun test tests/sdd-runner/live-renderer.test.ts` (fails)
- [x] 4.2 Implement `sdd-runner/src/live-renderer.ts`: inline the ANSI primitives (`ERASE_LINE`, `CURSOR_DOWN`, `cursorUp`), `writeBlock`/`clearBlock`/`writeSafe`/`fit`/`truncate`, and `formatDuration`/`formatTokenCount`/`MIDDLE_DOT` per design D4. Export `DynamicRenderer` with a `renderEvent(event)` method that maintains pipeline map (top), slot map (middle), status line (bottom), and redraws the block on each call. Wire it into `createRenderer`'s picker (replace the §3 placeholder). Verify: `bun test tests/sdd-runner/live-renderer.test.ts`; `bun test tests/sdd-runner/renderer.test.ts`; `bun run typecheck`

## 5. Thread verbosity end-to-end (design D5)

- [x] 5.1 Failing test in `tests/sdd-runner/orchestrator.test.ts`: `StartOptions` accepts `verbosity?: Verbosity` and the value reaches the harness's `createRenderer` call (assert via a tracking renderer factory injected through a new DI seam in `buildHarness`, OR assert that the parsed `CliCommand.verbosity` flows through `main` to `harness.runStart`). Update `tests/sdd-runner/cli.test.ts` to assert `cmd.verbosity` is forwarded to `runStart`. Verify: `bun test tests/sdd-runner/orchestrator.test.ts tests/sdd-runner/cli.test.ts` (fails)
- [x] 5.2 Thread verbosity: `StartOptions` gains `verbosity?`; `main` passes `cmd.verbosity`; `buildHarness(verbosity: Verbosity = 'normal')` constructs `createRenderer(process.stdout, verbosity)`. `runEntry` in `index.ts` parses verbosity from argv before building the harness (defer to `parseCliArgs` for the start subcommand). Verify: `bun test tests/sdd-runner/`; `bun run typecheck`

## 6. Docs + final verification

- [x] 6.1 Update `docs/architecture/sdd-pipeline.md`: add a "Live rendering" subsection under "Commands" noting the dynamic-vs-line picker, the verbosity modes, the slot/status layout, and the non-TTY fallback. Cross-reference the dormant `--wait`/`renderGateScreen` history (commit `fe3498040`). Verify: manual read
- [x] 6.2 Full verification: `bun test`, `bun run typecheck`, `bun run lint`, `bun run knip`, `bun run format:check`, `bun security`, `openspec validate sdd-runner-live-rendering --strict`. Update any other affected `docs/architecture/*.md` pages surfaced by the run. Manual smoke: `bun run sdd-runner:start -- task.md --verbosity debug` on a TTY (operator-only — capture not required for the gate).
