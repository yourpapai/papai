<!-- GENERATED by afk-runner from findings/resolutions sidecars - regenerated wholesale per round. -->

## Review

### Round 1

**Verdict**: 1b 2m 2n · 5 resolved · 0 dismissed · open

#### BLOCKER — F1

> asserts each gateable root reported by `isGateableImplFile` — `src/`, `client/`, `plugins/`, `review-loop/src/`, `afk-runner/src/`, `opencode-agent/src/` — appears in both
- **Question**: The D7 pin (and the spec's matching SHALL 'route every source tree the mutation gate treats as product code') cannot pass without a check.sh behavioral change that the artifacts themselves forbid: today `plugins/` and `afk-runner/src/` are gateable roots but neither `is_license_header_file` nor `is_oxlint_scoped_file` routes them, while D7 states "`check.sh` itself needs no edit" and the Non-Goals state "No check.sh behavioral change". Which resolution is intended: add `plugins/*` and `afk-runner/src/*` arms to check.sh (amending the non-goal and the migration step), or scope the agreement invariant to the trees the shell enumerations already route (rewording the spec requirement and D7's pin list)?
- **Code evidence**: scripts/check.sh:55 and :68 — both functions' arms are `src/*|client/*|scripts/*|review-loop/src/*|opencode-agent/src/*|tests/*|drizzle.config.ts`; `grep -n 'plugins\|afk-runner' scripts/check.sh` returns zero hits, so staged `plugins/**` and `afk-runner/src/**` files fall to `*) return 1` and are never enumerated. `.hooks/tdd/test-resolver.mjs:22-34` confirms both trees are gateable (`isPlugins`, `isAfkRunner`). The D7 test as specified fails on master, contradicting Migration step 5 ("`check.sh` itself needs no edit") and proposal.md:26 which lists `scripts/check.sh` under Code impact while design D7 says no edit is needed.
- **Resolution**: edited — Resolved per the option the change's own prohibitions select (no check.sh behavioral change): the agreement invariant is scoped to the trees that are both gateable and routed, and the unrouted gateable roots are recorded as a known pre-existing divergence. Edited openspec/changes/task/design.md D7: the pin now asserts the routed∩gateable set (src/, client/, review-loop/src/, opencode-agent/src/) in both is_license_header_file and is_oxlint_scoped_file, adds a paragraph recording plugins/ and afk-runner/src/ as gateable-but-unrouted today (no arm matches; staged files fall to `*) return 1`), and records the declined alternative of widening the two staged arms (would silently attach license-header/oxlint checks to trees this change does not own). Migration step 5 ("check.sh itself needs no edit") now holds. Edited openspec/changes/task/specs/mutation-gate/spec.md requirement 'The shell check's workspace enumeration routes gated source trees': the second SHALL became an anti-regression pin (keep routing every tree that is gateable AND routed), the plugins//afk-runner divergence is explicitly recorded as out of scope, and the 'agree' scenario now WHEN-conditions on routed gateable trees. Verified in-repo before editing: scripts/check.sh:55/68 arms are `src/*|client/*|scripts/*|review-loop/src/*|opencode-agent/src/*|tests/*|drizzle.config.ts` (grep for plugins|afk-runner: zero hits); .hooks/tdd/test-resolver.mjs:22-34 gates both plugins/ and afk-runner/src/.

#### MATERIAL — F2

> bun test:mutate:file $(git ls-files 'opencode-agent/src/**/*.ts' ':(exclude)**/index.ts' ':(exclude)**/constants.ts') --update-baseline
- **Question**: The named seed recipe selects 18 of the 152 workspace files, not the whole scope: git pathspec `opencode-agent/src/**/*.ts` (without `:(glob)` magic) matches only files under `opencode-agent/src/phases/`, so the 134 top-level `opencode-agent/src/*.ts` files are never passed to the paired runner and get no floor. Should the command use `:(glob)opencode-agent/src/**/*.ts` (verified: 152 files) or the directory pathspec `opencode-agent/src/`?
- **Code evidence**: Ran `git ls-files 'opencode-agent/src/**/*.ts'` in the repo → 18 files, all under `opencode-agent/src/phases/`; `git ls-files -- ':(glob)opencode-agent/src/**/*.ts'` and `git ls-files 'opencode-agent/src/' | grep '\.ts$'` → 152 each (matches the design's own measured 134+18 count). As written, D4's command violates the spec scenario "Seeding covers the whole newly added scope"; step 6's verification ("verify every newly covered file has a record") would catch it only after the ~4h run was budgeted against the wrong file set.
- **Resolution**: edited — Fixed the seed recipe in openspec/changes/task/design.md D4: the command now uses ':(glob)opencode-agent/src/**/*.ts' with the excludes unchanged, plus a sentence marking the magic load-bearing and stating the failure mode (default pathspec matching requires a subdirectory below src/, so the bare form selects only the 18 src/phases/ files and drops the 134 top-level sources). Re-verified in-repo before editing: `git ls-files 'opencode-agent/src/**/*.ts'` → 18 files, all under opencode-agent/src/phases/; `git ls-files ':(glob)opencode-agent/src/**/*.ts'` → 152; `git ls-files 'opencode-agent/src/' | grep '\.ts$'` → 152. The corrected recipe satisfies the spec scenario 'Seeding covers the whole newly added scope'.

#### MATERIAL — F3

> The floors SHALL be committed before the widened scope gates any change
- **Question**: The spec's ordering SHALL contradicts the design's own landing order: Migration steps 1–5 land the widened predicate/globs (making the widened scope live for every branch on master) and only step 6 commits the floors ("commit `scripts/mutation/baseline.json` before any branch relying on the widened scope is gated" — weaker than 'before the widened scope gates any change'), and the Risks section accepts "the remainder rides the documented no-floor rules" during the window. Should the design commit the scope and the seeded baseline in one landing commit (or seed first), or should the spec be amended to permit a bounded unseeded window?
- **Code evidence**: spec delta requirement 'Newly covered files are floored before their first gated change' vs design.md Migration Plan steps 1–6 and Risks entry '[Seed run is ~4 h and exceeds a CI job's 90-minute ceiling as one command]'. The repo cannot resolve this — it is a contract-ordering decision between two artifacts of the same change; both readings are implementable, so the artifacts must pick one before apply.
- **Resolution**: edited — Resolved in favor of the spec's SHALL (floors committed before the widened scope gates any change) — the contract artifact — by making the design comply instead of weakening the spec, which also coincides with D8's single-landing-commit fingerprint rationale. Edited openspec/changes/task/design.md: (1) Migration Plan step 6 is now seed-on-branch + verify, and a new step 7 lands the toolchain/scope edits and the seeded scripts/mutation/baseline.json as ONE commit with no 'scope first, floors later' window, so the ordering holds by construction (old step 7 docs becomes step 8); (2) D8 gained the sentence that the landing commit also carries baseline.json so scope and floors go live together; (3) D5's errored-files bullet now requires retrying chunks until every newly covered file is floored with the landing waiting for a fully floored scope, reserving the no-floor rules for post-seed files; (4) the Risks seed entry dropped the 'remainder rides the documented no-floor rules' window wording and now requires whole-scope floors before commit. The spec delta was left untouched.

#### NITPICK — F4

> the TDD hook checks (`enforce-tdd.mjs`, `tdd-nudge.mjs`, `verify-tests-pass.mjs`, `verify-no-new-surface.mjs`, `snapshot-surface.mjs`, `verify-test-import.mjs`) all import this one function
- **Question**: False for one of the six: `verify-test-import.mjs` imports `{ isTestFile, resolveImplPath, testFileImportsImpl }`, not `isGateableImplFile` — it routes the workspace via D3's `resolveImplPath` widening, not via the D2 predicate edit. Consequence-neutral (the pipeline-routing conclusion still holds across D2+D3), but the design's evidence misattributes the mechanism.
- **Code evidence**: `.hooks/tdd/checks/verify-test-import.mjs:6`: `import { isTestFile, resolveImplPath, testFileImportsImpl } from '../test-resolver.mjs'`. The other five files do import `isGateableImplFile` (grep over `.hooks/tdd/checks/*.mjs`: enforce-tdd.mjs:10, tdd-nudge.mjs:10, verify-tests-pass.mjs:52, verify-no-new-surface.mjs:46, snapshot-surface.mjs:21).
- **Resolution**: edited — Corrected the misattributed mechanism in openspec/changes/task/design.md D2: removed verify-test-import.mjs from the list of checks that import isGateableImplFile and added a parenthetical stating it joins the workspace routing through D3's resolveImplPath/testFileImportsImpl widening instead, so D2+D3 together cover the whole Write/Edit TDD hook pipeline. Verified before editing: .hooks/tdd/checks/verify-test-import.mjs:6 imports { isTestFile, resolveImplPath, testFileImportsImpl }; isGateableImplFile is imported by enforce-tdd.mjs (line 10), tdd-nudge.mjs (line 10), verify-tests-pass.mjs:7, verify-no-new-surface.mjs:9, snapshot-surface.mjs:9. Conclusion of D2 (predicate edit routes changed-file selection, CI dispatch, and the predicate-consuming hook checks) unchanged.

#### NITPICK — F5

> (known inputs: ~8,897 lines / 59 files; ~107s per-file gate cost; measured shard-sizing constants)
- **Question**: The proposal's durable What-Changes text carries the stale S6-5 snapshot as a 'known input' while the design supersedes it with measured numbers (152 files / 25,205 lines) and explicitly calls the old figure stale. Should proposal.md carry the corrected measurement (or mark the old one as a superseded snapshot) so the surviving summary does not state a scope ~3x smaller than the one being gated?
- **Code evidence**: Measured in-repo: `find opencode-agent/src -name '*.ts' | wc -l` → 152; line count → 25,205. The stale figures originate from `opencode-agent/docs/remaining-findings-evaluation.md:37,142` ('8,897 lines', '59 files'), and design.md Context already flags: "the proposal's \"~8,897 lines / 59 files\" is a stale snapshot from the S6-5 finding".
- **Resolution**: edited — Updated openspec/changes/task/proposal.md to carry the corrected measurement: the 'Measure before committing to scope' bullet now reads 'measured 2026-09-03: 152 product files / 25,205 lines, superseding the stale S6-5 snapshot of ~8,897 lines / 59 files', and the Modified Capabilities rationale's '~8.9k lines of shipped code' became '~25.2k lines (152 files; the ~8.9k/59-file S6-5 snapshot is stale)' — the same stale figure appeared there and would have kept understating the scope ~3x. Both figures re-verified in-repo before editing: find opencode-agent/src -name '*.ts' → 152 files; wc -l → 25,205 total.

### Round 2

**Verdict**: 1b 0m 2n · 3 resolved · 0 dismissed · open

#### BLOCKER — F6

> bun test:mutate:file $(git ls-files ':(glob)opencode-agent/src/**/*.ts' ':(exclude)**/index.ts' ':(exclude)**/constants.ts') --update-baseline
- **Question**: Does the D4 seed recipe select the 152-file scope it is required to measure? No — run on this repo (git 2.50.1), the command inside $(...) selects 0 files: `git ls-files ':(glob)opencode-agent/src/**/*.ts' ':(exclude)**/index.ts' ':(exclude)**/constants.ts'` -> 0, while the positive pathspec alone -> 152. Isolating the pieces: adding ANY unanchored wildcard exclude to a positive pathspec zeroes the whole selection (`:(exclude)**/index.ts` alone -> 0; even `:(exclude,glob)**/index.ts` -> 0; likewise for `src/providers/*.ts` + the same exclude -> 0), whereas tree-scoped globbed excludes work: `:(glob)opencode-agent/src/**/*.ts` + `:(exclude,glob)opencode-agent/src/**/index.ts` + `:(exclude,glob)opencode-agent/src/**/constants.ts` -> 151 (152 minus the one index.ts; zero constants.ts exist). The D4 sentence 'verified: the :(glob) form selects all 152' validated only the positive pathspec, never the full command with its excludes — the same recipe-level failure class F2 fixed for the positive half. Downstream the failure is silent: paired-run.ts iterates `sourceFiles.forEach` (paired-run.ts:214) so an empty list runs nothing, prints `files=0 skipped=0 errored=0`, and with --update-baseline calls runUpdateBaseline (seed-from.ts:181) which seeds an empty map and returns exit 0 — so migration step 6 'run the chunked scoped seed (D5) and verify every newly covered file has a record' would look green while seeding nothing, and step 7 would land the widened scope without floors, the exact state the spec's 'Seeding covers the whole newly added scope' (THEN every newly covered product file has a floor) and D8's single-landing-commit rationale exist to prevent. Fix is local: scope and glob the excludes (the verified-working form above), or use the directory pathspec `opencode-agent/src/` with tree-scoped excludes.
- **Code evidence**: Ran the exact D4 command's $(...) with git 2.50.1 (Apple Git-155) in-repo: 0 files. Isolation runs: positive glob alone 152; +:(exclude)**/index.ts 0; +:(exclude,glob)**/index.ts 0; +:(exclude,glob)**/constants.ts alone 0; tree-scoped (exclude,glob) form 151; non-glob tree-scoped exclude fails the other way (152, index.ts not excluded). Cross-checked the same zeroing on src/providers: `git ls-files 'src/providers/' ':(exclude)**/index.ts'` -> 0. Read paired-run.ts:200-232 (empty sourceFiles -> no-op run, exit code via resolvePairedRunExitCode on an empty merge) and seed-from.ts:181-189 (runUpdateBaseline seeds perFile as-is and writes the snapshot).
- **Resolution**: edited — Reproduced the reported failure on git 2.50.1 in-repo: the D4 command's $(...) selects 0 files with the unanchored excludes (positive glob alone -> 152; +':(exclude)**/index.ts' -> 0; +':(exclude,glob)**/index.ts' -> 0), while the tree-scoped globbed form selects 151 (152 minus the one index.ts; zero constants.ts exist). Rewrote design.md D4's seed command to use ':(exclude,glob)opencode-agent/src/**/index.ts' / ':...(glob).../constants.ts', replaced the verification sentence (which had only ever validated the positive pathspec) with both load-bearing pathspec properties, and added the downstream silent-failure warning (pairedRun iterates sourceFiles.forEach, so 0 files seeds an empty map with exit 0) with the instruction to assert the expected count before trusting a green exit. This keeps spec scenario 'Seeding covers the whole newly added scope' and D8's single-landing-commit ordering achievable.

#### NITPICK — F7

> **Code:** `stryker.config.json`; `.hooks/tdd/test-resolver.mjs`; `scripts/mutation/coverage-map.ts`; `scripts/mutation/baseline.json`; `scripts/check.sh`.
- **Question**: Is the proposal's Impact code list consistent with the change's own final scope? No, on two counts. (1) It lists `scripts/check.sh` as impacted code, but the change's own artifacts say check.sh is not edited: Non-goals state 'no check.sh behavioral change', design D7 resolves to a pin test only ('a test in tests/scripts/check.test.ts ... extracts the path-prefix arms'), and Migration step 5 says '`check.sh` itself needs no edit' (the F1 resolution). (2) It omits `paired-run-cli.ts`, which D4 edits ('the flag slots into `paired-run-cli.ts` beside `--threshold=`') and Migration step 4 tests — design.md's own Context inventory ('the change extends `test-resolver.mjs`, `coverage-map.ts`, `paired-run-cli.ts`, `stryker.config.json` and `check.sh`'s pin tests') gets this right. The Impact list is the stale pre-F1 surface statement; an implementer scoping the diff from proposal.md alone would expect a check.sh edit (contradicting D7's pin) and could miss paired-run-cli.ts.
- **Code evidence**: Cross-read proposal.md Impact against design.md D4/D7 and Migration steps 4-5; confirmed scripts/check.sh appears in the Impact list while paired-run-cli.ts does not; confirmed D4/step 4 make paired-run-cli.ts a touched product file and D7/step 5 plus the Non-goals ('no check.sh behavioral change') make check.sh untouched. Verified in-repo that paired-run-cli.ts exists with the --threshold= parser (paired-run-cli.ts:21-45) that D4's flag slots beside.
- **Resolution**: edited — Confirmed both counts against the artifacts: proposal.md Impact listed scripts/check.sh as impacted code while D7/Migration step 5/Non-goals all keep check.sh unedited, and omitted scripts/mutation/paired-run-cli.ts which D4 and Migration step 4 edit (file verified at scripts/mutation/paired-run-cli.ts). Rewrote the Impact Code line: removed scripts/check.sh from the edited set and stated explicitly it is not edited (agreement lands as a pin test in tests/scripts/check.test.ts per D7), and added scripts/mutation/paired-run-cli.ts (gains --update-baseline).

#### NITPICK — F8

> Full 152-file set (post-seed re-measure) | wants ⌈14,553/270⌉ = 54 shards | capped at `DEFAULT_SHARD_CAP` = 12 (the measured knee) → ≈20 min wall
- **Question**: Where does the 270s divisor come from? It matches no constant in scripts/mutation/shard-sizing.ts: the derivation is budget = targetWall(360, DEFAULT_TARGET_WALL_SECONDS:54) − ORCHESTRATION_OVERHEAD_SECONDS(60, :47) − preparation = 300 with zero preparation, giving ⌈14,553/300⌉ = 49 wanted shards, not 54. (270 would require assuming ~30s of preparation, which D6 never states.) The conclusion is unaffected — 49 and 54 both cap at DEFAULT_SHARD_CAP = 12, and the ≈20 min wall figure holds (14,553/12 ≈ 20.2 min) — but D6's own standard is 'Named, measured numbers for every workspace-scoped decision', and 270 is neither.
- **Code evidence**: Read shard-sizing.ts:47-130: DEFAULT_TARGET_WALL_SECONDS = 360 (:54), ORCHESTRATION_OVERHEAD_SECONDS = 60 (:47), resolveShardBudgetSeconds = target − overhead − preparation (:69-75), resolveShardCount = ceil(total / max(budget, slowest)) capped at 12 (:96-101); confirmed no 270 constant exists. Re-checked the capped wall arithmetic (14553/12 ≈ 20.2 min) and that the slowest workspace file (300 lines ≈ 164s, verified via wc -l) stays under any budget variant, so perShard = budget in every case.
- **Resolution**: edited — Confirmed in scripts/mutation/shard-sizing.ts: DEFAULT_TARGET_WALL_SECONDS=360 (:54), ORCHESTRATION_OVERHEAD_SECONDS=60 (:47), resolveShardBudgetSeconds = target - overhead - preparation (:69-75); no 270 constant exists. Fixed design.md D6's shard-sizing table row: derivation now budget = 360 - 60 - 0 preparation = 300, wanted shards = ceil(14553/300) = 49 (was an unstated-270s / 54), capped at DEFAULT_SHARD_CAP=12 with the wall figure made explicit (14553/12 = ~20 min). Conclusion unchanged, as the finding noted; every number in the row is now traceable to a named constant per D6's own standard.

### Round 3

**Verdict**: 0b 4m 4n · 8 resolved · 0 dismissed · open

#### NITPICK — F9

> Goals: "Floors for all 152 newly measured files committed from one fresh seeding run before the widened scope judges its first change." — the same 152-file framing of the *measured* set recurs in D3 ("Editing any one workspace test invalidates all 152 workspace fingerprints"), D5 ("ΣW over the new scope: 152 × 12s + 0.505 × 25,205 ≈ 14,553s"), and D6 ("Full 152-file set (post-seed re-measure)"). But design D4 itself establishes the measured/gated set is 151: "The full command above selects 151 files — the 152 sources minus the one excluded `index.ts`" — index.ts is excluded from the mutate scope (D1) so it is never measured, never floored, and has no fingerprint. The Goals line as written is an acceptance check an implementer can never satisfy (waiting for a 152nd floor record that cannot exist), and D5's ΣW double-counts index.ts's 288 lines (correct ΣW ≈ 14,415s; 49 shards becomes 48). All conclusions survive: ≈4.0 h one-off, cap 12, ≈20 min wall — but the four spots should say 151 (or '152 sources, 151 measured') for consistency with D4 and r2's verified seed count.
- **Question**: D4's verified seed recipe selects 151 files (index.ts excluded from measurement), so do the Goals/D3/D5/D6 statements that say 152 measured files / floors / fingerprints / ΣW misstate the measured set the rest of the same design commits to?
- **Code evidence**: Verified in-repo: `git ls-files ':(glob)opencode-agent/src/**/*.ts' ':(exclude,glob)opencode-agent/src/**/index.ts' ':(exclude,glob)opencode-agent/src/**/constants.ts' | wc -l` → 151; `wc -l opencode-agent/src/index.ts` → 288 (so 152×12 + 0.505×25,205 = 14,553s overstates the seeded scope by 12 + 145.4 ≈ 157s, and ⌈14,553/300⌉ = 49 becomes 48 — both capped at DEFAULT_SHARD_CAP=12 in scripts/mutation/shard-sizing.ts:40, wall time unchanged); stryker.config.json exclusion pattern confirms index.ts can never be measured/floored/fingerprinted.
- **Resolution**: edited — design.md restated against the 151-file measured set everywhere: Context bullet now carries '151 files / 24,917 lines once the index.ts exclusion is applied'; Goals 'Floors for all 151 newly measured files'; D3 churn 'all 151 measured workspace fingerprints / ΣW ≈ 14.4k'; D5 ΣW recomputed to 151 × 12s + 0.505 × 24,917 ≈ 14,395s ≈ 4.0 h; D6 row 'Full 151-file measured set, ⌈14,395/300⌉ = 48 shards, 14,395/12 ≈ 20 min wall'; Risks bullet '151-file measured set'. All conclusions (≈4.0 h one-off, cap 12, ≈20 min wall) unchanged, now consistent with D4's verified seed count. Edited files: openspec/changes/task/design.md.

#### NITPICK — F10

> proposal.md Impact: "**Code:** `stryker.config.json`; `.hooks/tdd/test-resolver.mjs`; `scripts/mutation/coverage-map.ts`; `scripts/mutation/paired-run-cli.ts` (gains `--update-baseline`); `scripts/mutation/baseline.json`." — and design.md Context: "the change extends `test-resolver.mjs`, `coverage-map.ts`, `paired-run-cli.ts`, `stryker.config.json` and `check.sh`'s pin tests." Neither list names `scripts/mutation/paired-run.ts`, yet `test:mutate:file` is `bun scripts/mutation/paired-run.ts` (package.json:92) and paired-run.ts is the module that calls `parsePairedRunCliArgs` (line 239) and `resolvePairedRunExitCode` (line 261) — so the D4 behaviors "runUpdateBaseline seeds `baseline.json` ... exit 0" (the seed-not-gate exit) can only be wired in paired-run.ts; paired-run-cli.ts exports only the parse/resolve helpers. The design body (D4, migration step 4's "seed-not-gate exit") fully specifies the behavior, so this is an Impact-list completeness slip, not a design gap — but the diff-plan surface is one file short.
- **Question**: If the flag 'slots into `paired-run-cli.ts` beside `--threshold=`' with no other module named, where does the seed-not-gate exit actually get wired, given paired-run-cli.ts only exports arg-parsing and exit-code helpers?
- **Code evidence**: Verified in-repo: package.json:92 maps `test:mutate:file` → `bun scripts/mutation/paired-run.ts`; scripts/mutation/paired-run.ts:13,239,261 import and invoke parsePairedRunCliArgs/resolvePairedRunExitCode; scripts/mutation/paired-run-cli.ts contains only parsePairedRunCliArgs/resolvePairedRunCliUsageExitCode/resolvePairedRunExitCode — no runUpdateBaseline call exists there, so paired-run.ts must be edited for --update-baseline to have any effect.
- **Resolution**: edited — proposal.md Impact Code list now names scripts/mutation/paired-run.ts (wires the flag in its main — seeds via runUpdateBaseline, exits 0 instead of the threshold verdict); design.md Context file list and D4 both state the split: flag parsed in paired-run-cli.ts beside --threshold=, seed routing necessarily in paired-run.ts's main (the only place runUpdateBaseline is reachable from the test:mutate:file entrypoint); Migration step 4 tests/implementation amended to cover both files. Edited files: openspec/changes/task/proposal.md, openspec/changes/task/design.md.
