<!-- GENERATED by sdd-runner from JSON sidecars - regenerated wholesale per round. -->

## Assumptions

### A8: gate reopen is restricted to the latest settled gate of a run, refuses while any gate is pending or the named version is missing/never-settled, reverts a terminal run's status to the pre-settle stage state, and clears persisted deadline fields.
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: design.md D9/D11, spec audit + deadline requirements, the future gate reopen verb implementation, run-state.ts status transitions
- **Status**: open

### A1: The audit veto overturn command pairs --veto with --confirm-all (semantically 'keep every auto-accepted item except this one') rather than extending desugarFlags with a veto-only path — smallest change, uses an existing accepted flag combination.
- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: design.md D9, spec audit requirement wording, and the audit.ts implementation of the printed command; no code outside sdd-runner
- **Status**: open

### A9: A human-steered extend (steer.md at a round boundary or landing during a deadline wait) reuses R2's roundCap mechanics but never consumes autoExtendsUsed — the bound applies only to policy-decided extends.
- **Basis**: default
- **Confidence**: high
- **Blast radius**: design.md D6/D11, review-loop.ts steering reader, waiter extend routing
- **Status**: open

### A6: R3 'accept-items' on a mixed gate is partial pre-check-and-present (low-blast items pre-checked with decided-by annotations, gate still presented for the rest); R3 never settles a whole gate, and accept-items is excluded from the interventions-avoided count and reported as per-rule 'items auto-accepted'.
- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: design.md D3/D9, spec R3 + gains requirements, auto-policy.ts evaluator output shape, report.ts gains fold
- **Status**: open

### A7: The policy-settle path emits the L2 gate presented/answered pair, and the gate answered event (post-settle-tail) is the settle commit record: an auto_decision approve/extend without a paired gate answered is treated as not-decided on resume and excluded from gains counts.
- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: design.md D3 step 4/D5/D9, events.ts emission sites, replay.ts fold, deriveResumePoint behavior on resume-after-crash, report gains filter
- **Status**: open

### A14: R1/R2 integrity posture under agent-writable inputs: a resolver-sidecar parse failure yields an unknown review result (fail closed, human gate) rather than empty open lists, and R1 fires only when replay-folded open-finding counts agree with sidecar counts — bug tolerance, not an adversarial-security boundary.
- **Basis**: code-evidence
- **Confidence**: medium
- **Blast radius**: design.md D3 step 1 + Risks, gate-digest.ts readReviewResultFromSidecars behavior change, auto-policy.ts signal assembly
- **Status**: open

### A4: Staged steering directives persist to runs/<id>/steer.staged.json, written before the steer.consumed.<n>.md rename completes, reloaded on resume, cleared on settle/orphan.
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: design.md D3 step 0 and D6, review-loop.ts steering reader, gate seam staging read
- **Status**: open

### A3: Deadline re-arm consumption is persisted as a boolean RunState field `gateDeadlineReArmed` (not a counter and not a new event variant), set before the re-armed deadline is written.
- **Basis**: default
- **Confidence**: high
- **Blast radius**: design.md D11/D12, run-state.ts schema (additive field with default, same migration shape as autoExtendsUsed)
- **Status**: open

### A11: The deadline waiter settles a hand-edited gate file only after its mtime and content hash are unchanged for 3 consecutive 1s ticks (~3s), guarding against non-atomic editor writes and two-step edits being settled mid-edit.
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: design.md D11, the waiter's hand-edit pickup path
- **Status**: open

### A13: A steer.md extend landing during an early-gate deadline wait translates to the extend outcome (same as --extend); at a final gate it is warned about and skipped, since parseGateResponse rejects extend there.
- **Basis**: convention
- **Confidence**: high
- **Blast radius**: design.md D11, spec deadline requirement, waiter steer-consumption code
- **Status**: open

### A10: Mutual exclusion between concurrent deadline waiters is an exclusive-create claim file runs/<id>/gate-<n>.expiry-claim acquired after the state reload and before the ladder re-run; losers exit, and the file remains as an audit artifact.
- **Basis**: default
- **Confidence**: medium
- **Blast radius**: design.md D11, spec deadline requirement, the future waiter implementation in extend-round.ts/cli.ts
- **Status**: open

### A2: The deadline waiter is the default only for non-TTY flagless `gate resume`; TTY flagless resume keeps the interactive session, and a new --no-wait flag forces the immediate answer path.
- **Basis**: convention
- **Confidence**: high
- **Blast radius**: design.md D11, spec deadline requirement, cli.ts flag surface for `gate resume`
- **Status**: open

### A12: R3's per-assumption file evidence is a new required evidence: { files: string[] } field on the resolver output contract (review-model.ts + agent-layer.ts), cross-checked against artifact/materialize events; missing, empty, or unverifiable evidence classifies high-blast (fail closed).
- **Basis**: default
- **Confidence**: high
- **Blast radius**: design.md D1/D12, proposal Impact, spec R3 requirement, resolver prompt/schema and agent-layer sidecar schema changes, gate-digest.ts gatherAssumptions
- **Status**: open

### A5: `deadlineExpired` lives as a PolicySignals field (default false, set only by the waiter expiry path) rather than a post-hoc wrapper filtering evaluator output to conservative branches.
- **Basis**: convention
- **Confidence**: high
- **Blast radius**: design.md D1 interface and D11, auto-policy.ts signal assembly at the gate seams
- **Status**: open
