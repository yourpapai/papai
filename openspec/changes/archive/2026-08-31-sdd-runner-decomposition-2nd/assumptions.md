<!-- GENERATED by sdd-runner from JSON sidecars - regenerated wholesale per round.
     Do not hand-edit: change via gate-<n>.md or the sidecars. -->

## Assumptions

### A1: The oversize verdict rides the existing estimator sidecar (optional `oversize: boolean`, undefined reads false) and the `depth` event carries it, with `driver.newChange` moved after the estimator so the verdict precedes any scaffolding — the proposal's "planner sidecar says oversize" is realized as "the intake-stage sidecar carries the verdict; the planner sidecar then drafts", keeping `PlanSchema` as the planner spawn's `outputSchema` verbatim.

- **Basis**: default
- **Confidence**: medium
- **Blast radius**: intake schema, estimator prompt, depth-event shape, and scaffolding ordering for every run — single-run paths must stay byte-identical, so this is the most-pinned surface the change touches; a wrong oracle placement or ordering reshapes intake for all runs.

### A2: The parent observes a child's terminal state by reading the child's persisted `state.json` (read-only, tolerant: an unloadable state counts as not-done), and maps every non-completed terminal status (aborted/failed/stopped) to the two-valued `child_done` outcome `'failed'`, since part 1 pinned that enum.

- **Basis**: default
- **Confidence**: medium
- **Blast radius**: parent completion and failure semantics plus the event-log vocabulary; a wrong mapping misreports subtree health on resume and could complete a parent over a broken subtree.

### A3: Three additive optional event fields are needed beyond part 1's shapes — `depth.oversize`, `child_spawned.runId`, `child_done.usage` (`AgentUsageSchema`, optional; absent usage forces `costKnown: false`) — following the `done.model` additive precedent; old lines parse unchanged.

- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: event schemas, replay, and any external `events.ndjson` consumer; a non-additive change breaks every pre-existing log.

### A4: The aggregate budget ledger fails closed — tree spend = parent done-events + Σ `child_done.usage`, unknown usage counts against the budget never as headroom — and the parent passes its committed spend into nested runs as a baseline so each child's own R4 guard sees parent + prior children against the single configured `budget`.

- **Basis**: default
- **Confidence**: medium
- **Blast radius**: tree-wide budget enforcement and the five-key config contract (no new key may appear); an optimistic ledger would let auto-decides spend past the ceiling.

### A5: The plan-gate prelude reduces to the R4 budget guard alone — no rule can approve or extend a plan gate in this part — with the guard's projection computed as spent + childCount × the conservative per-round constant.

- **Basis**: convention
- **Confidence**: high
- **Blast radius**: autonomy behavior at the plan gate; an unwanted auto-settle would start child spend without human sign-off.

### A6: Parent calm-stop writes the active child's stop marker through the existing cross-process marker seam, honored at the child's next stage/round boundary; the parent then consumes its own marker and settles through the shared stopped-result tail, leaving both nodes resumable.

- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: stop/resume interplay across the tree; a missed propagation orphans a stop marker and the child runs past the request.

### A7: The plan gate is decided through the gate-file protocol and the decision flags only; the interactive TUI session keeps early/final views until part 3 widens it to the tree view.

- **Basis**: default
- **Confidence**: medium
- **Blast radius**: interactive operator UX at the plan gate only; artifacts, events, and non-TTY behavior are unaffected.

### A8: The proposal's tick targets (2.3, 4.1–4.3, 5.1–5.4) do not exist under that numbering in the part-1 change folder's `tasks.md` (its numbering is 1.1–4.1, data-layer tasks); this change's own `tasks.md` carries the authoritative list, and the part-1 checkboxes are ticked by cross-reference or renumbered in the same commit.

- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: process bookkeeping only — no runtime behavior; a mismatch left unfixed just leaves stale checkboxes.

