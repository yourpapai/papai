<!-- GENERATED by sdd-runner from JSON sidecars - regenerated wholesale per round. -->

## Assumptions

### F4: Plan-gate veto rounds are unbounded: each veto round triggers exactly one plan revision incorporating that round's redirects, the gate re-presents at the next version, and the loop continues until every box is checked (approve) or ABORT is written. There is no lifetime revision cap; ABORT remains available in every round.
- **Basis**: code-evidence
- **Confidence**: high
- **Blast radius**: sdd-runner plan-gate operator contract before any child executes; single-run gates and post-settlement behavior untouched; worst case is operator-controlled gate ping-pong, exitable via ABORT
- **Status**: open
