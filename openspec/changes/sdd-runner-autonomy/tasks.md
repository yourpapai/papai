# Tasks: sdd-runner autonomy

Prerequisites (external to this change, verify before starting): `openspec/changes/shared-tui-renderer/` is landed (runway, consumed as-is) and `openspec/changes/sdd-runner-extend-round/` Shape-B semantics are present in `extend-round.ts` (sequencing dependency). Every new/changed file under `sdd-runner/src/**` is TDD-hook-gated: write the failing test first, then implement — each task below is one red-green cycle whose verification command must pass at completion.

## 1. Event schema and replay fold (D5)

- [x] 1.1 Add the `auto_decision` L2 event variant (`AutoDecisionEvent` in `EVENT_VARIANTS` and `SddEventSchema`) to `events.ts`: `{ altitude: 'L2', type: 'auto_decision', rule: R1–R5|'none', decision: preview|approve|extend|accept-items|gate, evidenceDigest, gateVersion }`; malformed records rejected; old logs without the type still parse (TDD: failing schema tests first). Verify: `bun test tests/sdd-runner/events.test.ts`
- [x] 1.2 Fold `auto_decision` into `ReplayState.autoDecisions` in `replay.ts foldEvent` so replay alone rebuilds observe previews (proposal invariant 5) (TDD: failing fold tests first). Verify: `bun test tests/sdd-runner/replay.test.ts`

## 2. Autonomy config and CLI resolution (D2)

- [x] 2.1 Add the `autonomy` block (`level`, `costCeilingUsd`, `autoExtendMax`, `deadlineMinutes`, `rules`) to `RunnerConfigSchema` in `config.ts` with safe defaults (`observe`, `5.0`, `1`, off, `{}`); `rules` entries naming R4/R5 parse but are ignored (never-cut invariants); mirror the defaults into `config.example.json` (TDD: failing parse tests first). Verify: `bun test tests/sdd-runner/config.test.ts`
- [x] 2.2 Add `--autonomy <level>` and `--auto-deadline <minutes>` to `start`/`resume`/`continue` in `cli.ts` (per-command; CLI > config > default; effective-ceiling normalization `min(budgetUsd ?? +∞, costCeilingUsd)`; fixture proving `budgetUsd` alone never gated anything before this change) and thread the resolved `AutonomyConfig` onto `OrchestratorDeps` so `auto-policy.ts` never sees the CLI (TDD: failing tests first). Verify: `bun test tests/sdd-runner/cli.test.ts tests/sdd-runner/orchestrator.test.ts`

## 3. R3 evidence schema on resolver sidecars (D1)

- [x] 3.1 Require a per-assumption `evidence: { files: string[] }` field in `review-model.ts ResolverOutputSchema` and `agent-layer.ts AssumptionRecordSchema`; sidecars missing/emptying it fail validation (TDD: failing schema tests first). Verify: `bun test tests/sdd-runner/review-model.test.ts tests/sdd-runner/agent-layer.test.ts`

## 4. Policy engine: pure module `auto-policy.ts` (D1)

- [x] 4.1 Implement `classifyAssumptions` (R3) in the new pure module `sdd-runner/src/auto-policy.ts` (no I/O, no clock, no config reads): low-blast iff all referenced files are inside the change folder/run dir, no spec delta (`specs/**/spec.md`), no `tasks.md` checkbox line; missing/empty/un-cross-checkable `evidence.files` or unparseable sidecar → high-blast (fail closed); agent `blast_radius` text never consulted (TDD: failing table-driven tests first). Verify: `bun test tests/sdd-runner/auto-policy.test.ts`
- [x] 4.2 Implement `evaluateFinalGate` / `evaluateCapHit` and the decision ladder in `auto-policy.ts` (types `AutonomyLevel`, `PolicySignals`, `PolicyDecision`): R1 converged-final-approve predicates; R2 trajectory predicates (0 BLOCKERs, ≥1 open MATERIAL, strictly decreasing open findings over last k=2 rounds, `autoExtendsUsed < autoExtendMax`, projected spend under effective ceiling); R4 exceedance and `costKnown === false` fail-closed (every evaluator returns `action: 'gate'` on unknown cost); R5 reversibility boundary; ladder returns first applicable decision else `action: 'gate'`; rule toggles honored for R1/R2/R3 only (TDD: failing table-driven tests first). Verify: `bun test tests/sdd-runner/auto-policy.test.ts`

## 5. Gate grammar extension (D4)

- [x] 5.1 Extend `renderGateAnswers` (`gate-answers.ts`) and `parseGateResponse` (`gate-model.ts`) with the optional `· decided-by: policy Rx` trailing annotation on answered checkbox lines and a decision-level `decided-by:` line under the `## Gate response` header; parser recognizes and strips the suffix; round-trip holds; hand-edited files without the annotation parse exactly as today (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate-answers.test.ts tests/sdd-runner/gate-model.test.ts`
- [x] 5.2 Strip the `### Auto-decision preview` section (header to next `## `/`### ` header or EOF) in `parseGateResponse` before processing, so a hand-mangled preview cannot become gate input (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate-model.test.ts`
- [x] 5.3 Accept an optional `decidedBy` field on `GateAnswers` and assert it survives the `settleAnswers` write-then-parse self-check in `gate-session.ts` (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate-session.test.ts`

## 6. Policy prelude at the gate seams (D3) — observe level first

- [x] 6.1 Implement signal assembly (build `PolicySignals` exactly once per gate from sidecars/replay/usage) plus the observe-level preview path in the three `presentGateAt` call sites (`runPostReviewToGate`, `post-review-tail.ts`, `runExtendRound`), write order gate file → `auto-policy.jsonl` → `auto_decision` event → state save; `settleVeto`'s re-presentation call site deliberately gets no prelude (TDD: failing integration fixture first — at `observe`, behavior and non-TTY output byte-identical to pre-change except the additive `> `-prefixed, parse-inert `### Auto-decision preview` block, one appended `auto-policy.jsonl` line per gate, one `auto_decision` event with `decision: 'preview'`). Verify: `bun test tests/sdd-runner/gate.test.ts tests/sdd-runner/post-review-tail.test.ts tests/sdd-runner/extend-round.test.ts`
- [x] 6.2 Implement the step-1 integrity cross-checks in the signal-assembly seam: a resolver sidecar failing `ResolverOutputSchema.parse` yields unknown review result → human gate (never silent empty-open-lists fallback); R1 requires replay-folded open-finding counts from `events.ndjson` to agree with sidecar counts, mismatch → human gate (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate.test.ts`

## 7. assist/auto auto-settle path (D3 step 4)

- [x] 7.1 Extract `verifyGateIntegrity` (artifact-hash verification, drift check, `human_edits` emission from `resumeGate`) into a shared helper in `gate.ts` and export `settleApprovedGate` from `extend-round.ts`; prove both the human path and the policy path call the same integrity helper (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate.test.ts tests/sdd-runner/extend-round.test.ts`
- [x] 7.2 Wire the step-4 auto-settle path using `settleApprovedGate` + `verifyGateIntegrity` (write order: gate file + `gate-hashes-<version>.json` sidecar → `gate presented` event → integrity verification + settle tail → `gate answered` event → `auto_decision` event → state save) (TDD: failing integration fixture first — `assist` converged run reaches `completed` with zero prompts; auto-settled gate file has pre-checked boxes and `decided-by: policy R1` annotations; the L2 event pair carries decided-by attribution; no `state.gate` pending entry remains). Verify: `bun test tests/sdd-runner/extend-round.test.ts`
- [x] 7.3 Implement resume reconciliation for unpaired `auto_decision` events: an `auto_decision` approve/extend without its paired `gate answered` is treated as not-decided on resume (prelude re-runs idempotently); crash before state save re-presents the gate (TDD: failing crash-consistency tests first). Verify: `bun test tests/sdd-runner/extend-round.test.ts tests/sdd-runner/gate.test.ts`
- [x] 7.4 Implement the step-0 never-cut pre-checks in the prelude: open BLOCKER skips the ladder entirely at every level; `costKnown === false` presents the human gate; the pre-settle `steer.md` + `steer.staged.json` check lets a queued abort/veto take precedence over a pending auto-decision (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate.test.ts tests/sdd-runner/review-loop.test.ts`
- [x] 7.5 Implement the R3 accept-items partial path in the prelude: mixed gate gets low-blast items pre-checked with `decided-by: policy R3`, `auto_decision` with `decision: 'accept-items'` emitted, gate still presented to the human; accept-items never counts as an intervention avoided (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate.test.ts`
- [x] 7.6 Implement the step-5 undecidable path: human gate presented unchanged plus preview block, `auto_decision` event with `decision: 'gate'` (`rule` naming the matched-but-unpermitted rule or `'none'`), and the policy-debt ledger append to `<workDir>/policy-debt.jsonl` performed by the seam (single writer) (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate.test.ts`

## 8. R2 trajectory auto-extend (D3 R2 specifics)

- [x] 8.1 Add `RunState.autoExtendsUsed` (default 0) with persistence in `saveRunState`, incremented and persisted before the extended round starts spending, so a crash mid-extended-round still consumes the bound and resume falls through to the normal early gate when the predicate no longer holds (TDD: failing tests first). Verify: `bun test tests/sdd-runner/run-state.test.ts`
- [x] 8.2 Route R2 trajectory auto-extend into the existing `runExtendRound` unmodified: strictly decreasing burndown at cap-hit auto-extends exactly one round with one trajectory row and one `auto_decision` event, no prompt; flat/increasing trajectory presents the human gate; `autoExtendsUsed` bound enforced (TDD: failing fixtures first). Verify: `bun test tests/sdd-runner/extend-round.test.ts`

## 9. Queued steering (D6)

- [x] 9.1 Implement the steering reader in `review-loop.ts`: `steer.md` parsed line-by-line against the fixed grammar (`extend`, `veto <id>=<redirect>`, `abort`); unknown directives/unknown veto ids surfaced as renderer warn lines and skipped, never fatal, never an `events.ndjson` variant; directives never interpolated into shell/paths/prompts (TDD: failing tests first). Verify: `bun test tests/sdd-runner/review-loop.test.ts`
- [x] 9.2 Implement round-boundary consumption in `review-loop.ts`: cheap `existsSync`+read at the round-cap evaluation point; staged set written to `steer.staged.json` before the rename to `steer.consumed.<n>.md` completes (crash mid-tick re-consumes idempotently); append-only audit; resume-after-crash reloads the staged set (TDD: failing tests first). Verify: `bun test tests/sdd-runner/review-loop.test.ts`
- [x] 9.3 Re-read persisted `state.roundCap` at each round boundary so a steered `extend` takes effect at the next boundary and does not consume `autoExtendsUsed` (TDD: failing tests first). Verify: `bun test tests/sdd-runner/review-loop.test.ts`
- [x] 9.4 Implement veto staging/orphaning and `abort` translation in the gate seam: staged vetoes pre-fill the next presented gate; a veto staged into a gate that gets auto-decided first is recorded as orphaned on the renderer and dropped; `abort` short-circuits to `finalizeGate aborted` at the next gate (TDD: failing tests first). Verify: `bun test tests/sdd-runner/review-loop.test.ts tests/sdd-runner/gate.test.ts`

## 10. Audit verb, gate reopen, and policy-debt reporting (D9)

- [x] 10.1 Implement `sdd-runner audit <runId>` in new `sdd-runner/src/audit.ts` and wire the verb in `cli.ts` (`VALID_SUBCOMMANDS`, `CliCommand` union, harness method): resolve run id via the path-traversal-safe `resolveRunId`; list per real decision (`approve`/`extend`/`accept-items` only — no `preview`/`gate`/`none` records) the rule, evidence digest, and the copy-pasteable overturn command `sdd-runner gate reopen <runId> --gate <n> && sdd-runner gate resume <runId> --confirm-all --veto <id>=<redirect>` (or `--abort`); observe runs yield no reconsider entries; policy-debt ledger reported with `(rule, hash(evidenceDigest))` dedupe + count, read-only in `audit` (TDD: failing tests first). Verify: `bun test tests/sdd-runner/audit.test.ts tests/sdd-runner/cli.test.ts`
- [x] 10.2 Implement `sdd-runner gate reopen <runId> --gate <n>` in `gate.ts`/`cli.ts`: refuse when a gate is already pending, when `gate-<n>.md` does not exist/was never settled/is the currently pending version, or when `<n>` is not the latest settled gate; re-render the gate at a fresh version as an unanswered digest (boxes unchecked, answered section cleared, digest sections carried over); write a fresh `gate-hashes-<freshVersion>.json` over current artifacts (old hashes never copied forward); revert terminal `completed` status to the pre-settle stage state; clear `gateDeadlineAt`/`gateDeadlineReArmed`; set `state.gate` pending so existing veto/abort resume mechanics apply (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate.test.ts tests/sdd-runner/cli.test.ts`

## 11. Report gains block (D9)

- [x] 11.1 Implement the gains block in `report.ts buildReport`: `auto_decision` `approve`/`extend` events paired with a subsequent `gate answered` fold count as interventions avoided (unpaired events never count); `accept-items` reported separately as per-rule items auto-accepted; `preview`/`gate`/`none` excluded; output shape `interventions avoided: N · human gates: M · ~wall-time saved` per rule id; wall-time estimate = N × median human-gate dwell from `gate presented`→`gate answered` timestamps, fixed 5-min constant without history, `~`-labeled, formula in one helper (TDD: failing tests first). Verify: `bun test tests/sdd-runner/report.test.ts`

## 12. Auto level dead-man deadline (D11)

- [x] 12.1 Implement the deadline field lifecycle on `RunState` and in the presentation seam: at `auto` with `deadlineMinutes`, a presented gate records `gateDeadlineAt` via `saveRunState`, prints the bell/notification line, and the process exits without blocking; presentation overwrites `gateDeadlineAt` when configured and clears both deadline fields when not; both fields cleared on any settle and on `gate reopen` (TDD: failing tests first). Verify: `bun test tests/sdd-runner/run-state.test.ts tests/sdd-runner/gate.test.ts`
- [x] 12.2 Implement the foreground waiter `gate resume --wait-deadline`: default for flagless non-TTY `gate resume` on a deadline-pending gate; TTY flagless keeps the interactive session (deadline shown in header); `--no-wait` forces the immediate path; 1s polling reloads `state.json` and `gate-<n>.md` from disk every tick; external settle observed → clean exit; hand edits honored by parsing the gate file each tick and settling through normal `resumeGate` (integrity check included) only when answered and stable (mtime + content hash unchanged for 3 consecutive ticks); waiter polls `steer.md` with the same rename-on-consume protocol, translating landing `abort`/`veto` to `--abort` / `--confirm-all --veto` outcomes, landing `extend` to the extend outcome at an early gate, skipping `extend` with a warn line at a final gate (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate.test.ts tests/sdd-runner/cli.test.ts`
- [x] 12.3 Implement deadline expiry handling: reload state immediately before any write; exclusive-create `gate-<n>.expiry-claim` (`wx`) claimed after reload and before the ladder re-run — losing waiter exits; ladder re-run with `deadlineExpired: true` permits only conservative branches (R1 approve, else R2 extend, else stay pending); re-arm persisted via `gateDeadlineReArmed` set before writing the re-armed deadline; re-arm at most once, never auto-abort, second expiry leaves the gate pending indefinitely; an already-passed deadline is processed on first observation (TDD: failing tests first). Verify: `bun test tests/sdd-runner/gate.test.ts`

## 13. Tier 0 output polish (D10)

- [x] 13.1 Append `· <model>` to `formatEvent`'s `done` branch in `renderer.ts` when the `done` event carries a model id, and render the pre-change line exactly (no placeholder) when it does not (TDD: failing golden-byte tests first). Verify: `bun test tests/sdd-runner/renderer.test.ts`
- [x] 13.2 Implement `renderPipelineMap` per-stage wall time/cost on completed stages (from `stage_enter`/`stage_exit` folds) and the animated elapsed marker on the active stage in `renderer.ts`/`live-renderer.ts` (TDD: failing snapshot tests first). Verify: `bun test tests/sdd-runner/renderer.test.ts`
- [x] 13.3 Implement slot lines in `live-renderer.ts`: wide-char-aware truncation via the local `Intl.Segmenter` + range-table helper (no dependency), retry badges from `retrying` events, done slot line `<agent> done · <model> · in X out Y · $Z` (TDD: failing snapshot tests first). Verify: `bun test tests/sdd-runner/live-renderer.test.ts`
- [x] 13.4 Implement the status line in `live-renderer.ts`: ETA from median completed-round duration, reasoning-tokens segment only when count > 0, right-aligned numeric columns (TDD: failing snapshot tests first). Verify: `bun test tests/sdd-runner/live-renderer.test.ts`
- [x] 13.5 Implement the gate trajectory sparkline in `gate-render.ts`: unicode glyphs `▁▂▃▄▅▆▇` beside per-round counts, pure text, one glyph per round (TDD: failing snapshot tests first). Verify: `bun test tests/sdd-runner/gate-render.test.ts`
- [x] 13.6 Implement the terminal title `\x1b]0;sdd <change> · <stage>\x07` on stage transitions, TTY-only, best-effort restore via `process.on('exit')` + SIGINT/SIGTERM handlers registered by `index.ts` (never the renderer itself), fixed default restore string when the prior title cannot be queried (TDD: failing tests first). Verify: `bun test tests/sdd-runner/index.test.ts tests/sdd-runner/renderer.test.ts`
- [x] 13.7 Implement `--verbosity quiet` parsed on `start`/`resume`/`continue`/`gate` (per-command, not persisted in `RunState`) in `cli.ts`, `LineRenderer`/`DynamicRenderer`, and the `presentGateAt` output path: suppresses the stream to the final summary plus the always-kept operational lines (gate-pending path + `Next:` resume hint, deadline bell/notification lines, steering warn lines, one-line halt record `halted at gate <n>: <path>` when no final summary exists) (TDD: failing tests first). Verify: `bun test tests/sdd-runner/cli.test.ts tests/sdd-runner/renderer.test.ts tests/sdd-runner/live-renderer.test.ts`

## 14. clack gate-session front-end (D7)

- [x] 14.1 Add `@clack/prompts` as a workspace-local dependency in `sdd-runner/package.json`. Verify: `bun install && bun --cwd sdd-runner -e "await import('@clack/prompts'); console.log('ok')"`
- [x] 14.2 Implement `sdd-runner/src/clack-prompter.ts` (`clackPrompter(): Prompter` adapting `confirm`/`select`/`text`/`spinner` to `ask`/`say`, cancel symbol mapping to the `null` abandon signal) and select it in the composition root's `makePrompter` (`index.ts`) when interactive, with readline fallback (`SDD_NO_CLACK=1` or non-UTF8 terminals); `extend-round.ts collectGateDecision` and session walkthrough logic untouched (TDD: failing parity tests with a mocked `@clack/prompts` first — clack, flag, and hand-edit paths produce identical `GateResponse`s, version consumption, and run state). Verify: `bun test tests/sdd-runner/clack-prompter.test.ts tests/sdd-runner/index.test.ts`

## 15. Watch verb (D8)

- [x] 15.1 Add `ink` + `react` deps and `ink-testing-library` devDep to `sdd-runner/package.json`; keep the Ink import dynamic so `start`/`resume`/`gate` startup cost is unchanged. Verify: `bun install && bun --cwd sdd-runner -e "await Promise.all([import('ink'), import('react'), import('ink-testing-library')]); console.log('ok')"`
- [x] 15.2 Implement the watch view in `sdd-runner/src/watch-view.ts` (default `React.createElement`, no JSX to avoid tsconfig churn): four regions — pipeline map + stage times, scrollable findings list, live burndown + `autoDecisions` list, per-agent slots folded from `spawned`/`retrying`/`done` into the `live-renderer.ts` slot model (TDD: failing `ink-testing-library` string-snapshot tests first). Verify: `bun test tests/sdd-runner/watch-view.test.ts`
- [x] 15.3 Implement the replay-then-tail engine in `sdd-runner/src/watch.ts` and wire the `watch` verb in `cli.ts`: replay via `createReplayFolder`, record the consumed byte offset, poll `fs.stat` mtime/size on a 500ms interval, fold only bytes past the offset (event appended between replay and first poll folded exactly once); exit on terminal `state.json` status (polled read-only), on 60s idle on a terminal log (with idle-exit notice), and on q/Ctrl-C with explicit cursor restore; watch is read-only — no writes to the run dir; run id resolved via `resolveRunId` with path separators rejected (TDD: failing tests first). Verify: `bun test tests/sdd-runner/watch.test.ts tests/sdd-runner/cli.test.ts`

## 16. Docs

- [x] 16.1 Update `docs/architecture/sdd-pipeline.md` with autonomy levels, rules R1–R5, never-cut invariants, audit/watch/gate-reopen verbs, queued steering, deadline semantics, and the TUI decision. Verify: `grep -q 'R5' docs/architecture/sdd-pipeline.md && grep -q 'auto-extend' docs/architecture/sdd-pipeline.md && grep -q 'watch' docs/architecture/sdd-pipeline.md && grep -q 'reopen' docs/architecture/sdd-pipeline.md && grep -q 'steer' docs/architecture/sdd-pipeline.md`
- [x] 16.2 Verify `observe` byte-identity end-to-end against a pre-change run and confirm rollback is config-level (`level: "observe"`) per the Migration Plan. Verify: `bun test tests/sdd-runner`
