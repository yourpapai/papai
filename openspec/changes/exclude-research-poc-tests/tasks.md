# Tasks — exclude-research-poc-tests

## 1. Exclusion + script

- [x] 1.1 Add `"docs/**"` to `bunfig.toml` `pathIgnorePatterns` with a comment naming the lane (research PoC self-checks; runnable via `bun run test:research`), and add the `test:research` script to `package.json` running the poc tree via explicit path. Sequencing: land on master between story-refactor qualifications — if a qualification branch is active, wait. Verify: `bun run test:research` exits 0 and runs all 16 poc files (53 cases).
- [x] 1.2 Verify the default lane delta from the persisted report after a full `bun run test`: exactly 16 fewer files, 53 fewer cases, in-test total down ~37.9 s vs the recorded baseline; `tests/analytics/intent/taxonomy.test.ts` still present and green in the run (import unaffected). Record the three before/after numbers in this file.
  - Recorded (baseline `reports/test` run @ `fdee2d785` 2026-08-25 → final parallel run @ `73e515687` 2026-08-26): files 1553 → 1537 (−16 exact); cases 16672 → 16619 (−53 exact); junit in-test total 1084.1 s → 450.0 s. The raw in-time delta (−634 s) is host-speed dominated — this host ran every case ~2.2–2.4× faster than the baseline run (wall 240.7 s → ~130 s); the poc's own in-test share was 37.9 s inside the baseline run and 24.4 s in a same-host with-poc A/B (517.5 s with vs 448.8/450.0 s without; ±44 s same-code run-to-run contention noise), so the exclusion is corroborated at its measured share, not by cross-day totals. `tests/analytics/intent/taxonomy.test.ts` present, 6 tests / 0 failures.
- [x] 1.3 Verify no coverage-floor movement: run the coverage lane (`bun test:coverage` + `bun coverage:ratchet`) and confirm measured-vs-floor unchanged within noise (the excluded files imported no production code). Record measured/floor numbers in this file.
  - Recorded (`coverage:ratchet` output, full lane A/B on 2026-08-26): with poc measured lines 94.24% / functions 93.08%; without poc lines 94.16% / functions 92.90%; floor 90.00% / 90.00% both sides, `floor.json` untouched, ratchet exit 0 both sides. Delta −0.08pp lines / −0.18pp functions — within the lane's run-to-run noise (each run pools 60–90 ephemeral tmp-plugin fixture records). Attribution: src/plugins SF record count identical (1125 = 1125); the only pool change is 28 poc-module lcov records leaving (docs SF 33 → 5; the 5 survivors are poc *source* modules product tests import, e.g. `poc/intent/taxonomy.js`).
- [x] 1.4 `bun check` green; SPDX untouched files only (no new files expected). Verify: `bun check` exits 0; `git status` shows only `bunfig.toml` + `package.json` modified.
  - Recorded: `bun check` (staged on `bunfig.toml` + `package.json`) exits 0 — lint, typecheck, format:check, license-headers 4/4 ✓. `git status` shows exactly `bunfig.toml` + `package.json` modified (staged) plus this change's own artifacts (`openspec/changes/exclude-research-poc-tests/{design.md,tasks.md}`, which ride with the code per repo convention); no new files, no SPDX edits.
