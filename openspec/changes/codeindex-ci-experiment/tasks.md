## 1. Ledger document

- [x] 1.1 Write `docs/operations/codeindex-ci-experiment.md`: the four procedures — (a) revert: unset the `AGENT_MCP_SERVERS` repository variable, what inert YAML remains and the optional cleanup commit; (b) read statistics: per-job step summaries, which jobs to read (planning/implement), review-worker queries landing in the same log, the deeper layers (issue-comment spend lines vs. comparable pre-experiment issues, encrypted transcripts for call interleaving, job-log greps for `database is locked` / `query-log record failed` / the boot banner); (c) decide: the rubric — keep on meaningful usage (≈10+ queries, >50 % hit rate) on ≥1 exploration-heavy issue with no recurring errors and no token regression; revert on ~0 usage across ≥3 genuine planning/implement issues, or recurring lock/boot failures, or tokens up while usage ≈ 0; ambiguous → extend; (d) remove statistics after keeping: delete the report step + script, and `"logQueries": false` in `.codeindex.json` to stop query logging entirely. The ledger states the exact token-free variable content: `{"codeindex": {"type": "local", "command": ["bun", "run", "scripts/codeindex-cli.ts", "mcp"]}}`, where to set it (this repository carries the knob as a secret, which wins over the variable — the codeindex entry merges into the existing secret; a repo with no secret may use the variable), and the sibling-pin maintenance note. Verification: every procedure names its own commands; `rg -n 'revert|rubric|logQueries|AGENT_MCP_SERVERS'` finds each section.

## 2. Usage report script

- [x] 2.1 Write `scripts/codeindex-ci-report.ts` (with its SPDX header): read `.codeindex/queries.db` read-only via `bun:sqlite` (schema per the sibling's `storage/query-log.ts`: tool, queryText, resultCount, hit, latencyMs, error, timestamp), aggregate per tool — calls, hit rate, median/max latency, error count — plus top-10 query texts by frequency, canary count (entries matching the canary's fixed query text, excluded from model-usage counts and reported on their own line), explicit `code_index` request count, and the ledger link (absolute URL to the default-branch blob). Emit markdown to stdout; the workflow step redirects to `$GITHUB_STEP_SUMMARY` and the log. A missing or unopenable DB is a rendered empty-report line naming the path, never a throw. Config via DI-shaped input (queries path argument) so tests need no fixtures on disk beyond what they create. Verification: `bun run test tests/scripts/codeindex-ci-report.test.ts`.
- [x] 2.2 Write `tests/scripts/codeindex-ci-report.test.ts`: create a temp `queries.db` with the sibling's schema, insert a spread of entries (hits, misses, errors, latency spread, repeated query texts, canary text, an explicit `code_index` row), run the report, assert the aggregates — per-tool counts, hit rates, median/max ordering, top-queries truncation, canary exclusion and its separate line, the ledger link, the `code_index` count; plus the missing-DB empty-report path and read-only behavior (file mtime/content unchanged after run). Follow the `tests/scripts/` DI-first pattern.

## 3. Workflow wiring

- [x] 3.1 Edit `.github/workflows/agent-pipeline.yml` (protected path — maintainer-only by construction): add to the `agent` job, after `bun install`/client-bundle prep and before the pipeline step, four steps all gated on the knob declaring codeindex (`if: contains(env.AGENT_MCP_SERVERS, 'codeindex')`, read off the job-level env declaration `secrets.AGENT_MCP_SERVERS || vars.AGENT_MCP_SERVERS` — a step `if:` may read `env` but not `secrets`, and this repository carries the knob as a secret): (a) `actions/checkout` at the existing pinned version with `repository: yourpapai/codeindex`, `ref: <pinned SHA — the sibling HEAD verified during implementation>`, `path: ../codeindex`, `token: ${{ secrets.AGENT_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}`; (b) `bun install --frozen-lockfile` with `working-directory: ../codeindex`; (c) `bun run codeindex:index` (the prebuild — spec: index warm before any model turn; failure fails the job before pipeline); (d) canary `bun run scripts/codeindex-cli.ts symbol kaneoAutoProvision` whose empty result fails the step with output naming the canary and likely causes. After the pipeline step: the report step — `if: always() && contains(env.AGENT_MCP_SERVERS, 'codeindex')` — running `bun scripts/codeindex-ci-report.ts | tee -a "$GITHUB_STEP_SUMMARY"` (stdout already lands in the job log). Keep the comment voice of the file (why-ordering comments on each step). Verification: `bun workflows:lint`.
- [x] 3.2 Add the pointer in `opencode-agent/README.md`'s MCP-servers section: one short paragraph naming the experiment, the gated steps, and linking `docs/operations/codeindex-ci-experiment.md` as the operating procedure. Verification: `rg -n 'codeindex-ci-experiment' opencode-agent/README.md`.

## 4. Live verification

- [ ] 4.1 Operator-run live proof, outside the suite per repo convention: land the change (steps inert — variable unset), set the variable per the ledger, trigger one real agent run (a scratch issue is fine), and read the run page — canary line present in setup, usage report rendered on the job with the ledger link, model usage counts nonzero on at least one exploration turn, no `database is locked` in the job log. This run also settles the design's open assumption (the relative MCP command resolving against the opencode server's cwd). Record the outcome (run URL, one-line verdict) in the ledger document under a **Recorded runs** heading. Verification: the run page itself; `rg -n 'Recorded runs' docs/operations/codeindex-ci-experiment.md`.

## 5. Gates

- [x] 5.1 Full gates on the branch: `bun run test` (report-script tests included), `bun run typecheck`, `bun run lint`, `bun security`, `bun workflows:lint`, `openspec validate codeindex-ci-experiment --strict`.
