# Tasks — sdd-runner-decomposition

Test-first bottom-up per design.md Migration Plan; every step keeps the full suite green and non-oversized runs bit-identical. TDD: failing test in `tests/sdd-runner/` before the gated `sdd-runner/src/**` edit. New files (`sdd-runner/src/plan.ts`, its tests) need SPDX license headers from the first commit.

## 1. Additive schema foundations

- [ ] 1.1 Failing tests: `MAX_CHILDREN_PER_PLAN` and `MAX_NESTING_DEPTH` exported from `review-model.ts` next to `ROUND_CAPS`, positive integers, referenced by nothing else yet. Implement the two compiled constants. Verify: `bun test tests/sdd-runner/review-model.test.ts`
- [ ] 1.2 Failing tests in `events.test.ts`: old fixture `events.ndjson` lines parse unchanged; new L2 variants round-trip through `appendEvent`/`readEvents` — `oversize {verdict: boolean, rationale, source: 'prescreen'|'estimator'}`, `plan_rejected {rule, attempt}`, `child_started {runId, name}`, `child_finished {runId, outcome: 'completed'|'aborted'|'failed'|'stopped'}`. Implement additively in `events.ts`. Verify: `bun test tests/sdd-runner/events.test.ts`
- [ ] 1.3 Failing tests in `events.test.ts`: `StageIdSchema`/`STAGE_ORDER` accept `'plan'` and `'execute'`. Implement additively in `events.ts`. Verify: `bun test tests/sdd-runner/events.test.ts tests/sdd-runner/stage-machine.test.ts`
- [ ] 1.4 Failing tests in `events.test.ts`: `gate` event mode enum accepts `'plan'`. Implement additively in `events.ts`. Verify: `bun test tests/sdd-runner/events.test.ts`
- [ ] 1.5 Failing tests in `run-state.test.ts`: a legacy `state.json` with no tree fields parses unchanged (absence IS the legacy encoding, no backfill); `createRunState` defaults `kind: 'single'`, `childRunIds: []`; `parentId?`, `planPath?`, `oversize? {verdict, rationale}` survive save/load. Extend `run-state.ts`. Verify: `bun test tests/sdd-runner/run-state.test.ts`
- [ ] 1.6 Failing tests in `run-state.test.ts`: `gate.mode` accepts `'plan'` in `PersistedRunStateSchema`. Extend `run-state.ts`. Verify: `bun test tests/sdd-runner/run-state.test.ts`
- [ ] 1.7 Failing tests in `replay.test.ts`: folding logs containing the four new event types reconstructs the tree section (children with ids/outcomes, oversize verdict, plan rejections) in `ReplayState`; logs without them yield today's shape. Extend `replay.ts` folds. Verify: `bun test tests/sdd-runner/replay.test.ts`

## 2. Plan artifact schema and pure validator (`plan.ts`)

- [ ] 2.1 Failing tests in new `tests/sdd-runner/plan.test.ts`: `PlanSchema` parses a full valid document and rejects malformed ones — ordered `children[]` of `{name, scope, acceptance_signals[], coverage[]}`, top-level planner-emitted `intent_units[]` inventory, `declined[]` of `{units[], reason}`. Create `sdd-runner/src/plan.ts` (SPDX header). Verify: `bun test tests/sdd-runner/plan.test.ts`
- [ ] 2.2 Failing table-driven tests: `validatePlan(plan, bounds)` returns the failing-rule list for each case — coverage gap (unit neither covered nor declined), overlap (unit in two children or child+declined), empty scope/acceptance signals, duplicate or slug-invalid child name, name colliding with an existing `openspec/changes/` entry, count above `MAX_CHILDREN_PER_PLAN`, declined entry naming units outside the inventory, and incomplete partition alongside empty `declined`. Implement as a pure function. Verify: `bun test tests/sdd-runner/plan.test.ts`
- [ ] 2.3 Failing tests: synthesizing approved children writes `<parentRunDir>/children/<order>-<slug>.md` whose H1 is the composed `<parent-slug>--<child-slug>` string, followed by scope, acceptance signals, a parent-intent excerpt, and sibling position; deterministic across regeneration from the same plan; the composed H1 is exactly what the validator scans `openspec/changes/` against. Implement synthesis in `plan.ts`. Verify: `bun test tests/sdd-runner/plan.test.ts`

## 3. Oversize verdict at intake

- [ ] 3.1 Failing tests in `intake.test.ts`: `DepthClassificationSchema` requires an `oversize` object `{verdict, rationale, signals}` (spans-multiple-capabilities / more-than-one-change / shippable-in-isolation markers); prescreen keyword hint classifies sample texts; disagreement between prescreen hint and estimator resolves **toward oversize**; source attribution mapping ('prescreen' only when the hint wins over a negative estimator, else 'estimator'). Implement schema extension + pure mapping helpers in `agent-layer.ts`/`intake.ts`. Verify: `bun test tests/sdd-runner/intake.test.ts`
- [ ] 3.2 Failing tests in `intake.test.ts`: with an oversize-positive estimator, `runIntake` emits the `oversize` L2 event and persists the verdict into run state **before** `driver.newChange` is called (no change folder scaffolded); a negative verdict scaffolds exactly one change as today with unchanged emitted events; `--depth` override replaces only the S/M/L result while prescreen hint + estimator spawn still run for the oversize axis. Reorder classification ahead of scaffolding in `intake.ts`; wire persistence through `orchestrator.ts`. Verify: `bun test tests/sdd-runner/intake.test.ts tests/sdd-runner/orchestrator.test.ts`

## 4. Tree-wide budget and ladder rung R6

- [ ] 4.1 Failing tests in `usage-aggregate.test.ts`: subtree spend walks `childRunIds` recursively and sums each node's own event-derived usage; any unknown-cost node makes `costKnown` false (fail closed); a single (tree-less) run aggregates identically to today. Implement subtree roll-up input for `PolicySignals.spentUsd` threading at every ladder evaluation site inside any node. Verify: `bun test tests/sdd-runner/usage-aggregate.test.ts tests/sdd-runner/resume-budget.test.ts`
- [ ] 4.2 Failing tests in `auto-policy.test.ts`: new R6 plan-gate rung auto-approves only when validation passed AND zero declined entries AND the R4 projection is clear; R3 low-blast prechecking disabled for plan mode. Extend `auto-policy.ts`. Verify: `bun test tests/sdd-runner/auto-policy.test.ts`
- [ ] 4.3 Failing tests in `auto-policy.test.ts`: R4 projection evaluates against subtree spend and fails closed — cost unknown or projected exceedance forces the human gate; exceedance anywhere in the tree degrades any auto-decision to a human gate. Extend `auto-policy.ts`. Verify: `bun test tests/sdd-runner/auto-policy.test.ts`

## 5. Composite stage spine and resume derivation

- [ ] 5.1 Failing tests in `stage-machine.test.ts`: `remainingStages(from, kind)` — kind `'single'` keeps today's six-stage flow including the atomicity-at-S skip; kind `'composite'` follows `intake → plan → execute` and never enters `draft/review/decompose/atomicity/gate`; single-run observable behavior unchanged. Extend `stage-machine.ts`. Verify: `bun test tests/sdd-runner/stage-machine.test.ts`
- [ ] 5.2 Failing tests in `renderer.test.ts`: renderer dims skipped stages on composite runs exactly like atomicity-at-S. Verify: `bun test tests/sdd-runner/renderer.test.ts`
- [ ] 5.3 Failing tests in `resume-point.test.ts`: `deriveResumePoint` composite arm — plan-gate pending → `gate` with plan mode; plan settled but children incomplete → `execute` targeting the first non-completed child; completed children never re-derived for re-execution; legacy states derive exactly today's points. Extend `resume-point.ts`. Verify: `bun test tests/sdd-runner/resume-point.test.ts`

## 6. Plan stage: drafting, validation loop, approval gate

- [ ] 6.1 Failing tests (fake agent layer): new `AgentRole` value `'planner'` in `config.ts`; the plan stage spawns the planner via `runStageAgent` with the proposal-admission rules in the prompt and writes `plan.json` into the parent run dir through the `agentWritePath` sidecar seam; a validation rejection respawns with the failing rules appended (≤2 attempts, `plan_rejected` event per attempt); exhausting attempts throws `StageHaltError` naming the rule — loud, resumable, zero children started. Implement in `plan.ts` + `orchestrator.ts`. Verify: `bun test tests/sdd-runner/plan.test.ts tests/sdd-runner/orchestrator.test.ts`
- [ ] 6.2 Failing tests: plan gate presents each child as a checkbox row (scope + acceptance signals) built from `plan.json`, materialized through the assumptions path so the TUI gate screen, `--confirm-all`, hand-edited file protocol, write-then-parse self-check, and versioned `gate-<n>.md` audit trail work unchanged; checking every box settles approve. Implement mode `'plan'` in `gate-model.ts`/`gate-render.ts`/`gate-session.ts`. Verify: `bun test tests/sdd-runner/gate-model.test.ts tests/sdd-runner/gate-render.test.ts tests/sdd-runner/gate-session.test.ts`
- [ ] 6.3 Failing tests: leaving one child unchecked with a redirect triggers exactly one revision pass regenerating the affected child definitions (via `updateAssumptionsFromVetoes` + resolver-role spawn) and re-presents at `gate-<n+1>`; rounds repeat unbounded until all-checked or ABORT; ABORT aborts the parent pre-children via the normal path; no child change folder exists at any point before settlement. Implement the plan-mode veto loop mirroring `settleVeto` in `gate-resume-tail.ts`. Verify: `bun test tests/sdd-runner/gate-resume-tail.test.ts tests/sdd-runner/veto-updater.test.ts`

## 7. Child execution loop, completion, abort fan-out

- [ ] 7.1 Failing tests (fake drivers/spawns): the `execute` stage loops over approved children sequentially in plan order, each an in-process fresh `runStart` seeded with the synthesized child task file and a child `RunState` stamped `kind`/`parentId`/shared deps; parent log gains `child_started`/`child_finished` (outcome recorded) append-only; at most one descendant in flight; steer/veto directives during execution affect only the receiving run and never mutate `childRunIds`. Implement in `orchestrator.ts`. Verify: `bun test tests/sdd-runner/orchestrator.test.ts tests/sdd-runner/events.test.ts`
- [ ] 7.2 Failing tests: parent reaches `completed` only when every descendant completed; any descendant ending `aborted`/`failed`/`stopped` persists the parent as `stopped` (halted-but-resumable) and the report names the blocking descendant by persisted status; parent never reports success over an incomplete subtree. Implement in `orchestrator.ts`/`report.ts`. Verify: `bun test tests/sdd-runner/orchestrator.test.ts tests/sdd-runner/report.test.ts`
- [ ] 7.3 Failing tests: requesting stop/abort of any node writes calm-stop markers into every incomplete descendant of that node's subtree at abort-observation time (not at the parent's next loop boundary); siblings outside the subtree and completed nodes untouched; every affected node individually resumable afterwards. Extend `stop-controller.ts` fan-out. Verify: `bun test tests/sdd-runner/stop-controller.test.ts tests/sdd-runner/orchestrator.test.ts`
- [ ] 7.4 Failing tests: nesting depth threads down as a plain integer on child creation; a child whose intake returns oversize at `MAX_NESTING_DEPTH` halts that branch with `StageHaltError` naming the exhausted bound — no deeper recursion, no forced shallow change, resumable once the constant is raised; oversize within the bound decomposes normally. Implement depth plumbing in `orchestrator.ts`. Verify: `bun test tests/sdd-runner/orchestrator.test.ts tests/sdd-runner/intake.test.ts`

## 8. Routing, claims, tree-aware continue

- [ ] 8.1 Failing tests: driving any node claims its run dir via exclusive-create claim file and releases on drive end; an external `continue`/`resume` on a claimed node fails loudly naming the current holder (for a tree member, the driving ancestor's run id) and starts no second driver. Implement the claim seam in `cli-routing.ts`. Verify: `bun test tests/sdd-runner/cli-routing.test.ts`
- [ ] 8.2 Failing tests in `continue.test.ts`: continuing/resuming a composite parent routes to its active child's pending point — gate-pending child → that child's gate flow printing the child's concrete command and run id, interrupted child → resume, otherwise → start the next pending child; bare `continue` picks the sole gate-pending run wherever it sits in the tree; prefix resolution works across tree members and ambiguous prefixes fail loudly listing candidates. Implement in `continue.ts`. Verify: `bun test tests/sdd-runner/continue.test.ts tests/sdd-runner/run-state.test.ts`

## 9. Reporting, live tree, byte contract

- [ ] 9.1 Failing tests: `report.ts` shows the run tree — per-node status for every node and subtree-aggregated cost/token/duration on ancestors alongside their own figures, subtree totals equal to the sum of descendants; single-run reports unchanged. Extend `usage-aggregate.ts`/`report.ts`. Verify: `bun test tests/sdd-runner/usage-aggregate.test.ts tests/sdd-runner/report.test.ts tests/sdd-runner/report-footer.test.ts`
- [ ] 9.2 Failing tests: `live-view.ts`/TUI running screen renders parent/child structure from the `child_started`/`child_finished` fold (no new view state) and highlights the active node; attaching mid-child shows the tree position. Verify: `bun test tests/sdd-runner/live-view.test.ts tests/sdd-runner/tui-running.test.ts tests/sdd-runner/watch-view.test.ts`
- [ ] 9.3 Failing tests: non-TTY single-change output stays byte-identical modulo the done-line model id; composite runs add only `[plan]`/`[execute]` stage lines and tree event lines through the same LineRenderer contract; done lines without model id render exactly as before. Extend renderer byte-freeze tests. Verify: `bun test tests/sdd-runner/renderer.test.ts tests/sdd-runner/render-mode.test.ts`

## 10. Docs and end-to-end verification

- [ ] 10.1 Add the decomposition section to `docs/architecture/sdd-pipeline.md`: oversize axis, plan artifact + validation, plan gate + veto rounds, nested execution, tree state/resume/routing, bounds, tree-wide budget. Cross-check wording against implemented behavior. Verify: manual read against `sdd --help` and the spec
- [ ] 10.2 Full gate: `bun run test`, typecheck, lint, format; confirm zero observable difference for non-oversized runs (existing suites untouched pass); `openspec validate sdd-runner-decomposition --strict`. Verify: all green
