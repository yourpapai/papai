<!-- GENERATED by sdd-runner from JSON sidecars - regenerated wholesale per round. -->

## Assumptions

### A1: For /fix discovery, a check run counts as failed when its conclusion is `failure` or `timed_out`; `cancelled`, `skipped`, `stale`, `neutral`, `action_required` and `success` do not. The repo's only existing failure predicate (red-run.ts:43) is job-side and knows only `failure`, so it does not decide the check-run case; including `timed_out` is the least-surprise reading of GitHub's terminal conclusions (a timed-out check renders red on the pull request and carries output a fix round can address), recorded in design D7 and pinned by its step-(4) test.
- **Basis**: default
- **Confidence**: high
- **Blast radius**: Command-bought discovery only: opencode-agent/src/phases/ci-fix.ts's discovery branch, its FailedJob mapping, and the handler tests pinning the filter. The red-run door's job predicate (red-run.ts), every other phase, and all persisted state are untouched.
- **Status**: open
